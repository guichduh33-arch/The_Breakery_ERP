# Ruflo — Claude Code Configuration

## Rules

- Do what has been asked; nothing more, nothing less
- NEVER create files unless absolutely necessary — prefer editing existing files
- NEVER create documentation files unless explicitly requested
- NEVER save working files or tests to root — this is a pnpm/turbo monorepo: code goes in `apps/{pos,backoffice}/src`, `packages/{domain,supabase,ui,utils}/src`, or `supabase/{functions,migrations,tests}`. Co-locate tests in `__tests__/` next to the code.
- ALWAYS read a file before editing it
- NEVER commit secrets, credentials, or .env files
- Keep files under 500 lines
- Validate input at system boundaries

## Active Workplan

> Per-session history (S13→S49: specs, plans, INDEX files, numbered deviations) lives in
> `docs/workplan/{specs,plans}/` and `docs/superpowers/{specs,plans}/`, with merged sessions
> under `archive/`. **Do not duplicate that history here** — link to the dated file instead.
> Plans/specs are dated, append-only history: never rewrite a past plan; create a new dated file.

- **In flight:** rien — **clôture de caisse ②③ (comptage 3 volets + comptage par coupure) est LIVRÉE (S67)** : la clôture de caisse fiche 12 D2 est complète (①②③). Restent ouverts : chantiers **Vague 2** (facture PDF B2B ; CRUD customer categories + prix négociés ; E2E nightly — dégelé, le dev est officiellement le staging) · **Vague 3** (spec « mise en prod » — **REPORTÉE**, décision propriétaire 2026-07-07 : « attendre la fin totale du développement » ; snapshot COGS ; QC réception ; mode hors-ligne). **Prochaine session (S68)** : un chantier Vague 2 au choix. Dettes S67 : `docs/workplan/plans/2026-07-07-session-67-INDEX.md` (D-1..D-8, dont **D-1 : volet carte `card`+`edc` jamais testé avec de vraies lignes `order_payments`** et **D-8 : grille d'ouverture client-only, pas de RPC d'open**) ; dettes S66 : INDEX S66 (D-1..D-5 + **finding F-1 : le lockout `_verify_pin_with_lockout` des RPCs PIN-in-arg ne persiste pas les échecs — l'exception qui suit annule l'UPDATE du compteur ; touche les 6 RPCs S38, seul le chemin EF compte durablement**) ; dettes S65 : INDEX S65 (D-1..D-8 ; D-9 ✅ soldée S66) ; dettes S64 : INDEX S64 (D-1..D-7, dont **enforcement serveur des méthodes désactivées** — l'EF accepte toujours les 6) ; dettes S63 : INDEX S63 (D-1..D-12 ; I-1 ✅ fixé S64) ; dettes S62 : INDEX S62 (D-1..D-10).
- **Deferred (S51+) :** dettes « cutover sain » non soldées (#129) — POS-view `security_invoker` (casserait la cascade CASHIER/waiter), privatisation bucket `product-images`, `search_path` des fn INVOKER, Leaked Password Protection (manuel), secret repo optionnel `SUPABASE_ANON_KEY` pour les tests anon-path de la CI live-RPC (le secret `SUPABASE_SERVICE_ROLE_KEY` est présent depuis 2026-06-27 ; le `fetch failed` du job venait du fallback localhost `VITE_SUPABASE_URL`, corrigé S58 — cf. rapport T4) ; follow-ups S51 — `quote_order_pricing_v1` (reçu pré-paiement, si divergence constatée), isolation subagents.
- **Merged (latest):** **S67 — Clôture de caisse ②③ : comptage 3 volets + comptage par coupure (branche `swarm/session-67`).** **Fiche 12 D2.2 + D2.3 livrées** (B1.4 « trois volets » et B1.1/B2.5 « par coupure » deviennent réels) : **`close_shift_v4 → v5`** (migrations **`20260710000121..124`**, corps live DEV-S57-02, DROP v4, trio S20) — comptage **3 volets** cash/QRIS/carte (**carte = `card`+`edc` fusionnés**, décision propriétaire ; `p_counted_qris`/`p_counted_card` `DEFAULT NULL` = volet non compté, masqué si méthode désactivée S64), expected par volet miroir du calcul cash (orders `paid`), variances persistées (`pos_sessions.counted_qris/card`) + figées au **snapshot Z section `reconciliation`** (`_123` in-place, + `denominations`) et rendues PDF/BO **rétro-compatibles** (sections omises si clés absentes, zéro orphan header — fix revue DEV-S67-06) ; **gardes note (S60) et PIN (S66) étendues en OR sur les volets comptés** (mêmes seuils, pct relatif à l'expected du volet, DETAIL nomme les volets) ; **aucune JE non-cash** (décision propriétaire — un écart QRIS/carte est un décalage de settlement, trace + gardes seulement ; la JE cash 1110↔4910/5910 est byte-identique). **Comptage par coupure opt-in** : flag **`business_config.shift_denomination_count_enabled`** (défaut OFF, toggle BO Settings via settings RPCs `_124` in-place) ; grille IDR canonique **`packages/domain/src/cash/denominations.ts`** (miroir byte-exact de l'allowlist RPC) ; à l'open (client-only, `opening_denominations` — pas de RPC d'open, D-8) et au close **enforced serveur** (`denominations_required`/`invalid_denomination`/`denomination_total_mismatch`, Σ = `p_counted_cash`, persistée `closing_denominations`) ; replay idempotent exempté de TOUTES les gardes. **POS** : `CloseShiftModal` 3 saisies à l'étape count (**blind count LOT 4 intact**, volets masqués selon `useEnabledPaymentMethods`, review non-cash counted-only DEV-S67-02), `DenominationGrid` (steppers 44 px, total auto `sumDenominations`) remplace numpad/quick-amounts quand flag ON (close **et** `OpenShiftModal`), `useCloseShift` v5 + 4 mappings d'erreurs, hook dédié **`useDenominationCountEnabled`** (fail-closed `false`, DEV-S67-01). **BO** : toggle `SettingsGeneralPage` + rendu reconciliation dans `SignZReportModal` ; EF `generate-zreport-pdf` redéployée. ⚠️ **DEV-S67-03** : bug `array || 'literal'` (22P02) attrapé par la nouvelle suite pgTAP → `array_append` ×6, redéployé live, `_122` amendé pré-merge. pgTAP live : **`close_shift_three_way` 15/15** (nouvelle) · repoints v5 `close_shift_pin_gate` 11/11 · `close_shift_note_enforced` 7/7 · `cash_register` 12/12 · ancre **`s44_money_gates` 12/12**. Types regénérés (+5 colonnes, v4→v5). **Money-path v17/v11/fire_v4 non modifiée.** Déviations DEV-S67-01..06 + dettes D-1..D-8 dans l'INDEX `docs/workplan/plans/2026-07-07-session-67-INDEX.md`.
- **Previously merged (S66):** **S66 — Clôture de caisse ① : PIN manager sur gros écart + dette D-9 (branche `swarm/session-66`).** **Fiche 12 D2.1 livrée** (B1.4 « validation manager par PIN » devient réelle) : seuils dédiés **`business_config.shift_variance_pin_threshold_abs/pct`** (défauts **200 000 IDR / 2 %**, CHECK ≥ 0, éditables BO Settings via les settings RPCs étendus in-place depuis le live) + permission **`shift.variance.approve`** (seedée MANAGER/ADMIN/SUPER_ADMIN) + colonne trace **`pos_sessions.variance_approved_by`** (migration **`20260710000118`**) ; **`close_shift_v3 → v4`** (**`_119`**, corps live DEV-S57-02, DROP v3, trio S20) — au-delà du seuil PIN, la clôture exige un **approbateur DÉSIGNÉ** (`p_approver_id`, décision brainstorm : pas de matching PIN anonyme, le lockout par compte reste possible) + PIN 6 chiffres via `_verify_pin_with_lockout` ; erreurs `pin_approval_required`/`approver_not_authorized`/`invalid_pin` (format checké AVANT le helper, miroir manager-pin.ts)/`account_locked` ; replay idempotent exempté ; garde note S60 inchangée ; auto-approbation autorisée (B2.3 double-signature = futur). **POS** : section « Manager approval » dans `CloseShiftModal` (étape review, blind count intact — select natif des rôles manager via `list_login_users_v1` + PIN masqué), `useCloseShift` v4 + 4 mappings d'erreurs, `useShiftCloseSummary`/`Pos.tsx` propagent les seuils. **BO** : `SettingsGeneralPage` +2 champs `pos` (`_120` expose/valide les 2 clés). **Dette D-9 S65 soldée** (`_117`) : `lan.devices.read` seedée **CASHIER/waiter** — `useStationPrinters` recevait 0 ligne en silence sous la policy S65 (routage KOT muet) ; suite `lan_devices_rls` 7/7 avec T3 inversé (visibilité CASHIER). pgTAP live : **`close_shift_pin_gate` 11/11** (nouvelle) · `close_shift_note_enforced` 7/7 (repointée v4, variance recalibrée 1 % + fixture s2 dynamique) · `cash_register` 12/12 (9→12) · ancre **`s44_money_gates` 12/12** ; Vitest live-RPC `cash-register-close` repointé v4 (fond 2M, variances sous le seuil PIN) ; smoke `CloseShiftModal` 11/11. Types regénérés (+3 colonnes, v3→v4). **Money-path v17/v11/fire_v4 non modifiée.** ⚠️ **Finding F-1** (pré-existant S38, découvert par T5b) : les RPCs PIN-in-arg qui `RAISE` après `_verify_pin_with_lockout` perdent l'incrément `failed_login_attempts` dans le rollback — le helper ne vaut que par son check `locked_until` (posé par le chemin EF) ; dette D-1. Déviations DEV-S66-01..02 + dettes D-1..D-5 dans l'INDEX `docs/workplan/plans/2026-07-07-session-66-INDEX.md`.
- **Earlier merged (S65):** **S65 — Print-bridge versionné + scan réseau imprimantes + CRUD LAN Devices (branche `swarm/session-65`).** Nouveau workspace **`apps/print-bridge`** (Express 4 + node-thermal-printer 4.6, DI complète, 62 tests) : contrat V2 POS conservé octet pour octet (`/health`, `/print/receipt`, `/print/ticket`, `/drawer/open`) + **`/scan/printers`** (sweep TCP 9100 /24 concurrent) + **`/status/probe`**, anti-SSRF plages privées sur scan/probe **et** sur les cibles `printer` des routes print (DEV-S65-07, `invalid_printer_target`) ; templates reçu (**rend `promotions[]` + `promotion_total` — ferme l'action S60**, le template externe est remplacé) et KOT station/waiter (`*** ADDITIONAL ORDER ***`) ; `server.ts` + README install Windows (NSSM/pm2) + build tsup `dist/server.js`. **Types partagés** : payloads d'impression déplacés dans `packages/domain/src/printing/payloads.ts`, re-export type-only par `printService.ts` (**POS runtime inchangé**). **BO LAN Devices = CRUD complet** : `bridgeSettingsStore` (URL bridge persist) + client `bridgeApi` (`bridge_unreachable` dédié), hooks `useUpsertLanDevice`/`useDeleteLanDevice` (**writes directs sous la RLS S13 `lan.devices.manage`**, design D8 — zéro RPC/migration ; merge `capabilities`, 23505→`code_taken`, soft-delete), `LanDeviceFormModal` (station conditionnelle, warning doublon non bloquant), `LanDevicesTable` réécrite (IP/station, Test/Edit/Delete gated manage — fixe au passage les tokens `state-*` invalides S13/S14, DEV-S65-05), `ScanPanel` (AbortController, hits croisés par IP), page assemblée. pgTAP **`lan_devices_rls` 7/7 live** — ⚠️ la policy SELECT live est gatée `lan.devices.read`, pas `USING true` (DEV-S65-02 ; dette D-9 : auditer le chemin station-printers POS). **Zéro migration DB · money-path intouchée · POS runtime inchangé.** Suite monorepo verte (typecheck 7/7, build 3/3, test exit 0 — smoke KPI S14 réparé DEV-S65-08). Revue finale : Ready to merge après 2 fixes Important (appliqués). Déviations DEV-S65-01..08 + dettes D-1..D-9 dans l'INDEX `docs/workplan/plans/2026-07-06-session-65-INDEX.md`.
- **Earlier merged (S64):** **S64 — Moyens de paiement configurables + fix I-1 voids même-jour (branche `swarm/session-64`).** **Fiche 19 D2.1 livrée** (ferme B1.1c/B1.7) : colonne **`business_config.enabled_payment_methods`** JSONB NOT NULL DEFAULT (les 6 : cash/card/qris/edc/transfer/store_credit) + CHECK array non vide (migration **`20260710000115`**) ; branche `WHEN 'payments'` dans `get_settings_by_category_v1` et validation whitelist (array/non-vide/6 connues/anti-doublon) dans `set_setting_v1` (audit old/new hérité), corps repris du live (DEV-S57-02), trio S20. **BO** : page `SettingsPaymentMethodsPage` (draft/dirty/save, gates `settings.read`/`update`, ordre canonique au save) + route + tuile hub activée. **POS** : hook **`useEnabledPaymentMethods`** (miroir `useTaxRate` — SELECT direct `business_config`, **fail-open = les 6**, staleTime 30 s + refetchInterval 60 s + refetch on focus → effet ≤ 60 s) filtrant `PaymentMethodGrid` **et** `PerPayerMethodStep`, + garde de désélection dans `usePaymentFlowLogic`. ⚠️ **Enforcement UI v1** : l'EF `process-payment` accepte toujours les 6 (dette D-1, enforcement serveur = session future). **Fix I-1** (décision propriétaire 2026-07-06, migration **`20260710000116`** `[types-noop]`) : `AND NOT r.is_full_void` dans les **3** soustractions de refunds de `get_dashboard_overview_v1` (revenue_today + CTE 30 j) et `get_daily_sales_v1` (CTE day_refunds) — un void même-jour ne double-pénalise plus le net ; les refunds partiels restent soustraits. pgTAP live : `payment_methods_config` 11/11 · **`net_revenue_full_void` 11/11** (pin du fix) · non-régression `dashboard_overview` 14/14. **Money-path non modifiée** (v17/v11/fire_v4/EF intouchés). Déviations DEV-S64-01..05 + dettes D-1..D-7 dans l'INDEX `docs/workplan/plans/2026-07-06-session-64-INDEX.md`.
- **Earlier merged (S63):** **S63 — Dashboard BO réel (branche `swarm/session-63`).** Le stub d'accueil BO (zéros codés en dur depuis S14) est câblé sur du réel : nouveau RPC **`get_dashboard_overview_v1`** (migrations **`20260710000113..114`** — `_114` = fix revue in-place `paid_at IS NOT NULL` sur payment_methods, `[types-noop]`), **lecture pure** SECURITY DEFINER STABLE, gate `reports.read`, trio S20, bucketing `business_config.timezone`, définition « commande valide » miroir `get_daily_sales_v1` dans les 6 sections : 5 KPIs jour (`revenue_today` **net des refunds**, `avg_basket` = brut÷commandes miroir `aov`, B2B compté **au paiement** — `b2b_pending` invisible), série 30 j **continue** (generate_series, jours vides à 0), revenu par type, top 5 produits (hors lignes annulées, `name_snapshot`), ventes horaires locales, paiements (`order_payments`, bucketing `op.paid_at`). **BO** : hook `useDashboardOverview` (polling 60 s, classify 42501→`permission_denied`) + `Dashboard.tsx` câblé (état « accès restreint » propre, Last updated = `generated_at` serveur, prop `data?` de test conservée) + 5 panneaux (`features/dashboard/components/` : LineChart 30 j, donut par type, BarChart 0-23 complété client, listes top produits/paiements — palette `chartColors.ts`, zéro hex neuf) ; polyfill `ResizeObserver` centralisé dans `vitest.setup.ts` (ResponsiveContainer jsdom). pgTAP **`dashboard_overview` 14/14 live** (delta-based DB non vide : exclusions voided/`b2b_pending`/ligne annulée, bord timezone 01:00 locale, 30 points, 42501, ACL anon) ; smoke Dashboard 8/8 ; types +1 ligne additive. **Money-path non modifiée** (v17/v11/fire_v4 intouchés — RPC de lecture pure). Dettes D-1..D-12 + finding I-1 (net/voids même-jour, hérité du miroir daily_sales — décision propriétaire due) + déviations DEV-S63-01..04 dans l'INDEX `docs/workplan/plans/2026-07-06-session-63-INDEX.md`.
- **Earlier merged (S62):** **S62 — Purges actées + plafond ardoise serveur (branche `swarm/session-62`).** **Purge mesh LAN mort** (décision 2 : `lanHub`/`lanClient`/`lanHubMessageHandler`/`useLanHub`/`useLanClient` + `packages/domain/src/lan/` entier, −1510 lignes, 0 dep npm ; branche morte `send`/broadcast retirée de `useLanHeartbeat` — les heartbeats S59 restent, RPC seul). **`print_queue` STATUÉE → DROPPÉE** (`_110` : garde table-vide + DROP des 5 RPCs `*_print_job_v1` + TABLE + permissions `print_queue.*` cascade ; page BO print-queue purgée ; le vrai print POST directement au bridge externe via `printService.ts`, non touché). **Purges légères** : `TIERS[].discount` retiré (décision 3 — `points_multiplier` et redemption intacts), `vite-plugin-pwa` + arbre workbox évacués (décision 5), permission `rbac.update` supprimée (`_111`, décision 1, `rbac.read` intact). **Plafond ardoise serveur** (décision 4) : colonne `customers.retail_credit_limit` (NULL = illimité, CHECK ≥ 0) + nouveau RPC **`attach_tab_customer_v1`** — « ardoise nommée » : attache un client à une commande comptoir `pending_payment` + pose le total provisoire (miroirs verbatim v11 : SUM(line_total), subtotal TTC, `round_idr`, `business_config id=1`), gate d'encours **live** (miroir debts v3, `created_via='pos'`, sans lookback, self-exclusion) sous locks anti-TOCTOU order→customer, **P0011 `credit_limit_exceeded`** DETAIL jsonb (miroir B2B S52), gate `payments.process`, trio S20 ; l'ardoise devient visible `/pos/debts` et payable via le flux S60 — **money-path v17/v11/fire_v4/_record_sale_stock_v1 non modifiés** (gate à l'attache ; v11 recalcule au paiement — design assumé D-7). **POS** : bouton « Ardoise » (44 px) sur les lignes `pending_payment` de `HeldOrdersModal` → `CustomerAttachModal` → `useAttachTabCustomer` (invalide held-orders + pos-outstanding-debts), message FR plafond parsé du DETAIL, case `credit_limit_exceeded` au `retryClassifier`. **BO** : `RetailCreditLimitSection` sur la fiche client retail (chaîne réelle CustomerDetailPage→InfoTab, vide ↔ NULL jamais 0 ; `B2BFieldsSection` découvert code mort). Migrations **`20260710000110..112`**. Types regénérés (−191/+7). pgTAP **`retail_tab_credit_gate` 8/8 live** ; ancres `s44_money_gates` 12/12 + `pay_existing_flag_aware` 3/3 re-passées live en closeout. ⚠️ Incident résolu : le commit du plan avait écrasé les éditions #150 de CLAUDE.md/00-INDEX (index sale post-merge concurrent) — restaurées au closeout. INDEX `docs/workplan/plans/2026-07-06-session-62-INDEX.md`.
- **Earlier merged (S61):** **S61 — Findings S58 F-2/F-5 + décommissionnement péremption (branche `swarm/session-61`).** **F-2 P2 fixé** (`_107` in-place depuis le corps live : les 2 gardes d'insuffisance de **`_record_sale_stock_v1`** lèvent `ERRCODE P0002` — l'EF `process-payment` classe enfin `insufficient_stock` 409 au lieu de `no_open_session` (P0001) — et la **garde vitrine devient inconditionnelle** : `allow_negative_stock` ne s'applique jamais au comptoir display, dont la CHECK `display_stock_quantity_check` interdisait déjà le négatif (l'échec sortait en 23514 brut → `check_violation` 422) ; signature inchangée, ACLs S53 survivantes vérifiées ; nouvelle suite pgTAP **`display_oversell_contract` 4/4** + 6 ancres re-passées vertes). **F-5 P2 fixé** (`_108` in-place : allowlist stations d'**`import_catalog_v1`** alignée sur la CHECK live `categories_dispatch_station_check` — `bakery`→`display`, 2 occurrences seules patchées dans le corps ~470 lignes ; catégorie `display` importable, `bakery` rejetée en validation `invalid_dispatch_station` au lieu de crasher en 23514 ; `catalog_import` 31→33 asserts verts). **Décommissionnement léger péremption** (06 D3.1) : cron `mark_expired_lots_hourly` **désactivé** (`_109`, `cron.alter_job active:=false`, réversible, **aucun DROP**) ; purge frontend BO (pages ExpiringStock/PerishableTurnover + routes + sidebar + tuile ReportsIndex + `ExpiringLotsBadge`/`useExpiringLots`/`usePerishableTurnover` + terme expiring d'`AlertsBadge` + panneaux dashboards produit + type PDF `perishable_turnover` + tests) ; `stock_lots`/RPCs/template PDF EF restent **dormants**. Migrations **`20260710000107..109`**. Types **no-drift** (aucun changement de signature, rien à regénérer). Ancre `s44_money_gates` 12/12 live en closeout — **v17/v11/fire_v4 non modifiés**. Revue finale de branche : Ready to merge (0 Critical/Important ; 6 dettes D-1..D-6 documentées dans l'INDEX, dont `classify()` B2B sans match « Insufficient display stock », pré-existant S53). Suite monorepo verte (typecheck+build+test exit 0). INDEX `docs/workplan/plans/2026-07-05-session-61-INDEX.md`.
- **Earlier merged (S60):** **S60 — Remise à plat Vague 1 lot 2 : les 6 quick wins money-path (branche `swarm/session-60`).** **Ardoise payable en direct** depuis `/pos/debts` (`useLoadDebtOrder` mirror pickup — SELECT `order_items` `name_snapshot`, lock+print, `pickedUpOrderId` → `pay_existing_order_v11` ; **B2B exclu** avec hint « settle in Backoffice », leur `b2b_pending` ne passe pas la garde v11). **`CashInOutModal` monté** (il était **orphelin**, jamais importé) via SideMenuDrawer (Cash In/Out, session requise, `sessionId` prop-drillé depuis Pos.tsx) + select natif `reason_code` (apport_owner/bank_transfer → JE 1110/3100/1112, replenishment/misc sans JE) + clé d'idempotence. **`close_shift_v2 → v3`** (`_105`, DROP v2 même migration, corps live DEV-S57-02) : note d'écart > seuil **enforced serveur** (`variance_note_required` P0001, prédicat miroir de `shouldShowWarning`, seuils `business_config`, replay idempotent exempté) ; suites `close_shift_note_enforced` 7/7 + `cash_register.test.sql` réparée (STALE : assertait les v1 droppées) 9/9 ; clients repointés v3. **Lignes promo nommées** sur le payload ticket (`ReceiptPayload.promotions[]` + `totals.promotion_total`, snapshot de `appliedPromotions` au succès — ⚠️ **template print-bridge externe à MAJ**, action utilisateur) et dans le détail BO (embed `promotion_applications(amount, description, promotions(name))` dans `useOrderDetail` partagé → page + drawer, libellé = `description` snapshot, zéro migration). **KDS « All ready »** : nouveau **`kds_bump_order_v1`** (`_106`, UN update atomique pending|preparing→ready non-cancelled, `bumped_at` posé → undo per-item 60 s préservé, replay via `audit_logs` `kds.bump_order`, gate `kds.operate`, trio S20 ; boucle client impossible : `kds_bump_item_v1` exige `preparing` P0011) + bouton header `KdsOrderCard` + `useKdsBumpOrder` ; pgTAP `kds_bump_order` 11/11. **Void BO** : `x-idempotency-key` enfin envoyé (`VoidArgs.idempotencyKey` + header, miroir POS S55 byte-identique, EF inchangée — elle lisait déjà le header) + commentaire stale « PIN in body » corrigé. Migrations **`20260710000105..106`**. Types regénérés. **Money-path v17/v11/fire_v4 non modifiés** (ancre `s44_money_gates` 12/12 `num_failed=0` re-passée live en closeout). Revue finale de branche : Ready to merge (0 Critical/Important ; 8 dettes D-1..D-8 documentées dans l'INDEX). Suite monorepo verte (typecheck+build+test exit 0). INDEX `docs/workplan/plans/2026-07-05-session-60-INDEX.md`.
- **Earlier merged (S59):** **S59 — Remise à plat Vague 1 lot 1 : findings S58 + quick wins UI pures (branche `swarm/session-59`).** **F-1 P0 fixé** (`_101` in-place depuis corps live : gardes dernier-admin de `delete_user_v1` **et** `update_user_role_v1` filtrent `is_active = true` — SYS-CRON inactif ne protège plus ; suite `users` 29/29 dont nouvelle T_USR_11 downgrade) ; **F-4 P1 fixé** (`_102` : `_emit_expense_je` folde la TVA dans le débit de la charge TTC, plus aucune ligne 1151/`EXPENSE_VAT_INPUT` — ADR-003 NON-PKP, miroir du pattern achats ; suite `expenses` 19/19, T_EXP_08 réécrit légitimement — le fixture appelait `resolve_mapping_account('EXPENSE_VAT_INPUT')` en direct — + T_EXP_11 appel réel vat>0). **Quick wins UI** : `visible_on_pos` enfin respecté au POS (`useProducts` + `useProductVariants`, la tablette hérite) ; ticker écran client branché sur les items réellement `ready` (`useReadyOrders`, plafond 5, tri urgence) ; **KDS câblé** — undo-bump 60 s/recall/prep-timer via RPCs `kds_*_v1` (l'UPDATE brut `useBumpItem` supprimé — violation pré-existante purgée), alarme WebAudio (dédup order_id, mute persisté, resume autoplay), bandeau « Recently served » 15 min ; **note par commande tablette** — **`create_tablet_order_v2 → v3`** (`_103` +`p_notes`→`orders.notes`, DROP v2, idempotence préservée ; `_104` REVOKE miroir), affichée KDS + pickup, suites Vitest live-RPC `idempotency-hardening` et e2e s43 repointées v3 ; **BO** — drill-down JE→origine (`resolveJeSourceEntity`, 26 `reference_type` couverts ; ⚠️ `sale_refund`/`refund` volontairement en fallback texte : leur `reference_id` = `refunds.id`, pas un order id — lookup futur), bouton « Dupliquer » dépense (seed sans date/receipt), filtres + metadata (`payload` non SELECT par `get_audit_logs_v1/v2` → bump additif futur) du journal d'audit avec debounce 300 ms en updater fonctionnel ; **nettoyage** — `ProductionSuggestions`+hook orphelins purgés (RPC gardé : 2ᵉ consommateur `ProductionAlertsTab`), purge hex → tokens (6 littéraux sans équivalent documentés) ; **`auth-change-pin` PINs en headers `x-current-pin`/`x-new-pin`** (hard cutover S25, body `user_id` seul, CORS étendu, EF **v8** ; vrai call-site `useChangePin.ts` + wrapper mort `pinAuth.ts` migrés) ; **heartbeats LAN montés** (POS/KDS/tablette, `posSettingsStore.deviceCode` + champ Settings→Devices ; page BO « LAN Devices » vivante ; ⚠️ seed dev `KDS-CUISINE-1`/`TABLETTE-1` = codes d'exemple à adapter). Migrations **`20260710000101..104`**. Types regénérés (v3). Revue finale de branche : Ready to merge (I-1 fixé `3b3a254`). Suite monorepo verte (typecheck 6/6, build 2/2, test exit 0). INDEX `docs/workplan/plans/2026-07-04-session-59-INDEX.md`.
- **Earlier merged (S58):** **S58 — Remise à plat Vague 0 : réparer ce qui trompe l'exploitant (branche `swarm/session-58`).** 4 chantiers P0 : **(0.1) chaîne d'embauche réparée** — RPC **`list_login_users_v1`** anon-callable (`_099`, trio S20 REVOKE PUBLIC+GRANT+COMMENT, exposition minimale id/nom/rôle, carve-out **nommé** dans `security_anon_grants` + suite `list_login_users` 7/7), les 3 UserPicker login POS/BO dynamiques, **PIN exactement 6 chiffres partout** (`_100` in-place depuis les corps live, UserFormDialog 4-8→6, copy POS, OpenShiftModal) ; **(0.2) nightly pgTAP trié** — 28/33 suites re-vertes (16 staleness S50→S57 réparées), 3 quarantaines datées **`supabase/tests/_quarantine/`**, 2 rouges assumées tests INTACTS (tripwires : `users` **F-1 P0** garde dernier-admin `delete_user_v1` sans `is_active` → SYS-CRON inactif permet de supprimer le dernier admin réel ; `expenses` **F-4 P1** `_emit_expense_je` référence `EXPENSE_VAT_INPUT`/compte 1151 désactivé ADR-003 → crash sur toute dépense `vat_amount>0`) ; **job live-RPC réparé** (`fetch failed` = fallback `localhost:54321`, `VITE_SUPABASE_URL` exportée ; secret optionnel `SUPABASE_ANON_KEY` documenté) ; drift types nul + comparaison normalisée `--schema public` ; preuve nightly dispatché run 28680835452 : 33→4 échecs, zéro fetch failed ; **(0.3)** `staging-deploy.yml`+`playwright-e2e.yml` sur `workflow_dispatch` seul ; **(0.4)** labels honnêtes (sidebar « Permissions (read-only) », 5 tuiles Settings hub reliées). Findings annexes : F-2 (oversell vitrine 23514 vs P0002 v10), F-3 (attendu purchasing_po corrigé 122100 = ×1.11 NON-PKP), F-5 (allowlist `import_catalog_v1` désync : `display` inimportable). Migrations **`20260710000099..100`**. Types regénérés (T3 ; T4 = no-drift). INDEX `docs/workplan/plans/2026-07-04-session-58-INDEX.md`.
- **Earlier merged (S57):** **S57 — P2 restant : gouvernance promos/combos, UX POS/BO, outillage, marge brute (branche `swarm/session-57`).** Ferme **P2.1/P2.3/P2.4/P2.5/P2.6**. **P2.1 combos/promos serveur** : helper interne **`_resolve_combo_price_v1`** (`_090`, REVOKE ×3) valide la composition (`combo_groups`/`combo_group_options` → `combo_invalid_component`/`combo_group_violation`) ET facture `base + Σ surcharge` (revenue leak fermé) ; **`complete_order_with_payment_v16 → v17`** (`_092`, DROP v16, ⚠️ GRANT authenticated) ; **plafonds promo** `promotions.max_uses`/`max_uses_per_customer` (`_089`, NULL = illimité ; comptage `promotion_applications`×orders non-voided, un void libère) — advisory dans **`evaluate_promotions_v1 → v2`** (`_091` ; `_095` fix corps v1 **historique** lisant `customers.tier_id` inexistant), **gate dur atomique** (`pg_advisory_xact_lock`+re-count → `promo_cap_exceeded`) dans v17 **et** `pay_existing_order_v11` (`_096`, fix pattern-guardian TOCTOU multi-caisses) ; EF `process-payment` redéployée (v17 + 3 codes 409) ; caps dans `PromotionForm` (split 804→175+6). **P2.6** : **`get_gross_margin_by_product_v1`** (`_093`, coût = WAC courant avec caveat — snapshot COGS→P3, gate `reports.financial.read`, POS+B2B, tz locale) + page BO Gross Margin ; **`get_payments_by_method_v1 → v2`** (`_094`, bucketing UTC→`business_config.timezone`). **P2.3 POS** : `isError`+Retry sur ProductGrid/TabletGrid/KDS, `useReconnectInvalidate` KDS, cibles 44 px, broadcast **`payment_complete`** (merci/monnaie 8 s, masquée ≠ cash, rétro-compat). **P2.4 BO** : prop `emptyState` sur `ReportPage` (~18 pages), hook **`useUrlState`**, parité sidebar↔hub (+4 reports, +Marketing, +Security), gate `accounting.read` route index. **P2.5** : `db:*` stubs cloud, **lint-ratchet CI bloquant** (diff PR `**/src/**`, inauguré vert — 80 erreurs soldées, 0 disable), `max-lines` 500 warn, split `CustomerDetailPage` 643→316+5. Migrations **`20260710000089..096`**. pgTAP **104 assertions vertes live** (4 nouvelles suites + 9 ancres dont `combo_sale` 12, `canonical_line_price` 13, `s44_money_gates` 12 ; ~52 ancres secondaires → nightly). Suite monorepo verte, build 2/2, types regénérés. INDEX `docs/workplan/plans/2026-07-03-session-57-INDEX.md` ; spec `docs/superpowers/specs/2026-07-03-s57-p2-governance-ux-design.md`. **Leçon durable (DEV-S57-02)** : tout bump/copie de RPC part du corps **live** (`pg_get_functiondef`), jamais du fichier de migration d'origine (drift cloud↔git).
- **Earlier merged (S56):** **S56 — UI déférées + consolidation audit (branche `swarm/session-56`).** Ferme **DEV-S54-01**, **DEV-S52-03** et **P2.2 reliquat**. **Accounting** : `accounting.year.close` dans `PermissionCode`, hook `useCloseFiscalYear` (classify 8 codes), **`AnnualCloseModal`** + bouton gaté dans `SettingsAccountingPage` ; libellé `period_undefined` (JE manuelle) + **fix bug classify** des 3 hooks B2B (`no fiscal period` avec espace tombait en `unknown`). **B2B** : `useB2bInvoices` (1ᵉʳ lecteur de `view_b2b_invoices`), multi-sélection de factures dans `RecordB2bPaymentModal` (**ordre de coche = ordre d'allocation** `p_invoice_ids`, récap `allocations[]`, aucune coche = FIFO serveur), onglet **« Invoices »** (`B2bInvoicesTab`) avec **Cancel par facture** (`CancelB2bOrderModal`, gate `b2b.order.cancel`, ssi `b2b_pending` non alloué) + Record pré-rempli depuis Outstanding/Invoices. **Audit (P2.2)** : `_087` réécrit in-place les **26 dernières fonctions** écrivant via la vue compat (25 par DO-block regex auto-vérifiant + `duplicate_recipe_v1` explicite, seul lecteur) ; `_088` **DROP la vue `audit_log` + trigger compat** → **`audit_logs` est l'unique surface d'audit** (vocabulaire `entity_type`/`entity_id`/`metadata` ; dualité `metadata`/`payload` documentée, ne pas fusionner) ; 7 suites de tests migrées ; nouvelle suite pgTAP **`audit_consolidation` 6/6**. Ancres re-passées : cfy 19/19, security 20/20, leak_guard 13/13, s26 15/15 (3 staleness pré-existantes réparées), update_account 4/4. Migrations **`20260710000087..088`**. Types regénérés (vue disparue). INDEX `docs/workplan/plans/2026-07-03-session-56-INDEX.md` ; spec `docs/superpowers/specs/2026-07-03-s56-deferred-ui-audit-consolidation-design.md`.
- **Earlier merged (S55):** **Vague P1.5 « Durcissement EF restant » (branche `swarm/session-55`, PR #138, ferme audit T7).** **Idempotency reversals** (flavor 1 S25) : **`void_order_rpc_v4`** (`_082`, replay via `refunds.idempotency_key`) et **`cancel_order_item_rpc_v3`** (`_083`, nouvelle colonne `order_items.cancel_idempotency_key` + index unique partiel) — replay renvoie l'enveloppe du premier appel + `idempotent_replay:true` ; EFs `void-order`/`cancel-item` lisent `x-idempotency-key` ; POS : clé per-modal (`useRef(crypto.randomUUID())`, rotée à la fermeture). **REVOKE v4/v3 from `authenticated`** (`_084`, EF-only). **Discount-PIN hors SQL** : table nonce **`discount_authorizations`** (`_085`, service-role only, TTL 60 s) ; **`complete_order_with_payment_v16`** (`_086`, DROP v15) remplace `p_manager_pin` par `p_discount_auth_id` consommé atomiquement (P0003 sinon) ; l'EF `process-payment` vérifie le PIN in-EF (`_shared/manager-pin.ts` + `checkPermissionForRole('sales.discount')`), mint le nonce et **dérive `p_discount_authorized_by` du PIN vérifié**. ⚠️ **v16 DOIT `GRANT EXECUTE TO authenticated`** (caveat S51 reporté). Migrations **`20260710000082..086`**. pgTAP : `reversal_idempotency`, `discount_auth_nonce` 6/6 + 11 suites repointées vertes. T0 attesté : `auth-change-pin` rate-limit durable + `notification-dispatch` secret header déjà couverts. INDEX `docs/workplan/plans/2026-07-02-session-55-INDEX.md` ; spec `docs/superpowers/specs/2026-07-02-ef-hardening-p15-design.md`.
- **Earlier merged (S54):** **Vague P1.3 « Correctness compta » (branche `swarm/session-54`, PR #137).** Ferme audit **T6/C10**. **`check_fiscal_period_open` fail-closed** (`_077`, in-place) : aucune période couvrante → `period_undefined` P0004 (les 34 call-sites traitent déjà P0004). **Fix leak cumul `get_trial_balance_v3`** (`_078`, in-place) : le double LEFT JOIN de `_061` laissait les JE draft/futures/void-dédupliquées polluer le cumul des comptes permanents — join interne parenthésé. **`close_fiscal_year_v1(p_fiscal_year, p_manager_pin)`** (`_079` permission `accounting.year.close` + `_080` contrainte+RPC) : préconditions 12 périodes closed/locked (`FOR UPDATE`) + pas de `year_close` existante ; JE `year_close` 31/12 zérotant classes 4/5/6 (dédup canonique) → contrepartie **3200** (CR profit / DR perte) ; **seed 12 périodes N+1** (neutralise la bombe à retardement du fail-closed) ; `_verify_pin_with_lockout` ; `reference_type='year_close'` ajouté à la CHECK. **Exclusion `year_close` des rapports** (`_081`, in-place ×2) : P&L v2 (WHERE) ; TB v3 colonnes de période seulement (ouverture/cumul l'incluent) ; **BS v2 inchangé** (CYE YTD retombe à 0 au 31/12). Migrations **`20260710000077..081`**. pgTAP : `fiscal_guard_fail_closed` 4/4, `close_fiscal_year_v1` 19/19, `trial_balance_v3_cumulative` +T7 ; ancres PB1 3/3 + gates + sanity P&L ; typecheck 6/6, build 2/2. Types regénérés (+`close_fiscal_year_v1`). INDEX `docs/workplan/plans/2026-07-02-session-54-INDEX.md` ; spec `docs/superpowers/specs/2026-07-02-fiscal-correctness-design.md`.
- **Earlier merged (S53):** **Vague P1.4 « Unification de la déduction stock à la vente » (PR #136 mergée).** Ferme audit **T2**. Nouvelle procédure interne unique **`_record_sale_stock_v1`** (SECURITY DEFINER, REVOKE anon+authenticated+PUBLIC, magnitude positive) qui possède toute la mécanique de déduction d'une vente : garde de suffisance flag-aware, `INSERT stock_movements (-qty)`, `UPDATE products.current_stock`, isolation `display_stock`/`display_movements` (`display_movements.movement_type` est l'enum distinct `display_movement_type` → cast text ; `reference_type` **display='order' / stock='orders'** préservé). Les **9 `INSERT INTO stock_movements` bruts** des 3 RPCs de vente sont supprimés. **`complete_order_with_payment_v15`** : refactor **in-place** (comportement identique, reste v15 — déviation P10 assumée, précédent #122). **`create_b2b_order_v2 → v3`** : devient **display-aware** (décrémente `display_stock` pour un article vitrine — v2 ne le faisait pas ; DROP v2). **`pay_existing_order_v10 → v11`** : devient **flag-aware** (respecte `business_config.allow_negative_stock` ; v10 rejetait inconditionnellement ; DROP v10). Migrations **`20260710000073..076`**. Suites pgTAP `sale_stock_unification` 12/12, `b2b_display_aware_stock` 3/3, `pay_existing_flag_aware` 3/3 ; ancres v15/v3/v11 vertes (`sale_flag_aware`, `combo_sale`, `s44_display_symmetry`, `combo_fire_pay`, `modifier_ingredient_deduction`) ; typecheck 4/4, build 2/2, smokes 18/18. Types regénérés, hooks BO/POS repointés v3/v11. INDEX `docs/workplan/plans/2026-07-02-session-53-INDEX.md` ; spec `docs/superpowers/specs/2026-07-02-sale-stock-unification-design.md`.
- **Earlier merged (S52):** **Vague P1.2 « B2B per-invoice settlement » (mergé `master` via PR #134 le 2026-06-29 ; PR #135 = doublon no-op mergé le 2026-07-01, branche `swarm/session-52` supprimée).** Ferme audit **T5/C3/C4**. Nouvelle table append-only **`b2b_payment_allocations`** (paiement→facture, source unique : `outstanding = total − Σ amount_applied`). **`record_b2b_payment_v1 → v2`** : allocation réelle ciblée (`p_invoice_ids`, ordre du tableau) + FIFO en repli, pose **`orders.paid_at`+`status='paid'`** au solde complet (C3 fermé), gate dédié **`b2b.payment.record`**. **`get_pos_b2b_debts_v2 → v3`** : le `paid` B2B dérive des allocations (l'ardoise retail reste sur `order_payments`) → POS == BO (C4 fermé). **`view_b2b_invoices`/`view_ar_aging`** reconstruites sur `outstanding` (exclut `voided`). Nouveau **`cancel_b2b_order_v1`** (contrepasse JE + stock `sale_void` + balance, `status='voided'`, bloqué si allocation ; gate `b2b.order.cancel`). **`create_b2b_order_v1 → v2`** : re-check plafond **après** `FOR UPDATE` (TOCTOU). **`reconcile_b2b_balance_v1`** (read-only, gate `b2b.read`) alerte le drift cache↔ledger. Migrations **`20260710000065..072`**. pgTAP `b2b_settlement` **14/14**. UI : hooks BO repointés v2 + `useCancelB2bOrder`, POS debts v3 ; liste-factures UI déférée (DEV-S52-03). INDEX `docs/workplan/plans/2026-06-29-session-52-INDEX.md` ; spec `docs/superpowers/specs/2026-06-29-b2b-per-invoice-settlement-design.md`.
- **Earlier merged (S50/S51):** **#132 — S50 Vague 2a-i (intégrité) + S51 Vague 2a money-path (« prix-ligne canonique serveur »).** Money-path bumped **`complete_order_with_payment_v14 → v15`** (signature 16-args identique, **DROP v14** même migration) : le serveur est seule autorité sur le prix de ligne — modifier `price_adjustment` + surcharges combo relus depuis `product_modifiers` (prix client **ignoré**), lignes-cadeau validées vs `evaluate_promotions_v1` `free_items`, retour enrichi `lines[]`/`tax_amount` — via le helper interne **`_resolve_line_price_v1`** (SECURITY DEFINER STABLE, REVOKE PUBLIC+anon+authenticated). EF `process-payment` repointée v15 (redéployée v13) ; le POS consomme `lines[]` serveur + `useTaxRate()` lit `business_config.tax_rate` (fin du `0.10` hardcodé sur l'encaissement). Tranche **2a-i** : PB1 dedup void/refund, `adjust_b2b_balance_v2` (JE+PIN), stock B2B flag-aware, **`get_trial_balance_v3`** (solde cumulé as-of + `opening_balance`), `void_zreport_v2` (PIN). Migrations **`20260710000057..064`**. ⚠️ **v15 DOIT `GRANT EXECUTE TO authenticated`** (l'EF appelle via JWT utilisateur — sans ce grant, toute la money-path casse en `permission denied`). Spec money-path `docs/superpowers/specs/2026-06-28-pos-canonical-server-line-price-design.md` ; INDEX `docs/workplan/plans/2026-06-28-session-51-INDEX.md`.
- **Yet earlier:** **#129** — S50 Vague 1 « cutover sain » (sécurité + drift + CI ; **ne touche pas la money-path**) : gates de 5 RPCs financiers (`get_general_ledger`/`get_trial_balance`/`get_profit_loss`/`get_balance_sheet`/`get_sales_by_hour` → `_v2` + `has_permission`), fermeture fuites `audit_log` (security_invoker) + MV (`mv_pl_monthly`/`mv_sales_daily`/`mv_stock_variance` REVOKE), gates routes/sidebar BO + RPCs customer (`search_customers_v3`/`get_customer_v3`), activation du filet CI (`supabase/tests`, jobs nightly live-RPC + drift, `pgtap-pr` bloquant, gate types-regen). Migrations `20260710000051..056`. Nouveaux perms `b2b.read`, `settings.security.manage`. **#128** — audit intégral par module (12 auditeurs). **#125** — Spec B-1 dispatch/print routing (`display` vocab, mapping catégorie→station complet, ticket waiter, multi-station par produit ; RPC `resolve_dispatch_stations_v1` ; migrations `20260710000031/040..043`). **#124** route-split BO (−69% bundle). **#123** docs refresh. **#122** — `track_inventory`/`deduct_stock` + négatif-stock global (`_resolve_recipe_consumption_v1`, `complete_order_with_payment_v14` remplacé in place, `business_config.allow_negative_stock`, `record_stock_movement_v1` 13-arg ; mig. `20260710000020..025`). ⚠️ **Migration-bookkeeping caveat (toujours actif) :** un `supabase migration repair` d'un subagent a abîmé le bookkeeping cloud `schema_migrations` (~400 lignes clock-stamped supprimées ; max `20260629000012`) — schéma réel intact, workflow MCP `apply_migration` non affecté ; non reconstruit.
- **Latest on `master`:** PRs **#157/#160/#161** (design vagues A/B/C de l'audit 2026-07-07 — a11y/ergonomie, sweep tokens + lock ESLint palette brute, cohérence par surface ; #158/#159 = fermées auto par GitHub à la suppression de leurs branches de base, remplacées par #160/#161 ; la CI de #157 a aussi soldé la dette lint pré-existante S56 de `packages/supabase` + `BumpButton`). Avant : **#156** (S66 clôture PIN), #155 (S65 print-bridge), #154 (skill breakery-design), #153 (S64), #152 (S63), #151 (S62), #138 (S55 EF hardening). Avant : #137 (S54), #136 (S53), #135/#134 (S52), #132. Historique antérieur (held-order #120/#121, Phase 2a #116, Phase 1 #114/#115, #117 Cost Analytics, #118 POS P0 hardening, #106/#103/#95/#98/#99/#100) archivé dans [`docs/workplan/2026-06-26-claude-md-workplan-archive.md`](docs/workplan/2026-06-26-claude-md-workplan-archive.md). Détail par session (S13→S49) dans `docs/workplan/` — ouvrir l'INDEX daté.
- **Migrations:** numbering is monotonic. Check `supabase/migrations/` for the highest NAME-block before picking the next. Cloud `version`s are clock-assigned (S36+ convention); local file names use the NAME-block. Always regen types after a schema change (see Build & Test). **Jamais de `BEGIN;`/`COMMIT;` dans le corps d'une migration** — MCP `apply_migration` wrappe déjà dans une transaction ; un COMMIT interne la termine prématurément et affaiblit l'atomicité (leçon S58).
- **Next-session source:** `docs/workplan/remise-a-plat/00-INDEX.md` (vagues 0→3 + décisions + règle money-path) et les sections D des fiches `NN-*.md`. L'ancien backlog-by-module est archivé (`docs/_archive/backlog-by-module-fige-S14-S30/`, priorités périmées — ne plus trier dedans).
- **Execution skill:** invoke `superpowers:subagent-driven-development` (or `superpowers:executing-plans`) before running a phase — one subagent per isolated phase, parallelizable per Wave.
- **Module reference (canonical):** [`docs/workplan/remise-a-plat/`](docs/workplan/remise-a-plat/) — fiches réel-vs-demandé par module (code vérifié `5b0fa92`, 2026-07-04). `docs/reference/04-modules/` est STALE (S13) et ne fait plus foi ; hiérarchie de vérité complète dans `docs/README.md`. Chapitres reference dangereux (01/03/07/08/09/10) **supprimés** le 2026-07-04 — régénération depuis le code en Phase 3.

## Project Conventions (The Breakery ERP)

### Critical patterns — don't break these
- **DB target is Supabase cloud, NOT local Docker** — As of 2026-05-14, Docker / local supabase stack is **retired** on this machine. All migrations, RPCs, pgTAP tests, and types regen run against the V3 dev project on the cloud: **`ikcyvlovptebroadgtvd`** (`the-breakery-v3-dev`, region `ap-southeast-1`, Pro plan $10/mo) — dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>. Apply migrations via `mcp__claude_ai_Supabase__apply_migration`, run SQL via `execute_sql`, regen types via `generate_typescript_types`. **DO NOT run** `pnpm db:reset`, `supabase start`, `supabase db reset`, or `bash supabase/tests/run_pgtap.sh` — they require Docker and will fail. Prod (ref `abjabuniwkqpfsenxljp`) is V2 monolith and incompatible with V3 migration lineage.
- **PIN auth fetch wrapper** — the `auth-verify-pin` EF issues HS256 JWTs that GoTrue (ES256) can't validate via the default header. The Supabase client uses a custom fetch wrapper that injects the PIN JWT on every request via `setSupabaseAccessToken` (in `packages/supabase`). Never bypass with raw `Authorization` headers or `auth.setSession`.
- **Realtime channel names must be unique per mount** — StrictMode double-mounts components and shared channel names collide silently. See `apps/pos/src/features/kds/hooks/useKdsRealtime.ts`.
- **`packages/domain` is IO-free** — no `fetch`, no Supabase, no React. Pure TS, unit-testable.
- **Order writes go through RPCs** — never raw inserts. The POS POSTs the `process-payment` EF (`apps/pos/src/features/payment/hooks/useCheckout.ts`), which server-side calls the current money-path RPC `complete_order_with_payment_v17` (`supabase/functions/process-payment/index.ts`) — the POS never calls it directly ; le PIN discount est vérifié in-EF et transporté par un nonce `discount_authorizations` (S55, plus de PIN en arg SQL) ; **les combos sont validés ET pricés serveur** via le helper interne `_resolve_combo_price_v1` (S57 — composition vs `combo_groups`, surcharges facturées) et **les plafonds promo** (`promotions.max_uses`/`max_uses_per_customer`) sont hard-gatés sous advisory lock dans v17 et `pay_existing_order_v11`. Other order RPCs: `pay_existing_order_v11` (S53 flag-aware, S57 cap-gated), `fire_counter_order_v4`, `create_tablet_order_v3` (S59, +`p_notes`), `pickup_tablet_order` (unversioned), `evaluate_promotions_v2` (S57, filtre advisory des caps), `mark_item_served` (unversioned), `void_order_rpc_v4` / `cancel_order_item_rpc_v3` (S55, idempotency replay, EF-only). B2B order/AR RPCs: `create_b2b_order_v3` (S53, display-aware), `record_b2b_payment_v2`, `cancel_b2b_order_v1`, `adjust_b2b_balance_v2`, `get_pos_b2b_debts_v3`, `reconcile_b2b_balance_v1` (+ append-only `b2b_payment_allocations`). They handle JE triggers, loyalty, promotions, table state atomically. **Sale-time stock deduction (S53 P1.4)** : les 3 RPCs de vente (`complete_order_with_payment_v16`, `create_b2b_order_v3`, `pay_existing_order_v11`) déduisent via l'unique helper interne **`_record_sale_stock_v1`** (flag-aware + display isolation) — plus aucun `INSERT INTO stock_movements` brut en flux de vente. **RPC versions bump nearly every session — always verify the live version in `supabase/migrations/` + the call-site before relying on a number.**
- **Audit-trail = table `audit_logs` UNIQUEMENT (S56)** — la vue compat `audit_log` (singulier) et son trigger INSTEAD-OF sont **droppés** (`_087`/`_088`). Vocabulaire canonique : `entity_type`/`entity_id`/`metadata`/`actor_id`/`created_at`. Deux colonnes JSONB voulues : `metadata` (contexte free-form — cible de tous les writers RPC) et `payload` (diff before/after, S19) — **ne pas fusionner**. Jamais d'INSERT direct depuis app code ; les RPCs SECURITY DEFINER écrivent. Lecture BO via `get_audit_logs_v1/_v2` (RLS `admin_read`). Suite garde-fou : `supabase/tests/audit_consolidation.test.sql`.
- **`stock_movements` is an append-only ledger** — RLS revokes UPDATE/DELETE for `authenticated`. All writes go through SECURITY DEFINER RPCs (`record_stock_movement_v1` primitive ; `adjust_stock_v1`, `receive_stock_v1`, `record_incoming_stock_v1`, `waste_stock_v1`, future `*_transfer_v1` / `record_production_v1` / `finalize_opname_v1`). Never `INSERT INTO stock_movements` directly from app code or tests.
- **`stock_movements.unit` is NOT NULL** — any direct insert (tests, fixtures, RPCs) must populate `unit`. The `record_stock_movement_v1` primitive auto-resolves from `products.unit` if NULL is passed — don't bypass it. See migration `20260516000019_fix_record_stock_movement_v1_unit.sql`.
- **`stock_movements` section constraint is movement-type-aware** — `transfer_in/out` require both `from_section_id` AND `to_section_id` ; `adjustment*`, `waste`, `incoming`, `purchase`, `sale*`, `production*`, `opname*` require at least one (relaxed in migration `20260516000020`). Don't tighten without re-checking all RPCs.
- **`stock_movements.unit_cost` is per BASE unit** — receive must convert BOTH qty (×factor) AND cost (÷factor) to the base unit before recording; `purchase_order_items.unit_cost` stays per purchase unit (supplier price). All readers do `qty × cost`. Fixed 2026-06-20 (`receive_purchase_order_v2` + backfill, PR #103).
- **Inventory RPCs accept `p_idempotency_key UUID`** — replay returns the existing movement row instead of doubling it. Always pass one from the client on retry-able mutations.
- **RPC versioning is monotonic** — never edit a published `_vN` signature. Create `_vN+1` and `DROP FUNCTION ... vN(<old args>)` in the same migration if replacing. See `20260516000019` (drop original `record_stock_movement_v1` then recreate with `unit`).
- **Supabase auto-grants EXECUTE on public functions to `anon`** — `REVOKE ALL FROM PUBLIC` does NOT cancel it. Always add an explicit `REVOKE EXECUTE ... FROM anon` on admin-only RPCs to enforce gate intent at the role level (defense in depth). See S19 migration `20260523000022` for an example.
- **Anon GRANT defense-in-depth (S20)** — `REVOKE ALL FROM anon ON public.*` is the project-wide default for tables, views, AND functions, future-proofed via `ALTER DEFAULT PRIVILEGES FOR ROLE postgres`. Critical caveat: `REVOKE EXECUTE ... FROM anon` on functions is INSUFFICIENT on its own — `anon` inherits EXECUTE through PUBLIC membership via the `=X/postgres` ACL entry. Future REVOKE-on-functions migrations MUST also `REVOKE EXECUTE ... FROM PUBLIC` and `ALTER DEFAULT PRIVILEGES FOR ROLE postgres ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC`. See S20 migrations `20260524000020..031`. `supabase_admin`-owned extension objects (pgtap: `pg_all_foreign_keys`, `tap_funky`, pgtap helper functions) are platform-managed and not user-revocable — pgTAP suite excludes them. If a future feature legitimately needs anon (public landing-page RPC, embeddable widget), grant explicitly per-object with `COMMENT ON FUNCTION ... IS 'anon-callable: <reason>'`.
- **PIN / auth secrets en header HTTP, jamais en body JSON (S25)** — Any EF that consumes a manager PIN or other validation secret MUST read it from a dedicated HTTP header (e.g., `x-manager-pin`), NEVER from the JSON body. Rationale : request bodies get logged by default in PostgREST access logs, pgaudit, reverse proxies, and Supabase function logs ; headers are far less commonly captured. Hard cutover pattern : drop the body field in the SAME commit as the header read — no dual-mode fallback unless the EF has uncontrolled external callers (rare on this project, only POS calls these EFs). Reference : S25 `refund-order` migration body `manager_pin` → header `x-manager-pin` (`supabase/functions/refund-order/index.ts`). Sweep **DONE** : `void-order` + `cancel-item` hardened S34/PR #53 ; `kiosk-issue-jwt` verified compliant S36 (DEV-S36-A-01) ; all three read `x-manager-pin` from the header.
- **Idempotency 2-flavors selon la sémantique (S25)** — Two distinct patterns coexist on this project ; pick the right one for the flow :
  1. **HTTP `x-idempotency-key` header (EF retry safety)** — For HTTP requests where the client may legitimately retry (flaky network, double-click, React-Query auto-retry). The client generates a UUID v4 (`crypto.randomUUID()` stored in a `useRef` so it survives re-renders), sends it via header. The EF reads via `getIdempotencyKey(req)` from the shared helper `supabase/functions/_shared/idempotency.ts` (1 export, validates UUID regex, returns `string | null`) and propagates it as `p_idempotency_key` arg to the RPC. Reference : S25 `refund-order` EF + `refund_order_rpc_v2` (the RPC already had the arg from S13, only the EF + POS wiring was missing).
  2. **RPC arg `p_client_uuid` / `p_idempotency_key` (idempotence sémantique métier)** — For RPCs where idempotency is intrinsic to the business flow (e.g., "this cart, this tap" for tablet, "this payment record" for B2B, "this stock movement record" for inventory). The arg is REQUIRED at the RPC level (NOT NULL CHECK) and used as the primary key of a **dedicated** idempotency-keys table (never as a nullable column on the business table — isolation makes REVOKE simpler and avoids polluting the metric tables). Concurrency race handled via PK `unique_violation` catch + re-read. References : `create_tablet_order_v3(p_client_uuid)` with `tablet_order_idempotency_keys` (S25, bumped v3 S59), `record_b2b_payment_v2(p_idempotency_key)` with `b2b_payments.idempotency_key UNIQUE` (S24, bumped v2 S52), `record_stock_movement_v1` & family (S12). Replay returns the result of the first successful execution — by convention, RPCs return either the exact same value or an envelope `{ ..., idempotent_replay: true }` so callers can audit.

### Git
- Branches: `swarm/session-N` for ongoing session work, `feat/<scope>` or `fix/<scope>` for focused PRs. For phased plans, prefer `swarm/session-N` and squash-merge per phase.
- Commits: conventional commits (`feat(scope): …`, `fix(scope): …`, `test(scope): …`, `docs(scope): …`, `refactor(scope): …`). Co-author Claude when AI-assisted.

## Agent Comms (SendMessage-First Coordination)

Named agents coordinate via `SendMessage`, not polling or shared state.

```
Lead (you) ←→ architect ←→ developer ←→ tester ←→ reviewer
              (named agents message each other directly)
```

### Spawning a Coordinated Team

```javascript
// ALL agents in ONE message, each knows WHO to message next
Agent({ prompt: "Research the codebase. SendMessage findings to 'architect'.",
  subagent_type: "researcher", name: "researcher", run_in_background: true })
Agent({ prompt: "Wait for 'researcher'. Design solution. SendMessage to 'coder'.",
  subagent_type: "system-architect", name: "architect", run_in_background: true })
Agent({ prompt: "Wait for 'architect'. Implement it. SendMessage to 'tester'.",
  subagent_type: "coder", name: "coder", run_in_background: true })
Agent({ prompt: "Wait for 'coder'. Write tests. SendMessage results to 'reviewer'.",
  subagent_type: "tester", name: "tester", run_in_background: true })
Agent({ prompt: "Wait for 'tester'. Review code quality and security.",
  subagent_type: "reviewer", name: "reviewer", run_in_background: true })

// Kick off the pipeline
SendMessage({ to: "researcher", summary: "Start", message: "[task context]" })
```

### Patterns

| Pattern | Flow | Use When |
|---------|------|----------|
| **Pipeline** | A → B → C → D | Sequential dependencies (feature dev) |
| **Fan-out** | Lead → A, B, C → Lead | Independent parallel work (research) |
| **Supervisor** | Lead ↔ workers | Ongoing coordination (complex refactor) |

### Rules

- ALWAYS name agents — `name: "role"` makes them addressable
- ALWAYS include comms instructions in prompts — who to message, what to send
- Spawn ALL agents in ONE message with `run_in_background: true`
- After spawning: STOP, tell user what's running, wait for results
- NEVER poll status — agents message back or complete automatically

## Swarm & Routing

Reach for a multi-agent swarm (3+ agents) on: new features, cross-module refactors, API changes, security, performance. Use a single agent (or none) for: 1–2 line fixes, docs, config changes, questions.

| Task | Suggested agents |
|------|------------------|
| Bug fix | researcher, coder, tester |
| Feature | architect, coder, tester, reviewer |
| Refactor | architect, coder, reviewer |
| Security | security-architect, security-auditor |

> Optional tooling: the `claude-flow` / `ruflo` CLI (`npx @claude-flow/cli@latest …`) and its MCP coordination/memory tools (`memory_store`, `memory_search`, `swarm_init`, `agent_spawn`, `hooks_route`) are available — discover schemas via `ToolSearch("keyword")` if a task needs cross-session memory or hook routing. Day-to-day work uses the Agent + SendMessage team above and the project skills below.

## Agents

**Generic types** (any string works as a custom type): `coder`, `reviewer`, `tester`, `planner`, `researcher`, `system-architect`, `backend-dev`, `security-architect`, `security-auditor`, `performance-engineer`, `pr-manager`, `release-manager`, the coordinators (`hierarchical-coordinator`, `mesh-coordinator`, `adaptive-coordinator`), etc.

### Project agents & skills (The Breakery — adaptés au projet)

The repo versions a **specialized team** (PR #55). **Agents** (`.claude/agents/*.md`) are spawnable via the Agent tool ; **skills** (`.claude/skills/<name>/SKILL.md`) auto-trigger via their frontmatter `description` ONLY (the harness matches the prompt against it — `pathPatterns`/`promptSignals` are inert metadata, kept as documentation ; descriptions rewritten "pushy" with FR+EN trigger phrases 2026-07-07). Each points back to this CLAUDE.md as the source of truth and verifies the real schema (migrations/MCP) before asserting a fact.

**Agents** (`.claude/agents/`) :
- `pos-specialist` (sonnet) — `apps/pos/`
- `backoffice-specialist` (sonnet) — `apps/backoffice/`
- `db-engineer` (sonnet) — `supabase/migrations/` + RPCs (versioning, REVOKE pairs, MCP V3)
- `edge-functions-engineer` (sonnet) — `supabase/functions/` (PIN header, idempotency, rate-limit)
- `pattern-guardian` (sonnet, **read-only**) — reviews a diff vs the Critical patterns above
- `test-engineer` (sonnet) — pgTAP/Vitest/smoke + baseline env-gated
- `session-coordinator` (**opus**) — orchestration `swarm/session-N` (spec→plan→waves→closeout)

**Skills** (`.claude/skills/`, auto-triggered) :
- `stock-management` — inventory/recipes/production/WAC/lots
- `accounting` — COA/JE/PB1 NON-PKP/fiscal/GL/TB · `b2b-credit` — AR/credit-limit/b2b_payments
- `reports-exports` — report RPCs/PDF/CSV/Z-report/drill-down · `expense-governance` — thresholds/SOD/multi-step
- `products-catalog` — products CRUD/variants/categories · `orders` — lifecycle/list v2/edit-items/void/refund
- `security-auth` — RLS/REVOKE/perms/PIN-JWT/rate-limit · `breakery-ui-kit` — conventions `packages/ui`

Design : `docs/superpowers/specs/2026-05-31-agents-skills-team-design.md` (spec) + `docs/superpowers/plans/2026-05-31-agents-skills-team.md` (plan). `.gitignore` versions the root `.md` files of `.claude/agents/` ; the ruflo subfolders stay ignored.

### Plugin skill routing (MANDATORY — invoke BEFORE acting)

Plugin skills under-trigger by default. On this project, route these task shapes to their plugin skill **before** any other action (their descriptions live in the plugin cache and can't be tuned — this table is the trigger) :

| Si la tâche est… | Invoque D'ABORD |
|---|---|
| Nouvelle feature / « ajoute / crée / construis X » | `superpowers:brainstorming` puis `feature-dev:feature-dev` |
| Bug, test rouge, comportement inattendu (« ça marche pas », « pourquoi ça fait ça ») | `superpowers:systematic-debugging` |
| Implémenter une feature/bugfix (après brainstorm/plan) | `superpowers:test-driven-development` |
| Exécuter un plan daté (`docs/workplan/plans/*.md`) | `superpowers:subagent-driven-development` ou `superpowers:executing-plans` |
| « C'est fini / ça passe / prêt à merger » — avant toute affirmation de succès | `superpowers:verification-before-completion` puis `superpowers:requesting-code-review` |
| Branche terminée, décider merge/PR/cleanup | `superpowers:finishing-a-development-branch` |
| Revue du diff courant / d'une PR | `code-review` intégré (`/code-review`, PR → `/review`) |
| Simplifier/refactorer du code récent sans changer le comportement | `simplify` intégré (`/simplify`) |
| Question sur une lib/framework/SDK (React, Supabase, Tailwind…) | `find-docs` (Context7) — jamais de mémoire d'entraînement |
| Nouvelle UI visuelle (pas POS) | `frontend-design:frontend-design` (POS → `pos-design-craft` local) |
| CLAUDE.md à auditer/mettre à jour | `claude-md-management:*` |
| Créer/modifier une skill | `skill-creator:skill-creator` |

Le tableau complète (ne remplace pas) la règle superpowers « 1% de chance qu'une skill s'applique → invoque-la ».

## Build & Test

- ALWAYS run tests after code changes
- ALWAYS verify build succeeds before committing
- This project uses **pnpm 9.15** + **turbo** — never `npm`

### Local commands (no Docker required)
```bash
pnpm build && pnpm test     # turbo run build / turbo run test --concurrency=1
pnpm typecheck               # turbo run typecheck
```

### DB workflow — Supabase cloud staging (Docker retired 2026-05-14)
All DB operations target the cloud V3 dev project `ikcyvlovptebroadgtvd`. **Do NOT run** `pnpm db:reset`, `supabase start`, or `bash supabase/tests/run_pgtap.sh` — Docker is gone.

| Operation | MCP tool | Notes |
|---|---|---|
> ⚠️ Le serveur MCP Supabase actif est le **connecteur claude.ai** (préfixe `mcp__claude_ai_Supabase__`) — le plugin `supabase@claude-plugins-official` (préfixe `mcp__plugin_supabase_supabase__`) est présent en cache mais **désactivé** (2026-07-07). Si le préfixe change, chercher les outils via ToolSearch("supabase apply migration").

| Apply migration | `mcp__claude_ai_Supabase__apply_migration` | `project_id='ikcyvlovptebroadgtvd'`, `name` in snake_case, body = SQL. Wrapped in transaction. |
| Run SQL (incl. pgTAP) | `mcp__claude_ai_Supabase__execute_sql` | Use `BEGIN ... ROLLBACK` envelope for pgTAP. Extension `pgtap` already enabled. |
| Regen types | `mcp__claude_ai_Supabase__generate_typescript_types` | Returns `{ types: "..." }` — write to `packages/supabase/src/types.generated.ts` and commit. |
| Check drift | `mcp__claude_ai_Supabase__list_migrations` | Compares `supabase_migrations.schema_migrations` to local. |
| Direct psql (rare) | `postgresql://postgres.ikcyvlovptebroadgtvd:<URL_ENCODED_PWD>@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres` | Always go through the pooler — `db.<ref>.supabase.co` has no DNS A record. |

Dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>

### Targeted iteration (much faster than full suite during phase work)
```bash
pnpm --filter @breakery/supabase test inventory     # Vitest live RPC tests
pnpm --filter @breakery/backoffice test inventory   # BO smoke + unit
pnpm --filter @breakery/domain test inventory       # pure-TS unit
```

After Supabase schema changes (new migration via MCP `apply_migration`), **always** regen types via `mcp__claude_ai_Supabase__generate_typescript_types`, write to `packages/supabase/src/types.generated.ts`, and commit. A missing regen is the #1 cause of broken CI on this repo.

### Inventory phase test layout
- pgTAP (DB): `supabase/tests/inventory.test.sql` (steady-state suite) + `supabase/tests/inventory_phase1_complete.test.sql` (phase 1 acceptance — T1-T15+).
- Vitest live RPC: `supabase/tests/functions/inventory-*.test.ts` (per-phase, one file per RPC family).
- Domain unit: co-located `__tests__/` in `packages/domain/src/inventory/`.
- BO smoke/unit: co-located `__tests__/` in `apps/backoffice/src/features/inventory*/`.
