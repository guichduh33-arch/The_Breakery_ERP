# Ruflo — Claude Code Configuration

## Rules

- Do what has been asked; nothing more, nothing less
- NEVER create files unless absolutely necessary — prefer editing existing files
- NEVER create documentation files unless explicitly requested
- NEVER save working files or tests to root — this is a pnpm/turbo monorepo: code goes in `apps/{pos,backoffice}/src`, `packages/{domain,supabase,ui,utils}/src`, or `supabase/{functions,migrations,tests}`. Co-locate tests in `__tests__/` next to the code.
- ALWAYS read a file before editing it
- NEVER commit secrets, credentials, or .env files
- Keep files under 500 lines
- Validate input at system boundaries

## Active Workplan

> Read this **before** opening code on Session 20+ work.

- **Latest on `master` — standalone PRs post-S45 (2026-06-16 → 06-17, base `master` @ `565edf7`) :** trois PRs livrées après le merge S45. **PR #83** (`7503e9a`, 2026-06-16) — *catalog-import numeric hardening + Costing unit* (DEV-S45-IMP-01) : nouvelle règle **V19** de `import_catalog_v1` (bornes/magnitude NUMERIC) — les colonnes staging temp étaient non contraintes et V1-V18 ne vérifiaient jamais la magnitude → une valeur trop grande passait le dry-run `valid=true` puis crashait au commit en `22003` (numeric_field_overflow) → 400 PostgREST opaque sans body ; V19 miroir les bornes products (retail/wholesale 12,2 ; cost 14,2 ; min_stock 10,3 ; variants.retail 12,2) **+** recipes.quantity (10,3) + product_unit_alternatives.factor_to_base (20,10) → le dry-run reporte la cellule exacte `{sheet,row,sku}` code `value_out_of_range` (migrations `_013` products + `_014` recipes/units, `CREATE OR REPLACE` body déployé conservant correctives S41 `_014/_015/_016`, signature inchangée). Bug latent connexe : `_calculate_recipe_cost_walk` en `DECIMAL(14,4)` (plafond ~10^10) + `_snapshot_recipe_version` recast `NUMERIC(14,2)` → un coût recette ~10^10 (ex. quantité en grammes ×1M pcs) crashait `22003` sur **tout** chemin recette-edit (import W7, éditeur BO, production) → migration `_015` élargit les working vars en `NUMERIC(38,s)` (même scale 4/2, valeurs stockées inchangées, plafond ~10^34). Migration `_016` ajoute `recipe_unit` à `recipe_bom_full_v1` (l'onglet Costing affichait l'unité de stock matériau `kg` au lieu de l'unité de ligne recette `gr` — désaccord avec l'onglet Recipe) — `RETURNS TABLE` → DROP+CREATE, grants ré-appliqués, **types regen committé** (additif, consumers existants inchangés) ; front `BomLine`/`CostingPanel` rendent `recipe_unit`. **Tests** : pgTAP `catalog_import` **31/31** cloud (T25-T29 neufs) + `recipe_bom_full_v1` **10/10** + costing-panel smoke 3/3 + typecheck. **PR #84** (`759cd35`, 2026-06-17) + **PR #86** (`0099eb6`, 2026-06-17) — *POS visual & UX polish + Backoffice detail-page redesigns*. **POS** (component .tsx only — zéro changement store/hook/RPC/prop, via skill pos-redesign) : motion tactile (Button `active:scale`, ProductCard hover-lift/ken-burns, CategoryNav/CartLineRow/ActiveOrderPanel TOTAL pulse, BottomActionBar gold-hover/pressed) ; token rouge canonique unique `--red-fg` (3 rouges Tailwind bruts coexistaient → `red.fg`/`danger.fg`, remises/void/cancel routés ; teintes catégorielles avatars/split-payers conservées) ; ProductGrid skeletons shimmer ×8 ; Checkout/Void sur primitive Button ; cart TOTAL `text-2xl→3xl` ; règle CTA documentée (OR = naviguer vers paiement, VERT = engager l'argent irréversible) ; refonte payment-flow (QuickPayRow info-token + h-14, TenderDraftPanel presets min-h-44 WCAG 2.5.5, PaymentMethodGrid/ModeSelectStep press, **SuccessModal hero « Change to give »** text-4xl gold quand monnaie due, omis card/QRIS). **Backoffice** (#86, refontes data-driven sans migration) : **Customers** detail page riche (header avatar/badges, hero loyalty available/lifetime + progression palier, 4 KPI Visits/Spent/Avg Basket/Last Visit, onglets Info·Orders·Loyalty·Analytics·Pricing agrégés read-only) + lignes liste cliquables ; **Orders** refonte « Live Orders » (header Live/Refresh/Export CSV, 5 KPI, filtres compacts, pills statut mappés `order_status`, filtres URL S32/S33 préservés) + nouveau `OrderDetailDrawer` (Sheet réutilise `useOrderDetail` + `kitchen_status`) ; **Suppliers** onglets Price Evolution / Payments / Analytics activés (ex-« coming soon ») via nouveau hook `useSupplierPurchaseItems` (PO line items inner-join, pas d'RPC) + fix double « Rp Rp ». **Tests** : ui+pos typecheck clean, 46/46 payment + smokes BO (customers 3, orders 4, suppliers 3).
- **Session 47 reference (en cours, non mergée) :** Session 47 — Configurable Combos, sur `swarm/session-47-combos` (base `master` post-S46). Spec : [`docs/workplan/specs/2026-06-19-session-47-configurable-combos-spec.md`](docs/workplan/specs/2026-06-19-session-47-configurable-combos-spec.md), Plan : [`docs/workplan/plans/2026-06-19-session-47-configurable-combos-plan.md`](docs/workplan/plans/2026-06-19-session-47-configurable-combos-plan.md), INDEX : [`docs/workplan/plans/2026-06-19-session-47-INDEX.md`](docs/workplan/plans/2026-06-19-session-47-INDEX.md). Remplace le modèle combo « bundle figé » par des **choice groups** (single/multi, requis/optionnel, surcharge + défaut par option). Un combo reste une ligne `products` (`product_type='combo'`) ; 2 tables `combo_groups`/`combo_group_options` portent la structure ; les surcharges voyagent dans `order_items.modifiers`, les composants choisis dans un nouveau snapshot `order_items.combo_components` JSONB. **Vendu comme UNE ligne au prix de base du combo**, le stock déduit sur chaque **composant** choisi (le produit combo virtuel n'a pas de stock). **DB** (NAME-block `20260704000010..022`) : schema + guards (parent-est-combo, anti-nesting) ; `upsert_combo_v1` (REPLACE, idempotency, gates `combos.create`/`combos.update`) ; `delete_combo_v1` soft-delete (`combos.delete`) ; migration legacy → groups + **drop `combo_items`** ; **`complete_order_with_payment_v12→v13`** (+`order_items.combo_components`, déduit les composants pas le combo) ; reversals void/refund combo-aware ; **extension fire-path (DEV-S47-Aprime-01, validée owner)** : `fire_counter_order_v2→v3` (persiste le snapshot combo_components) + `pay_existing_order_v8→v9` (déduit chaque composant persisté, display-aware) — sinon un combo firé-puis-payé en dine-in échouait `P0002` (v8 vérifiait le stock du combo virtuel = 0). **Perm (DEV-S47-A2-01)** : réutilise `combos.{create,update,delete}` (S11), **pas** de nouvelle `products.combos.write`. **Domain** : `pricing` (`configuredPrice`/`priceRange`/`savingsPct`) + `validateSelection` (IO-free) + `addComboItem`. **BO** : ComboBuilderPage (general info + choice groups DnD + ComboProductPicker) + hooks + routes `combos/new`·`:id/edit` + CombosPage rewire. **POS** : `useComboConfig`, `ComboConfigModal` (radio/checkbox bornés min/max, défauts pré-cochés, prix live), tap→modal→`addCombo` (une ligne combo portant `combo_components`+`modifiers`), payloads forward `combo_components`, EF `process-payment` redeploy **v13** (`verify_jwt=false` — obligatoire car v12 droppé). **Test-infra (DEV-S47-D3-02)** : `afterEach(cleanup)` ajouté à `apps/pos/vitest.setup.ts` (suite POS sans isolation fichier → l'auto-cleanup RTL ne s'enregistre que dans le 1ᵉʳ fichier → les portails Radix Dialog s'accumulaient → « multiple elements ») + `ComboConfigModal.smoke` passe en import statique (29s→10s). **Tests** : pgTAP cloud `combo_crud`/`combo_migration`/`combo_sale`/`combo_reversal` + **`combo_fire_pay` 8/8 neuf** ; Vitest domain 40/40 + POS `combo` glob 21/21 + fire/pay/checkout 20/20 ; typecheck **6/6**. Process : subagents sans MCP → contrôleur applique migrations + pgTAP cloud + regen types + nettoie fichiers 0-octet racine. **Hors scope S48+** : quantité>1 par option, combos imbriqués, routage stations des composants (la ligne combo route par le `dispatch_station` du produit combo), combos à l'import catalogue, combos tablette B2B, vérif live navigateur. Déviations : INDEX (7 entrées — 3 medium).
- **Session 46 reference:** Session 46 — Purchasing Hardening (page Backoffice Achats) — **PR #95 ouverte** (non mergée) from `swarm/session-46` (base `master` @ `80cfaba`, post-merge PR #93/#94). Spec : [`docs/workplan/specs/2026-06-18-session-46-purchasing-hardening-spec.md`](docs/workplan/specs/2026-06-18-session-46-purchasing-hardening-spec.md), Plan : [`docs/workplan/plans/2026-06-18-session-46-purchasing-hardening-plan.md`](docs/workplan/plans/2026-06-18-session-46-purchasing-hardening-plan.md), INDEX : [`docs/workplan/plans/2026-06-18-session-46-INDEX.md`](docs/workplan/plans/2026-06-18-session-46-INDEX.md). Durcit `/backoffice/purchasing` sur 4 axes. **R1 picker matière première** : `useAllProductsForPO` filtre `categories.category_type='raw_material'` (172, embed `product_unit_alternatives`+`product_unit_contexts` en 1 requête) **+ guard server-side** sur `create_v2`/`update_v1` (badge SKU présentation-only **non** utilisé). **R2 unité contrainte** : input texte libre 16 car. → `<select>` borné (unité de base ∪ alternatives, défaut = `purchase_unit`) ; la ligne persiste `unit_factor_to_base` **create + edit** ; `receive_purchase_order_v2` convertit `qty × factor` en unité de base **avant** `record_stock_movement_v1` (le stock reste en base). **R3 paiements traçables indépendants** : ledger **append-only** `purchase_payments` (`record_po_payment_v1` gated `purchasing.po.pay`, idempotency flavor 2, helper interne no-gate `_record_po_payment_internal` appelé par le trigger pour l'auto-pay cash — DEV-S46-C1) ; trigger `create_purchase_journal_entry` redessiné : **DR INVENTORY / CR PAYABLE 2141 toujours** à la réception (VAT-fold NON-PKP ADR-003 préservée) + JE paiement séparée **DR 2141 / CR 1110 cash ou 1112 banque** (nouveau mapping `PURCHASE_PAYMENT_BANK`, split par méthode) ; badge **dérivé du ledger** (`derivePaymentStatus`, unpaid/partial/paid) **indépendant de la réception** ; auto-paiement à la réception pour termes `cash`. **R4 édition** : `update_purchase_order_v1` gated `purchasing.po.edit` + **lock D6** dès la 1ʳᵉ GRN OU le 1ᵉʳ paiement (P0001 `po_locked`) ; bouton Edit (jadis inerte) câblé → `POFormDraft` mode édition (prop `submitLabel`). **DB** (NAME-block `20260701000010..019`, 10 migrations) — voir bullet « Migration sequence active ». **2 perms** `purchasing.po.{pay,edit}` (MANAGER/ADMIN/SUPER_ADMIN), union `PermissionCode` étendu, types regen committé. **Décision clé DEV-S46-B2-01 (medium)** : bump **`create_purchase_order_v1 → v2`** indispensable — v1 ne persistait pas le facteur ni ne vérifiait la MP → R1/R2 n'auraient tenu que sur le chemin *edit* (un PO créé avec une unité non-base convertissait à facteur 1 = stock faux). Hook `useProductPurchaseUnits` prévu au plan **non créé** (agrégation embarquée dans `useAllProductsForPO`, DEV-S46-B2-02). **Tests** : pgTAP cloud **54/54** (po_create 6 + po_receive 13 + po_payments 14 + po_je 11 + po_update 10 ; fix bug latent parenthèse `po_je` T8 DEV-S46-B-JE-T8) + BO purchasing **28/28** smokes + typecheck 6/6 ; **pattern-guardian** 0 HIGH (MEDIUM `purchase_payments` TRUNCATE non révoqué → contourne l'append-only `REVOKE DELETE` → corrective `_019` mirroir Stock-Audit m1, DEV-S46-B-PG1 ; casts `as never` → bound rpc DEV-S46-B-PG2) ; **sweep BO 540 pass / 1 skip / 19 flakes** (baseline `waitFor` sous coverage+charge S42–S45, **18/18 verts en isolé**, purchasing jamais en échec, 0 régression réelle). **Process DEV-S46-PROC-01** : subagents sans MCP → contrôleur applique migrations + rejoue pgTAP en cloud + regen types + nettoie les fichiers 0-octet racine. **Open-questions tranchées** : #1 JE paiement = split par méthode (1110 cash / 1112 banque) ; #2 **pas de backfill** (0 PO cash reçu). **Hors scope S47+** : correction badge SKU « Raw Material » présentation-only, split multi-compte banque avancé, void/reversal d'un paiement partiel, multi-devise fournisseur, vérif live navigateur (non exécutée). Déviations : INDEX §9 (8 entrées — 2 medium fermées).
- **Session 45 reference:** Session 45 — Products page : finir les actions catalogue (6 boutons morts) — ✓ **merged 2026-06-14 via PR #82** from `swarm/session-45` (base `master` @ `b80e3d9`, post-merge S44 PR #81, 9 commits). Spec : [`docs/workplan/specs/2026-06-13-session-45-products-actions-spec.md`](docs/workplan/specs/2026-06-13-session-45-products-actions-spec.md), Plan : [`docs/workplan/plans/2026-06-13-session-45-products-actions-plan.md`](docs/workplan/plans/2026-06-13-session-45-products-actions-plan.md), INDEX : [`docs/workplan/plans/2026-06-13-session-45-INDEX.md`](docs/workplan/plans/2026-06-13-session-45-INDEX.md). Audit live navigateur (playwright-cli, Owner) : 6 boutons de `/backoffice/products` **dessinés mais jamais câblés** (vestiges en-tête S14). **Objectif atteint : zéro bouton mort.** **DB** (NAME-block `20260629000010..012`) : `delete_product_v1(UUID, UUID)` SECURITY DEFINER **soft-delete** (`is_active=false` **+ `deleted_at=now()`** — le catalogue filtre `deleted_at IS NULL` et affiche les inactifs, donc `is_active` seul ne masquait pas, DEV-S45-A-01 corrective `_012`), gate `products.delete` (**perm pré-existante S13**, ADMIN+SUPER_ADMIN — pas MANAGER → corbeille masquée pour MANAGER), garde parent-de-variantes actives (P0001), idempotent (replay sur `deleted_at IS NOT NULL`), audit `product.deleted`, REVOKE pair ; `_010` RPC + `_011` REVOKE + corrective `_012` deleted_at. pgTAP `delete_product_v1` **8/8 cloud** (vérifié contrôleur via MCP — T1 deleted_at+exclu catalogue, T2/T2b CASHIER+MANAGER 42501, T3 parent P0001, T4 replay, T5 1 audit, T6 anon revoked, T7 deactivated-not-deleted). Types regen (additif). **Front** : `useDeleteProduct` (rpc bindé, idempotency `useRef`, guard `deleted===true`) + `DeleteProductDialog` ; `?tab=` param dans `ProductDetailPage` (lecture une fois au montage, DEV-S45-C-01) ; `ProductsTable` actions Delete/`$` **conditionnelles** aux handlers (plus de bouton inerte) ; `ProductsHeader` Import **masqué** sans `catalog.import` (DEV-S45-D-02, aligné sur l'onglet) + Recipes câblé + **Modifiers retiré** (DEV-S45-D-01, aucune page Modifiers dans le projet) + Products neutralisé `<span aria-current>` ; `Products.tsx` câble `canDelete`/`canEditPricing`/`canImport`. **Tests** : pgTAP 8/8 + smokes BO neufs (delete-dialog 7, table-delete-wiring 3, tab-param 4, table-pricing-wiring 3, header-pills 8) + typecheck 6/6 + sweep BO **535 pass / 1 skip** (5 flakes waitFor sous coverage+charge = baseline S42 DEV-S45-E-01, verts en isolé). **Vérif live navigateur** : delete réel (dialog→confirm→disparaît + DB `deleted_at`+audit), `$`→onglet General, Import→`/products/import-export`, Recipes→`/inventory/recipes`, Modifiers absent, Products non-bouton. **Reviews** : pattern-guardian 14/14, spec-review + code-quality par wave, **final whole-branch APPROVED**. **Process** : MCP indisponible en subagent (DEV-S45-PROC-01, migrations via CLI + contrôleur vérifie en cloud) ; subagents ont créé des fichiers 0-octet à la racine (DEV-S45-PROC-02, nettoyés, jamais committés). **Hors scope S46+** : page Modifiers globale, dialog pricing inline, bulk delete/restore/corbeille, hard-delete. Déviations : INDEX §4 (9 entrées — 1 medium DEV-S45-A-01 fixée + 1 décision D1 + 1 minor D2 fixée + le reste info/process).
- **Session 44 reference:** Session 44 — Money-Path Hardening ✓ **merged 2026-06-13 via PR #81** from `swarm/session-44` (base `master` @ `6f9c757`, 15 commits). Spec : [`docs/workplan/specs/2026-06-13-session-44-money-path-hardening-spec.md`](docs/workplan/specs/2026-06-13-session-44-money-path-hardening-spec.md), Plan : [`docs/workplan/plans/2026-06-13-session-44-money-path-hardening-plan.md`](docs/workplan/plans/2026-06-13-session-44-money-path-hardening-plan.md), INDEX : [`docs/workplan/plans/2026-06-13-session-44-INDEX.md`](docs/workplan/plans/2026-06-13-session-44-INDEX.md). Durcit le chemin argent du POS. **DB** (NAME-block `20260628000010..018`) : `get_loyalty_multiplier(INT)` miroir SQL de `tiers.ts` (D4) ; corrective `create_sale_journal_entry` — le CASE mappait `debit_card`/`credit_card` **inexistants** dans l'enum (`cash|card|qris|edc|transfer|store_credit`) → `card/edc/transfer` tombaient en cash ; fix + seed `SALE_PAYMENT_TRANSFER`→1112 + fallback cash **observable** (`audit_logs je.payment_fallback_cash`) ; **`complete_order_with_payment_v12`** (DROP v11) : statut `paid` posé **après** les `order_payments` → le trigger JE split par méthode (avant : fallback cash 100 %), montant promo ré-évalué server-side (`evaluate_promotions_v1`, mismatch=reject — **DEV-S44-B1-01** : subtotal d'éval = somme non-gift `unit_price*qty` brute, mirroir exact du client, PAS `v_items_total`), multiplier fidélité résolu en DB (`p_loyalty_multiplier` supprimé), `change_given` revalidé, enveloppe replay honnête ; **`pay_existing_order_v8`** (DROP v7) : idem + parité `display_stock` (décrément + check display-aware, DEV-S44-B2-01) ; **`fire_counter_order_v2`** (DROP v1) : gate d'autorité sur toute remise de **ligne** appendée (P0-C 3) ; corrective `_017` reversal — `void_order_rpc_v2` + `refund_order_rpc_v3` restaurent `display_stock` (`movement_type='adjustment'`, P1-C ; `cancel_order_item_rpc_v2` non affecté, pré-paiement) ; corrective `_018` P11 `ALTER DEFAULT PRIVILEGES` (DEV-S44-P11-01). Types regen (v12/v8/v2 présents, v11/v7/v1 absents). **EF** : `process-payment` v9→**v10** (`verify_jwt=false`, appelle v12, mapping `23514` → `promo_amount_mismatch`/`invalid_change` 409). **Front POS** : `useStationMap` (routage cuisine incluant les **variantes** que `useProducts` masque via `parent_product_id IS NULL` → fermait ticket amputé + bouton mort sur panier 100 % variantes, P0-B) ; `cartStore` `voidOrder` reset `pickedUpOrderId` (P1-A) + `clear()` purge client/table sans ligne fired (P1-B) + `HeldOrdersModal` bloque restore pendant ordre fired ; loyalty affiché depuis l'enveloppe serveur (D4, le multiplier n'est plus calculé client) ; `buildOrderPayload` drop multiplier ; classifier FR pour les 2 nouveaux codes. **Tests** : pgTAP cloud `s44_money_gates` **12/12** + `s44_je_by_method` **7/7** + `s44_display_symmetry` **8/8** + `counter_fire` **15/15** ; non-régression bumpée v12/v8 `order_discount_gate` 10/10 / `pay_existing_discount_gate` 8/8 / `loyalty_transactions_append_only` 5/5 ; sweeps domain/ui/pos (**470 + 1 skip**, 6 flakes waitFor sous coverage-load = baseline S42/S43, verts isolés) / backoffice ✅ ; typecheck 6/6 ; pattern-guardian 12/14 → **14/14** après `_018`. E2E `tests/e2e/s44-money-path.spec.ts` **authored** (run env-dépendant, sonde JE DB env-gated). **Hors scope** : backfill JE historiques, ledger store credit, `loyalty_balance_after` chemin pickup (v8), KDS ventes directes, append post-pickup tablette, reçu honnête. Déviations : INDEX §5.
- **Session 43 reference:** Session 43 — POS Live Audit Fixes (3 P0 + 3 P1 + 10 P2) ✓ **merged 2026-06-13 via PR #80** (`6f9c757`, squash) from `swarm/session-43` (base `master` @ `396bd93`, 35 commits, subagent-driven TDD + double revue spec/qualité par task). Spec : [`docs/workplan/specs/2026-06-12-session-43-pos-audit-fixes-spec.md`](docs/workplan/specs/2026-06-12-session-43-pos-audit-fixes-spec.md), Plan : [`docs/workplan/plans/2026-06-12-session-43-pos-audit-fixes-plan.md`](docs/workplan/plans/2026-06-12-session-43-pos-audit-fixes-plan.md). Ferme l'audit POS live du 2026-06-12. **P0-1 remises impayables** (gate v11 = toute remise, client = seuil 10 %) : DiscountModal always-authorize, hoist autorisateur ligne → top-level (`buildOrderPayload`), **nouvelle EF `verify-manager-pin`** (PIN-only via `_shared/manager-pin.ts`, header `x-manager-pin` S25, lockout SEC-07 partagé, audit `manager_pin.verified` — **DEV-S43-B1-01** : le plan visait `auth-verify-pin`, EF de LOGIN exigeant `user_id`, impossible pour le flux PIN-only) + hook fetch brut, code EF `discount_requires_authorizer` distinct, bannière fatale persistante (clear au close, kind fatal only). **P0-2 realtime mort sous PIN-auth** : `realtime.setAuth` dans les 2 setters de token + au create du client (`packages/supabase/client.ts`) + filets `refetchInterval` 30 s (inbox, KDS). **P0-3 fire comptoir non persisté** : RPC `fire_counter_order_v1` (create/append, idempotence flavor 2 table dédiée, clamp discount ligne `[0, brut]`, P0002 anti silent-skip) ; `useFireToStations` persiste AVANT d'imprimer (+ mode `printOnly` pour l'auto-fire post-paiement — fermait un Critical de revue : ordre orphelin sur chaque vente directe) ; checkout append-then-pay via `pay_existing_order_v7` + `markLocked` des lignes appendées (anti double-append reopen/refire) ; Hold désactivé sur ordre fired. **P1** : sold-out via `isSellable` domain (`track_inventory` + `display_stock` prime, fallback `current_stock` ; 4 hooks vitrine invalident `['products']`) ; close-shift note obligatoire au-delà du seuil de variance (aligné badge) ; history refetch on open + Remaining correct (`remainingDue` vs `remainingRefundable`). **P2** : confirm void local + modal note hold (fin de `window.prompt`), held label `Held {heure} · {table}`, `accessibleTitle` sur FullScreenModal (l'ancien défaut « Modal » écrasait l'accessible name de TOUS les modals — 12 call-sites câblés, titre sr-only en span), KDS `##` + badge PAID, défaut `take_out` (**D9 — à valider owner**), auto-submit PIN open-shift, favicon, data fixes images placeholder + zone Patio. **4 bugs latents de prod découverts et corrigés** : DEV-S43-C1-01/F1-02 `order_number` UNIQUE global vs séquence quotidienne → 23505 sur la 1ʳᵉ commande de chaque jour (v11/v2/fire ; unicité par jour, bucket fuseau DB Asia/Makassar — l'E2E a attrapé la fenêtre 00:00–08:00 en réel) ; **DEV-S43-F1-01** `_shared/acting-user.ts` lisait `SUPABASE_JWT_SECRET` jamais injecté en déployé → **void-order/cancel-item/refund-order toutes en 401 en environnement déployé** (masqué par le bug CORS x-app), fix + redeploy ×4 EFs ; DEV-S43-F1-03 gate v7 `draft`-only → élargi `draft OU (pending_payment ET created_via='pos')` (pickup-first tablette préservé) ; clamp money-path `_013` (discount > brut = line_total négatif encaissé tel quel par v7). **EFs déployées** : `process-payment` v9 (mapping P0001 + CORS x-app), `auth-verify-pin` v9, `verify-manager-pin` v1→v4 (nouvelle, config.toml `verify_jwt=true`), `void-order` v10, `cancel-item` v10, `refund-order` v11. **Tests** : pgTAP `counter_fire.test.sql` **11/11** cloud MCP ; **E2E Playwright 3/3** (`tests/e2e/s43-pos-audit-fixes.spec.ts` : T1 remise→PIN→200, T2 realtime inbox <10 s sans reload, T3 fire→reload→KDS→paiement du MÊME ordre) ; sweeps domain 59 files / ui 54 files / POS 466+1 skip (2 fichiers flake waitFor sous charge, 9/9 verts isolés — signature S42) ; typecheck 6/6 ; pattern-guardian **13/14** (1 MEDIUM de forme DEV-S43-P11-01 : REVOKE `FROM PUBLIC, anon` combiné — les 3 garanties P11 satisfaites, déviation documentée). **Hors scope (backlog)** : DEV-S43-C1-02 réconciliation server-side des prix (`unit_price` verbatim, préexistant via `create_tablet_order_v2`), sold-out `VariantSelectModal` + badge low-stock sur `current_stock` brut, `window.prompt` discard held, union `OrderDetail.status` périmée, `refetchOnWindowFocus` per-hook inbox/KDS, thread `requiredPermission` void via le nouveau hook, seed re-run cassé post-`_012` (`ON CONFLICT (order_number)`), pin du bucket séquence si changement de fuseau DB.
- **Session 42 reference:** Session 42 — Catalog Import/Export polish (8 Minors S41 Wave B) ✓ **merged 2026-06-12 via PR #78** (`81480f5`, squash) from `fix/catalog-import-minors` (base `master` @ `740f938`). Spec : [`docs/workplan/specs/2026-06-12-session-42-catalog-import-minors-spec.md`](docs/workplan/specs/2026-06-12-session-42-catalog-import-minors-spec.md), Plan : [`docs/workplan/plans/2026-06-12-session-42-catalog-import-minors-plan.md`](docs/workplan/plans/2026-06-12-session-42-catalog-import-minors-plan.md). **100 % front, zéro migration/types regen/pgTAP.** Ferme les 8 Minor de la revue S41 Wave B : **parser** (`parseCatalogWorkbook.ts`) P1 header dupliqué détecté row 1, P2 erreur duplicate-SKU sur vraie ligne Excel via `rowMaps`, P3 colonne requise absente = 1 erreur header-level (plus de bruit per-row), P4 plus de double erreur sur la même cellule (`coerceErrored`) ; **dropzone** P5 input `tabIndex={-1}` (a11y), drop non-xlsx → `toast.error` + check case-insensible, P6 garde `relatedTarget` sur dragleave ; **tabs** P7 rôles ARIA tab abandonnés → `nav` + `aria-current="page"` (liens de routes) ; **page** P8 label « Import N items » = somme create+update uniquement + pluralisation + fallback « Import catalog ». **+16 tests** (parser 4, dropzone 4, page 3, tabs 1 + fixtures) ; sweep BO **515/516** (1 skip pré-existant, zéro failure — 1ʳᵉ passe avec 10 flakes waitFor sous charge coverage+typecheck parallèles, re-run propre 100 % vert) ; typecheck 6/6. **1 déviation actée** : polyfill `DragEvent` existence-guarded dans `apps/backoffice/vitest.setup.ts` (jsdom ne l'implémente pas, testing-library droppait `relatedTarget`). Process : subagent-driven TDD, spec+quality review par task, follow-up pluralisation (`ee93925`), final review READY FOR PR. **Hors scope (backlog)** : validation extension sur le chemin picker (`handleChange`), test `relatedTarget: null`, CSV, import async EF, entités étendues, cleanup data legacy, compteur `restored`.
- **Stock Audit Fixes reference:** Stock Management Audit Fixes ✓ **merged 2026-06-12 via PR #76** (+ m4 follow-up **PR #77** `740f938`) from `swarm/stock-audit-fixes` (base `master` post-PR #74 S41 + PR #75 POS audit). Audit : [`docs/audit/2026-06-12-stock-management-audit.md`](docs/audit/2026-06-12-stock-management-audit.md), Plan : [`docs/workplan/plans/2026-06-12-stock-audit-fixes-plan.md`](docs/workplan/plans/2026-06-12-stock-audit-fixes-plan.md). Ferme 19/22 findings : **Wave A** (front, 5 commits `b8aff78..fce0507`) C1 bind `supabase.rpc` ×8 hooks + garde regex, C4-front section requise, M1 pickers `track_inventory`, M4 erreurs honnêtes, m3/m5/m6/m7 UI recettes. **Wave B** (8 migrations NAME-block `20260626000010..017`) : `_010` C2 cron spoilage — profil SYSTEM `...999` + branche `auth.uid() IS NULL AND session_user='postgres'` dans `record_stock_movement_v1` ; `_011` C3 cron marges — `margin_alerts` pct NUMERIC(7,2) **incl. `delta_pct`** (le plan ne couvrait que 2 colonnes) ; `_012` C4 `section_required` P0001 sur `record_production_v1` ET `record_batch_production_v1` (pattern S38 DO-block replace) ; `_013` M2 gate `insufficient_section_stock` sur les 2 chemins transfers (create direct + receive) + reset 7 `section_stock` négatifs tracés `audit_logs` — **déviation actée : pas de CHECK >= 0**, les flux production décrémentent des sections jamais seedées (convention m10) ; corrective `_014` M1-DB — `create_internal_transfer_v1` validait `is_active` → rejetait les ingrédients que le picker A3 liste désormais (`track_inventory`) ; `_015` M5 `production_in` valorisé au coût réellement consommé (somme `_leaf_consumption` ÷ yield réel) + trigger WAC étendu à `production_in` (JE production équilibrée, pgTAP balance < 1 IDR) ; `_016` m1 REVOKE TRUNCATE/TRIGGER/REFERENCES ×5 tables stock ; corrective `_017` `row_to_jsonb`→`to_jsonb` (42883) dans `get_product_dashboard_v1` + `get_movement_aggregates_v1` — **bug latent masqué par les pages mortes C1, trouvé par l'E2E D3**. **Wave C** : M6 sidebar Incoming/Transfers/Expiring stock + route `inventory/expiring` (ExpiringStockPage était non routée, lien badge mort) ; M7 — **l'éditeur `min_stock_threshold` existait depuis S27** (« Alert threshold », chaîne complète vérifiée jusqu'à l'allowlist RPC ; prémisse audit périmée) → durci (type number, clamp ≥ 0, helper text) + smoke régression. **Wave D** : skill `stock-management` à jour (fixes + conventions m9/m10/m11 + statut M3) ; E2E `tests/e2e/stock-inventory-pages.spec.ts` **4/4 PASS** (login partagé, garde console `reading 'rest'`, opname create→cancel réel). **2 crons vérifiés en réel** : `recompute_recipe_margins_v1` OK (12 checked, marge min constatée **-15 727 %** — preuve du 5,2) ; `mark_expired_lots_hourly` OK (1 lot expiré + 1 auto-waste de rattrapage). **Tests** : pgTAP 17/17 cloud (BEGIN/ROLLBACK, jwt-claims simulées) + Sidebar 17/17 + product-detail-save 3/3 + typecheck 6/6 + sweep BO complet. **Hors scope / restes** : M3 FIFO consommation → spec dédiée obligatoire (`docs/workplan/specs/2026-06-XX-fifo-consumption-spec.md`) ; m4 data-cleanup **exécuté (validé owner 12/06)** via `_018`+`_019` — catégorie « Ingredients » mergée, résidus de test soft-deleted, batch de seed mnémonique mort (BEV-CAPP/BEV-FLAT/PAS-PAIN/BRD-SOUR, 0 usage chacun, les jumeaux SKU numérotés portent toutes les commandes) soft-deleted ; **BEV-AMER restauré par corrective `_020`** — c'est l'Americano du seed canonique (composant COMBO-001, fixture de ~20 tests Vitest live + 2 suites pgTAP par SKU) dont le nom avait été écrasé en test_smoke ; le soft-delete `_018` cassait `update_cost_price_v1` en cascade (re-run post-restore : products_cost_price_guard 6/6 + update_product_v1 5/5) ; paires inter-catégories American Bagel / Cheesy Brie conservées (produits distincts, désambiguïsées par m3) ; m2 parents de variantes (décision produit) ; m8 auto-submit PIN (hors module).
- **Session 41 reference:** Session 41 — Catalog Import/Export (produits + recettes) ✓ **merged 2026-06-12 via PR #74** (`edc10c8`) from `swarm/session-41` (base `master` @ `f6f5947`, post-S40 + bump turbo 2.9.18 `a997a86`). Spec : [`docs/workplan/specs/2026-06-12-session-41-catalog-import-spec.md`](docs/workplan/specs/2026-06-12-session-41-catalog-import-spec.md), Plan : [`docs/workplan/plans/2026-06-12-session-41-plan.md`](docs/workplan/plans/2026-06-12-session-41-plan.md), INDEX : [`docs/workplan/plans/2026-06-12-session-41-INDEX.md`](docs/workplan/plans/2026-06-12-session-41-INDEX.md). Livre l'onglet **« Import / Export »** de la page Products BO (`/backoffice/products/import-export`, gate `catalog.import`, onglet masqué sans perm, route avant `:productId`) : template Excel 6 onglets (Categories/Ingredients/Products/Units/Variants/Recipes — généré client-side depuis `templateDefinition.ts`, source de vérité unique parseur/template/export), **export complet round-trip** (`export_catalog_v1()`, gate `catalog.export`, shape = payload d'import exact), **import bulk atomique** `import_catalog_v1(p_payload, p_dry_run, p_idempotency_key)` — validation exhaustive V1-V18 (cycles BOM niveau SKU sur graphe effectif, conversions, conflits variant↔standalone 2 sens, contextes d'unités, REPLACE units), dry-run zéro écriture, upsert par SKU, idempotency flavor 2 (table dédiée `catalog_import_idempotency_keys`), audit `catalog.imported`. Front : parsing 100 % navigateur (SheetJS `xlsx` en dynamic import only), stepper idle→parsed→previewed→done, erreurs `{onglet, ligne Excel via rowMaps, sku, code, message}`. **7 migrations** NAME-block `20260625000010..016` (4 planifiées + 3 correctives) + types regen + `PermissionCode` +2. **Tests** : pgTAP `catalog_import.test.sql` **26/26 PASS** cloud (durci ×2 : +9 assertions post spec-review, re-run post-correctives) ; Vitest catalog-import 16/16 ; sweep BO **485/486** (1 skip pré-existant, zéro nouvelle failure) ; typecheck 6/6 ; **E2E Playwright** `tests/e2e/s41-catalog-import.spec.ts` **4/4 PASS** + vérif DB + cleanup. **2 bugs réels trouvés par l'E2E, invisibles en pgTAP** (voie PostgREST exercée seulement par le navigateur) : protection Supabase « dangerous writes » rejetait l'UPDATE sans WHERE in-RPC (21000, dry-run cassé en réel → corrective `_015` `WHERE 1=1`, DEV-S41-T6-01 HIGH) ; SKU soft-deleted bloquait le ré-import (UNIQUE global 23505 → corrective `_016` restore semantics, DEV-S41-T7-01). **Reviews** : spec-review Wave A ✅ (5 trous pgTAP fermés) + pattern-guardian **14/14** + review Wave B ✅ (3 Important fixés `05a8f26` + commit-time `valid:false` fixé `11a2702`, re-review APPROVED). **Hors scope S42+** : 8 Minor UI (a11y dropzone, ARIA tabs, header dupliqué…), allergènes/modifiers/sections/combos à l'import, CSV, import async EF, nettoyage données legacy dev incohérentes (recettes `g` sur matériaux `cup`/`pcs`). Deviations tracked in INDEX §5 (10 entries — 1 high + 2 medium + 1 important, tous fixés).
- **Session 40 reference:** Session 40 — Reports close-out : les 9 cards « Soon » du hub ✓ **merged 2026-06-12 via PR #73** (`ce5d9a4`, squash) from `swarm/session-40` (base `master` @ `e3ec866`, post-merge S39 PR #72). Spec : [`docs/workplan/specs/2026-06-12-session-40-spec.md`](docs/workplan/specs/archive/2026-06-12-session-40-spec.md), Plan : [`docs/workplan/plans/2026-06-12-session-40-plan.md`](docs/workplan/plans/archive/2026-06-12-session-40-plan.md), INDEX : [`docs/workplan/plans/2026-06-12-session-40-INDEX.md`](docs/workplan/plans/archive/2026-06-12-session-40-INDEX.md). **Note** : la session d'exécution initiale a crashé (API 529) en pleine task A1 — reprise 2026-06-12 avec re-vérification complète du local vs cloud (rien n'était appliqué ni committé). Active les 9 dernières cards « Soon » du hub Reports — **hub 100 % actif (26 liens, 0 Soon)**. **Wave A (DB)** : trigger `audit_role_permissions_changes` (INSERT/DELETE sur `role_permissions` → `audit_logs` `role.permission_granted`/`revoked` — ferme le trou d'observabilité RBAC) + 9 RPCs reports pattern S30 (`get_daily_sales_v1`, `get_purchase_items_v1` LIMIT 1000+truncated, `get_purchase_by_date_v1`, `get_purchase_by_supplier_v1` share_pct/avg_lead_days, `get_staff_performance_v1` 5-CTE served/voids/refunds/discounts/cancelled, `get_production_report_v1`, `get_production_efficiency_v1`, `get_price_changes_v1` LAG sur historique complet `product.update`, `get_permission_changes_v1`) — **aucune nouvelle table, aucune nouvelle permission**. 2 bugs réels trouvés par les gates : refunds des jours sans vente perdus (pgTAP T5 → corrective `_015` FULL OUTER JOIN, DEV-S40-A1-01) ; gate `audit_log.read` désaligné du front `reports.audit.read` (→ corrective `_021`, DEV-S40-C-01). **Wave B (BO, 3 agents parallèles)** : 9 hooks + 9 pages (modèle WastagePage, ExportButtons **CSV-only**, KPI cards, drill-downs Daily Sales→orders + Production→product) + 18 smokes. **Wave C** : 9 routes lazy PermissionGate + 9 entrées sidebar (nouveau sous-groupe « Purchase reports », DEV-S40-C-03) + hub 0 Soon + smoke hub 26 liens. **13 migrations** NAME-block `20260624000010..022` (10 planifiées + 3 correctives `_015`/`_021`/`_022`) + types regen (+44 lignes). Schema discoveries : `orders.discount_amount` (pas `discount`), `production_date` **timestamptz** (pas DATE), `yield_variance_pct` GENERATED stocke un **ratio** → exposé ×100 (DEV-S40-A2-01/02/03). **Tests** : pgTAP `s40_reports.test.sql` **22/22 PASS** via cloud MCP (T1 durci post spec-review — était vacueux) + smokes 9 pages + sweep BO 472/473 (1 skip pré-existant) + sweeps domain/UI/POS ✅ + typecheck 6/6. **E2E Playwright** `tests/e2e/s40-reports.spec.ts` **T1-T4 4/4 PASS** (hub 0 Soon/26 liens ; Daily Sales KPI + download CSV non vide ; Purchase by Supplier 0 erreur console ; Permission Change Log). **Reviews** : spec-reviewers Wave A ✅ + Wave B ✅ (shapes hooks↔RPC exacts ×9) ; pattern-guardian **13/14** (P11 ×12 MEDIUM — 3ᵉ ligne `ALTER DEFAULT PRIVILEGES` manquante → fermés par corrective unique `_022`, DEV-S40-D-01). **Hors scope S41+** : drill `served_by` Staff Performance, compare toggle + extra dims sur les 9 reports, wiring `aging_buckets`→`view_ar_aging`, stubs ProductDetail, PAT-01/02, POS-16/17, F-010..013/019..024, BO-08/21, dé-flakage `journal-entries`. Deviations tracked in INDEX §3 (17 entries — 3 medium toutes fixées, le reste informational).
- **Session 39 reference:** Session 39 — Backoffice Completion Bundle (BO-04/09/10/15) ✓ **merged 2026-06-11 via PR #72** (`0c9ee17`, squash) from `swarm/session-39` (base `master` @ `79d7f13`, post-merge S38 PR #70). Spec : [`docs/workplan/specs/2026-06-11-session-39-spec.md`](docs/workplan/specs/archive/2026-06-11-session-39-spec.md), Plan : [`docs/workplan/plans/2026-06-11-session-39-plan.md`](docs/workplan/plans/archive/2026-06-11-session-39-plan.md), INDEX : [`docs/workplan/plans/2026-06-11-session-39-INDEX.md`](docs/workplan/plans/archive/2026-06-11-session-39-INDEX.md). Ferme les 4 stubs BO restants — un seul chantier DB. **Wave A (BO-15 DB)** : table singleton `b2b_settings` (id=1 CHECK, RLS sans policy, REVOKE table — accès RPC-only ; ferme D-W6-B2BSET-01 S14) + `get_b2b_settings_v1` (gate `settings.read`) + `update_b2b_settings_v1(p_patch)` (gate `settings.update`, patch partiel, validations P0001 : terms uniques, default ∈ available, overdue 1..365, buckets contigus first-min-0/last-max-null ; audit `b2b_settings.updated`) + REVOKE pair canonique S25 ; **aucune nouvelle permission** (réutilise `settings.{read,update}` S13) ; `aging_buckets` persistés ne pilotent PAS `view_ar_aging` (décision actée, refactor déféré). **Wave B (products)** : **BO-09** UnitsPanel write-mode — `useProductUnits`/`useSetProductUnits` sur les tables S27 + `set_product_units_v1` (REPLACE semantics, soft-delete vérifié E2E), `SAMPLE_ALT_UNITS` supprimé, gate `products.units.update` ; **BO-10** `CostingPanel` + `CorrectCostDialog` — KPI WAC/retail/marge + breakdown BOM `recipe_bom_full_v1` (S17) + correction `update_cost_price_v1` (S22, idempotency flavor 2, gate `inventory.cost_correction` — code ajouté au union `PermissionCode`, absent depuis S22, DEV-S39-B2-01). **Wave C (orders + B2B)** : **BO-04** `ProductPicker` réel dans `EditOrderItemsModal` (search nom/SKU, parents de variants exclus, orchestrateur S33 inchangé ; bug latent S33 corrigé — qty pending écrivait dans `diff.updates`, DEV-S39-C1-01) ; **BO-15 front** `B2BSettingsPage` persiste (banner « read-only preview » supprimé ; fix rules-of-hooks trouvé en revue, `71dd13e`, DEV-S39-C2-02). **3 migrations** NAME-block `20260623000010..012` + types regen. **Tests** : pgTAP `b2b_settings` 10/10 (ré-exécuté indépendamment par le reviewer) + smokes 13 nouveaux + sweeps domain/UI/supabase ✅, POS 415/418 et BO 451/453 (2 flakes pré-existants hors S39, 2/2 isolés, DEV-S39-D2-01) + typecheck 6/6. **E2E navigateur Playwright** (`tests/e2e/s39-bo-completion.spec.ts`, login partagé — rate-limit 3/min/IP) : B2B Settings persist post-reload / Units add-save-remove / Costing correction 90→91→90 **PASS avec vérifs DB post-run** ; T4 ProductPicker authored, skip (pas d'ordre éditable en seed, DEV-S39-D3-02). **Reviews** : 2-stage par wave, spec-reviewers ✅ ×5 (1 issue Medium rules-of-hooks corrigée), pattern-guardian **12/12**. Leçon Vitest : mock DATA objects en `vi.hoisted()` refs stables sinon boucle de rendu infinie → OOM (DEV-S39-B1-01). **Hors scope S40+** : wiring `aging_buckets`→`view_ar_aging`, stubs `purchase`/`history` ProductDetail, sections/modifiers consumers, PAT-01/02, POS-16/17, F-010..013/019..024, BO-08, BO-21, dé-flakage `variant-select-modal` + `journal-entries`. Deviations tracked in INDEX §3 (13 entries — 1 Medium fixée, le reste informational).
- **Session 38 reference:** Session 38 — PIN Lockout Hardening + Split-Bill étendu + premier E2E navigateur ✓ **merged 2026-06-11 via PR #70** (`d1fb183`, squash) from `swarm/session-38` (base `master` @ `4b235bd`, post-merge S37 PR #69). Spec : [`docs/workplan/specs/2026-06-11-session-38-spec.md`](docs/workplan/specs/archive/2026-06-11-session-38-spec.md), Plan : [`docs/workplan/plans/2026-06-11-session-38-plan.md`](docs/workplan/plans/archive/2026-06-11-session-38-plan.md), INDEX : [`docs/workplan/plans/2026-06-11-session-38-INDEX.md`](docs/workplan/plans/archive/2026-06-11-session-38-INDEX.md). Zéro nouvelle table. Source = INDEX S37 §9 « Deferred S38+ ». **SEC-06 (anti brute-force PIN sur RPCs in-arg)** : nouveau helper `_verify_pin_with_lockout` (politique 5 échecs/15 min alignée EF `auth-verify-pin`, P0004 `account_locked` distinct de P0003 invalid_pin) câblé via `CREATE OR REPLACE` (signatures inchangées) dans `sign_zreport_v2`/`close_fiscal_period_v1`/`create_manual_je_v1`/`approve_expense_v3`/`complete_order_with_payment_v11`. **Découverte clé DEV-S38-A-02** : PostgREST enveloppe chaque RPC dans 1 transaction → le `RAISE P0003` rollback le comptage in-RPC ; le gate lecture-seule `locked_until` (P0004) reste effectif, mais le **comptage persistant** passe par `record_pin_failure_v1` (service_role only, tx séparée) appelé par l'EF `process-payment` après un échec de PIN discount (filtre message `'Invalid manager PIN'` — bug `'invalid_pin'` du 1er jet corrigé en revue, `fd78f6c`). **SEC-07 (chemin manager-PIN EF)** : bucket durable **per-IP** `manager-pin-fail` (5/15 min) consommé sur `no_match` + audit `manager_pin.failed`, câblé dans `void-order`/`cancel-item`/`refund-order` ; PAS de comptage per-manager (lockerait tous les managers — DoS interne). **POS-15 (split-bill étendu)** : helpers domain purs TDD `splitEqualAmounts`/`validateCustomSplit` + step `mode_select` (3 tuiles items/equal/custom) + `CustomAmountsStep` dans `SplitPaymentFlow` ; zéro changement DB/EF/RPC (somme tenders = total, déjà accepté v11/v7) ; surfacing lockout au POS (`useVerifyManagerPin` lit body 403 via `error.context.json()` ; `classifyCheckoutError` case `account_locked` → copy FR — 2 bugs de code mort corrigés en revue). **6 migrations** block NAME `20260622000010..016`. **Tests** : pgTAP `pin_lockout` **12/12** + non-régression `order_discount_gate` 10/10 via cloud MCP + domain 646 (splitModes 9/9 + retryClassifier 22/22) + POS 417(+1 skip) + UI 338 + typecheck 6/6. Types regen committé. **E2E navigateur (Playwright, 1er du projet)** : login PIN (échec → message + succès reset) → panier → checkout → split `mode_select` → Equal 2 payeurs (Rp 35 000 ×2) → paiement par payeur → finalize ; `process-payment` non émis (no_open_shift en session fraîche — limite env, pas un défaut code ; chemin multi-tender DB couvert pgTAP). **Reviews** : pattern-guardian 14/14 (0 HIGH) ; spec-reviewer Wave B ✅ ; spec-reviewer Waves A+C a trouvé 3 bugs code-mort (2 HIGH + 1 MEDIUM) **tous corrigés** (`fd78f6c`). **Hors scope S39+** : wrapper EF par RPC pour comptage PIN BO complet (cf. INDEX §8 reformulation critère DEV-S38-A-02), PAT-01/02 auth BO setSession, POS-16 LAN cart mirror, POS-17 course timing, split par siège, gate CI E2E. Deviations tracked in INDEX §9 (8 entries — 1 Medium DEV-S38-A-02, le reste informational).
- **Session 37 reference:** Session 37 — Fraud Hardening & Correctness Close-out ✓ **merged 2026-06-11 via PR #69** (`73bd4bf`, squash) from `swarm/session-37` (base `master` @ `06b8283`, post-merge S36 PR #68). Spec : [`docs/workplan/specs/2026-06-11-session-37-spec.md`](docs/workplan/specs/archive/2026-06-11-session-37-spec.md), Plan : [`docs/workplan/plans/2026-06-11-session-37-plan.md`](docs/workplan/plans/archive/2026-06-11-session-37-plan.md), INDEX : [`docs/workplan/plans/2026-06-11-session-37-INDEX.md`](docs/workplan/plans/archive/2026-06-11-session-37-INDEX.md). Zero new feature — money-flow fraud hardening + POS/BO correctness, source = post-S36 6-agent audit. **Wave A (DB+EF)** : **SEC-01/02/05** `complete_order_with_payment_v10→v11` (discount gate `sales.discount` réutilisée D4 + PIN in-arg validé, `unit_price` reconcilié server-side vs `get_customer_product_price` — pas retail brut, DEV-S37-A1-02 ; gift lines liées à `p_promotions`, DEV-S37-A1-01 ; audits `order.discount_applied`/`order.price_overridden` ; `loyalty_balance_after` additif) + `pay_existing_order_v6→v7` (gate **permission-only** sur le chemin direct-RPC pickup — aucun RPC du projet ne lit `request.headers`, DEV-S37-A2-01 Medium ; jsonb envelope pour le vrai total) ; **SEC-04** `loyalty_transactions` append-only (REVOKE writes) ; **DB-02** `process-payment` rate-limit durable + EF v6 deployed (appelle v11, forward discounts, relay `x-manager-pin` ; le POS transporte le PIN via `managerPinHolder` module-scoped, DEV-S37-A1-03) ; **BO-01** `sign_zreport_v1→v2` PIN validé in-RPC (D3). **Wave B (POS)** : POS-01 vrai total pickup (v7) ; POS-02 customer display post-promo ; POS-04 vrai solde loyalty sur reçu ; POS-06 void post-kitchen via EF server (pré-run `e3cadee`) ; POS-05 `DEFAULT_TAX_RATE` centralisé `@breakery/domain` (7 sites, grep 0). **Wave C (BO + PII cutover)** : BO-02 query keys `['order-detail', id]` alignés ; BO-03 toast erreur ; BO-05 per-row PDF pending ; BO-12 back URL ; **SEC-03/DB-03/DB-06 hard cutover customers** — RPCs v2 `search/get/create_customer_v2` (embed `category JSONB`, D6) + corrective `_019` default category server-side (DEV-S37-C5-02) + `get_pos_b2b_debts_v1→v2` vraie sémantique panel (tous types d'ordres dû > 0, lookback, champs crédit — prémisse v1 fausse, DEV-S37-C5-03) + 4+1 sites POS câblés (`useCustomerSearch`/`useCreateCustomer`/`Pos.tsx` inline/`useRestoreHeldOrder`/`useOutstandingDebts`) + build POS, **puis gate `_018` appliqué EN DERNIER** (policy `auth_read` exige `customers.read` ; grant MANAGER/ADMIN/SUPER_ADMIN ; DEV-S37-C5-01). **Wave D** : TEST-02 PR-time pgTAP gate (`ci.yml`) ; TEST-01 Vitest live suites `skipIf`-guarded ; PAT-05/06/17/18 CLAUDE.md 4 stale facts corrigés. **11 migrations** block NAME `20260621000010..020` (`_016` skipped — D4 ; `_018` appliquée chronologiquement après `_020`, order-independent). **Tests** : pgTAP `order_discount_gate` 10/10 + `pay_existing_discount_gate` 5/5 + `loyalty_transactions_append_only` 5/5 + `sign_zreport_pin` 6/6 + `customers_pii_gate` **7/7 post-gate** via cloud MCP + full sweeps domain 57 files / UI 54 / POS 102 (412 tests +1 skip) / BO 134 (+1 skip) + typecheck 6/6 PASS. Types regen ×2 committed. **Hors scope S38+** : PAT-01/02 auth BO setSession refactor, POS-15 split-bill, POS-16 LAN cart mirror, POS-17 course timing, F-010..013/019..024, BO-04/08/09/10/15/21, SEC-06/07 PIN lockout, TEST-05/07, print-bridge deployment, dynamic VAT `useTaxRate`. Deviations tracked in INDEX §10 (8 entries — 1 Medium DEV-S37-A2-01 anticipée par le plan, le reste informational).
- **Session 36 reference:** Session 36 — POS Correctness & Security Close-out ✓ **executed** on `swarm/session-36` (based on `feat/pos-redesign @ 7dbb10a`, NOT `master` — DEV-S36-PLAN-01, to avoid an `App.tsx` conflict with the unmerged reload-fix PR #66). Spec : [`docs/workplan/specs/2026-06-04-session-36-spec.md`](docs/workplan/specs/archive/2026-06-04-session-36-spec.md), Plan : [`docs/workplan/plans/2026-06-04-session-36-plan.md`](docs/workplan/plans/archive/2026-06-04-session-36-plan.md), INDEX : [`docs/workplan/plans/2026-06-04-session-36-INDEX.md`](docs/workplan/plans/archive/2026-06-04-session-36-INDEX.md). Zero new feature — correctness/security close-out. **Wave A** : **F-008** explicit canonical S25 REVOKE pair on `send_items_to_kitchen(UUID[])` (Task 0 found anon already revoked by the S20 sweep → defense-in-depth + regression guard, DEV-S36-A-02 ; pgTAP 2/2) ; kiosk-issue-jwt verified already PIN-in-header compliant, no change (DEV-S36-A-01). **Wave B** : **F-002** `orderTypeLabel` domain helper (4-key DB-enum union — domain `OrderType` is only 3-member, DEV-S36-B-01) + swept the ghost `take_away`/`takeaway` → real `take_out` across 3 sites (OrderHistoryPanel consumes the helper ; OrderQueueTicker + CurrentOrderCard keep bespoke customer "Pickup"/table copy, DEV-S36-B-03) ; **F-021** dropped `'postgres_changes' as never` in `useDisplayRealtime` (no regen, DEV-S36-B-02). **Wave C** : POS idle → `authStore.lock()` session-preserving (ratified reversal of the S35 "manual lock only" decision, DEV-S36-C-01 ; `IdleTimeoutMount` extracted) ; held-restore re-fetches the customer object for the badge (DEV-S35-C-05) ; VKP overlay portaled into the active Radix Dialog so it's not aria-hidden (DEV-S35-E3-01 ; test asserts structural containment, DEV-S36-C-02). **1 migration** `20260620000017_revoke_send_items_to_kitchen_anon` (REVOKE/GRANT only — no types regen). **Tests** : pgTAP `send_items_anon_revoke` 2/2 via cloud MCP + new smokes (order-type-label 1, idle-lock 2, restore-customer-refetch 2, vkp-dialog-a11y 2) + full sweeps domain 635 / UI 338 / POS 405 (+1 skip) + typecheck 6/6 PASS. **Reviewed** : spec ✅, code-quality ✅ (follow-ups folded — shared `CUSTOMER_SELECT`, label-scope doc), pattern-guardian 14/14. 8 commits (`333f547`..`d69e031`). **Merged 2026-06-11 via PR #68** (`a8caf0e`). **Hors scope S37+** : F-010..013/019..024 features, polish tail F-016..024, LAN cross-device cart mirror, print-bridge deployment. Deviations tracked in INDEX §9 (8 entries, all informational).
- **Pre-S36 base:** `master` clean @ `0086017` (post-merge S35 PR #62). The S35 INDEX §Hors-scope items idle→lock / VKP a11y (DEV-S35-E3-01) / customer re-fetch on held restore (DEV-S35-C-05) were **closed in S36** above ; remaining backlog : LAN cross-device cart mirror, F-010..013/019..024. Note: the reload-fix PR #66 (`feat/pos-redesign`) was the base of `swarm/session-36` — **merged** (`a6da9a5`).
- **Session 35 reference:** Session 35 — POS Service Polish ✓ merged 2026-06-04 via PR #62 (`0086017`) on `swarm/session-35`. Spec : [`docs/workplan/specs/2026-05-29-session-35-spec.md`](docs/workplan/specs/archive/2026-05-29-session-35-spec.md), Plan : [`docs/workplan/plans/2026-05-29-session-35-plan.md`](docs/workplan/plans/archive/2026-05-29-session-35-plan.md), INDEX : [`docs/workplan/plans/2026-05-29-session-35-INDEX.md`](docs/workplan/plans/archive/2026-05-29-session-35-INDEX.md). Closes 5 *Major* POS audit findings (subagent-driven, TDD, 2-stage review per task) : **F-014** Lock Terminal (`authStore.isLocked`/`lock`/`unlock` session-preserving + `<TerminalLockedOverlay>` PIN re-auth + `Pos.tsx` wiring) ; **F-009** POS Settings Printing tab (`usePosSettingsStore` Zustand persist `pos:settings` ; `printService` reads URL at call-time → resolves **F-015** ; `SuccessModal` auto-print/auto-drawer toggles ; `<PrintingSettingsTab>`) ; **F-003** Held orders DB-backed (Option A `orders.is_held`+`notes` ; 3 SECURITY DEFINER RPCs `hold_order_v1`/`restore_held_order_v1`/`discard_held_order_v1` + dedicated `held_order_idempotency_keys` ; gates `pos.sale.create` hold/restore + `orders.void` discard ; restore+discard **DELETE** the draft to avoid the `status→voided` JE trigger ; POS hooks query/hold/restore/discard/realtime + UI rewire + **localStorage `heldOrdersStore` retired**) ; **F-007** Customer Display live cart mirror (`useCartBroadcast`/`useCartBroadcastReceiver`/`<CDActiveCartView>` via `BroadcastChannel('breakery-cart')`) ; **F-005** Virtual keypad (`<QwertyLayout>`+`<VirtualKeypadProvider>` in `@breakery/ui`, focus-driven input+textarea, `inputmode=none` ; mounted around `/pos` + 3 reason/search inputs opt-in). **7 migrations** `20260620000010..016` (incl. correctives `_015` relax session_id for held + `_016` fix held RPCs default privileges `FROM PUBLIC`). **Tests** : pgTAP `held_orders` **16/16** via cloud MCP + POS/UI smokes (lock 6, settings ~20, display ~22, held ~35, keypad 8) + typecheck POS/UI/supabase 3/3 PASS. **Hors scope S36+** : LAN cross-device cart mirror, idle→lock rewire, VKP a11y inside Radix modals (DEV-S35-E3-01), customer re-fetch on held restore (DEV-S35-C-05), F-010..013/019..024. Deviations tracked in INDEX §4 (10 entries — DEV-S35-PLAN-01 spec's S34 draft-RPC myth ; DEV-S35-C-01..06 held-orders schema/perm/trigger calls ; DEV-S35-E3-01 VKP-in-modal a11y ; all informational/low).
- **Session 35a reference:** POS audit fixes + PaymentTerminal refactor ✓ merged 2026-06-03 via PR #61 (`2de5eac`) on `swarm/session-35a`. Realtime StrictMode channel-UUID-in-effect fixes (promotions/tables/tablet) ; receipt real payment method ; cash-drawer warning toast (mountedRef guard) ; print URL de-hardcoded `VITE_PRINT_SERVER_URL` ; single canonical kitchen print path ; typed LAN client ; `PaymentTerminal` decomposed 470→203 lines (5 components + `usePaymentFlowLogic`) ; `cash-drawer-error-toast` de-flaked (static import hoist). 26 commits, front-only.
- **Session 34 reference:** Station Ticket Printing ✓ merged 2026-06-02 via PR #54 + #56 (`ae4d93e`) on `swarm/session-34` → `feat/pos-station-printing`. Spec : [`docs/workplan/specs/2026-06-01-session-34-station-printing-spec.md`](docs/workplan/specs/archive/2026-06-01-session-34-station-printing-spec.md), Plan : [`docs/workplan/plans/2026-06-01-session-34-station-printing-plan.md`](docs/workplan/plans/archive/2026-06-01-session-34-station-printing-plan.md), INDEX : [`docs/workplan/plans/2026-06-01-session-34-station-printing-INDEX.md`](docs/workplan/plans/archive/2026-06-01-session-34-station-printing-INDEX.md).
- **Session 34 detail:** Station ticket printing for the POS — prep tickets routed per product category to barista/kitchen/bakery thermal printers; bill printing (pre-payment addition) to cashier/waiter; auto-fire of unprinted prep items on checkout; receipt routing to cashier printer. Architecture mock-first (`VITE_PRINT_MOCK=1` in CI; external print-bridge `localhost:3001` at runtime — **DEV-S34-W0-02, bridge deployment deferred S35**). 5 printer roles (`barista`, `kitchen`, `bakery` prep + `cashier`, `waiter` document) resolved via `lan_devices.capabilities->>'station'` from new `useStationPrinters`. Domain: `groupItemsByStation(items, stationByProductId)` (pure, IO-free). POS: `printService.ts` (`printStationTicket` + `printReceipt` + mock buffer), `useFireToStations` mutation (concurrent per-station fire, markLocked+markPrinted on success), `usePrintBill` (whole-order bill, re-printable), `SendToKitchenButton` rewired, `PrintBillButton` new, `PaymentTerminal` auto-fire + SuccessModal receipt→cashier. cartStore: `printedItemIds` + `markPrinted` + `unprintedItems()`. **1 migration** `20260601043059` (data-only: UPDATE categories dispatch_station — Beverage→barista, Sandwiches→kitchen, Pastry/Bread→bakery). **Tests** : pgTAP `category_station_remap` 7/7 PASS via cloud MCP + 5 POS smoke files 7/7 PASS + `pnpm --filter @breakery/app-pos typecheck` PASS. **Business pivot** (DEV-S34-W0-01): KDS on-screen station displays deferred S35 — replaced with hardware printers. **Hors scope S35+** : bridge deployment, KDS screens, receipt enhancements (method/tax/drawer), openCashDrawer error surfacing, cashierPrinter cold-query pre-warm, S33 backlog (BO+POS smokes, refund from BO, edit other fields, mobile responsive, CF drill, UnifiedReportFilters, compare toggle, hub KPI/favorites, 6 Soon cards). Deviations tracked in INDEX §9 (4 entries, all informational).
- **Session 33 reference:** Closes 4 trous post-S32 sur `/backoffice/orders` (observe → filter → control) : RPC `get_orders_list_v1→v2` server-side filters (refund_status / hour / terminal_id via JOIN `pos_sessions` post col add), realtime channel `useOrdersRealtime` (postgres_changes INSERT+UPDATE, StrictMode-safe via useId), `useVoidOrder` BO + `VoidOrderModal` (body PIN per DEV-S33-PRE-02 — void-order EF non-hardened S25), edit items sur open/draft/pending_payment orders via 3 RPCs atomiques (`add_order_item_v1`, `update_order_item_qty_v1`, `remove_order_item_v1`) + helper interne `_recalc_order_totals` + idempotency table dédiée `order_edit_idempotency_keys`. Orchestrateur BO `useEditOrderItems` séquence removes→updates→adds avec idempotency keys propres par RPC. `EditOrderItemsModal` 2-col (product picker stub V1 + cart preview live). Bonus : `pos_sessions.terminal_id UUID NULL REFERENCES lan_devices(id)` + POS `useOpenShift` bumpé (INSERT direct, pas d'RPC) + OpenShiftModal terminal selector + localStorage `pos:last_terminal_id` pre-select. **15 migrations** block `20260618000010..023` + cloud-clock corrective `20260529200749` (13 obligatoires + 2 correctives). 2 nouvelles perms (`orders.edit_open` + `orders.void`). **Tests ~29 PASS** : pgTAP `orders_list_v2` 10/10 + `order_edit_items` 12/12 + `pos_session_terminal` 3/3 via cloud MCP + BO unit `useOrdersList` 4/4 + typecheck 3/3 packages. **Correctives in-flight** : (1) CTE scope bug — original v2 référençait CTE `filtered` dans 2 SELECTs séparés (42P01) → fusionné en SELECT INTO multi-target ; (2) status enum check `'open'` n'existe pas dans `order_status` enum (actual: draft, paid, voided, pending_payment, completed, b2b_pending) → fixé en `('draft', 'pending_payment')` + `products.retail_price` (pas `.price`). **Hors scope S34+** : BO + POS smoke test suites (DEV-S33-4-SKIP-01), refund actions from BO, void-order EF header-PIN refactor, ProductPicker pour EditOrderItemsModal, edit autres champs open orders (customer/notes/table), mobile responsive, NOT NULL promotion `pos_sessions.terminal_id` post-backfill, concurrent edit conflict detection, realtime merge in-place, CF account drill (DEV-S32-1.D-01), UnifiedReportFilters extra dims, compare toggle S30 reports, hub mini-KPI + favorites, 6 Soon cards restantes. Deviations tracked in INDEX §9 (9 entries, 1 medium DEV-S33-4.2-01 fixed via corrective `_023`).
- **Standalone fix PRs (2026-05-31 → 06-04, between S34 and S35):** **PR #53** (`ff4d659`, security fraud-guard) — closes 6 fraud/leak gaps + lays gap-4 foundation ; **4 migrations** `20260619000040` (`create_pos_customer_rpcs`), `20260619000042` (`create_pos_b2b_debts_rpc`), `20260619000043` (`gate_customers_read`). **PR #59** (`e645d31`) + **PR #60** (`0646352`) — backoffice integrity audit fixes (Critical + High + 7/9 Medium ; M7+M9 display_stock seed + keyset cursor + pivot reconciliation), driven by [`docs/audit/2026-06-01-backoffice-integrity-audit.md`](docs/audit/archive/2026-06-01-backoffice-integrity-audit.md). **PR #57** (`70c5cf1`) — document project agents & skills roster.
- **Backoffice menu reorg (2026-05-27):** Refactor standalone livré sur `feat/bo-menu-reorg` (post-S32) — 3 groupes (Operations/Management/Admin) → 7 groupes (Operations/Sales/Purchase/Stock/Finance/Reports/Settings) avec 9 sous-sections visuelles internes. 3 drops (POS/KDS/New user) + 8 renames (Product Categories, Customer Categories, B2B Credit Settings, Cash Closing, Live Movements, Stock Movement History, RBAC Editor, Permissions Matrix). Aucune URL ne change. **Wave 8 addendum** : les 9 sous-groupes nommés (Finance > Expenses/Accounting ; Reports > 5 ; Settings > Devices/Users & Access) sont cliquables/dépliables, défaut all-collapsed, persistance navigateur via localStorage `bo:sidebar:subgroups`. Spec : [`docs/workplan/specs/2026-05-27-backoffice-menu-reorg-spec.md`](docs/workplan/specs/archive/2026-05-27-backoffice-menu-reorg-spec.md). Plan : [`docs/workplan/plans/2026-05-27-backoffice-menu-reorg-plan.md`](docs/workplan/plans/archive/2026-05-27-backoffice-menu-reorg-plan.md). Addendum collapsible : [`docs/workplan/plans/2026-05-27-backoffice-menu-reorg-addendum-collapsible.md`](docs/workplan/plans/archive/2026-05-27-backoffice-menu-reorg-addendum-collapsible.md).
- **Session 32 reference:** Session 32 — Reports Vague C close-out drill-down + /backoffice/orders list ✓ merged 2026-05-27 via PR #40 (commit `780e12e`) on `swarm/session-32` (~24 commits, 4 waves, 4 migrations `20260617000010..014`, INDEX: [`docs/workplan/plans/2026-05-26-session-32-INDEX.md`](docs/workplan/plans/archive/2026-05-26-session-32-INDEX.md), spec: [`docs/workplan/specs/2026-05-26-session-32-spec.md`](docs/workplan/specs/archive/2026-05-26-session-32-spec.md), plan: [`docs/workplan/plans/2026-05-26-session-32-plan.md`](docs/workplan/plans/archive/2026-05-26-session-32-plan.md)). Base `master` @ `c74e295` (post-merge S31 PR #39). Ferme les 7 reports "terminal documentés" de S31 — 4 accounting (P&L, BS, CF*, PB1) + StockMovementHistory product drill + PaymentByMethod + SalesByHour. **2 bumps DB additifs** (`get_profit_loss_v1` + `get_balance_sheet_v1` exposent `account_id` UUID en plus du `code` 3-4 digit — CF déféré S33+ DEV-S32-1.D-01 car la méthode indirecte n'a pas de lignes per-account natives). **1 nouvelle RPC** `get_orders_list_v1(p_start DATE, p_end DATE, p_filters JSONB, p_limit INT, p_cursor TEXT)` SECURITY DEFINER cursor-paginée avec filtres JSONB unique (10 axes : status / order_type / customer_id / served_by / payment_method / customer_type / total_min/max / refund_status* / hour* — \* post-fetch client-side V1 ; `terminal_id` dropped V1 car col absente DEV-S32-1.A-01). REVOKE pair S25 canonique. **1 nouveau hook réutilisable** `useAccountIdByCode(code)` dans `features/accounting/hooks/` (24h cache, résout COA code → UUID client-side, futur consumer pour cards single-account KPI). **`buildDrilldownUrl` étendu** avec entity `'order_list'` filter-only (id ignoré, URL = base + filter ; 18/18 unit PASS, +5 nouveaux). **1 nouvelle page** `/backoffice/orders` audit-grade (URL state = source of truth, 9 colonnes, infinite scroll cursor-based, barre filtres 9 contrôles + chips actifs, sidebar entry Management group icon ShoppingBag gate `orders.read`). **GeneralLedgerPage** accepte `?account_id=&start=&end=` URL params (Wave 3.A). **7 drill-down wirings** : P&L code → GL ; BS — nouvelle "Per-account detail" table sous le 3-col A=L+E ; PB1 payable KPI → GL acc 2110 ; StockMovementHistory product → ProductDetail ; PaymentByMethod → /orders?payment_method ; SalesByHour — nouvelle "Per-hour detail" table sous le BarChart → /orders?hour. **Schema discoveries vs spec** : `orders.terminal_id` n'existe pas (DEV-S32-1.A-01) ; `customers.name` (pas `full_name`, DEV-S32-1.A-02) ; `refunds.total` (pas `amount`, DEV-S32-1.A-03). **Tests 40 PASS** : 11 pgTAP (2 `accounting_account_id_exposed` + 9 `orders_list_v1`) via cloud MCP + 18 unit `buildDrilldownUrl` + 2 unit `useOrdersList` + 3 smoke `GeneralLedgerPage` + 3 smoke `OrdersListPage` + 6 smoke drill wirings + 5 S31 non-régression. `pnpm typecheck` 6/6 PASS. **Hors scope S33+** : CF account drill (refactor RPC indirect→direct method), `get_orders_list_v1` v2 server-side filters (refund_status, hour, terminal_id post-schema-bump), OrdersListPage refund/void actions + "+ New Order" button + mobile responsive, UnifiedReportFilters extra dims, compare toggle reports S30, hub mini-KPI + favorites, 6 Soon cards restantes. Deviations tracked in INDEX §10 (10 entries, all informational).
- **Session 31 reference:** Session 31 — Reports Drill-Down + 3 detail pages ✓ merged 2026-05-26 via PR #39 (commit `c74e295`) on `swarm/session-31` (~17 commits, 4 waves, 1 migration `20260616000010`, INDEX: [`docs/workplan/plans/2026-05-22-session-31-INDEX.md`](docs/workplan/plans/archive/2026-05-22-session-31-INDEX.md), spec: [`docs/workplan/specs/2026-05-22-session-31-spec.md`](docs/workplan/specs/archive/2026-05-22-session-31-spec.md), plan: [`docs/workplan/plans/2026-05-22-session-31-plan.md`](docs/workplan/plans/archive/2026-05-22-session-31-plan.md)). Base `master` @ `60a1ff3` (post-merge S30 PR #38). **Premier chantier Vague C** : drill-down navigation transverse sur les 17 reports BO + 3 nouvelles detail pages minimales read-only (`customers/:id`, `orders/:id`, `inventory/recipes/:productId`). Composant entity-aware `<DrilldownLink entity={...} id={...} filter={...} />` consume helper pur `buildDrilldownUrl(entity, id, filter)` (13/13 unit PASS). 3 detail pages utilisent direct SELECT PostgREST + reuse RPC `recipe_bom_full_v1` (S17) — pas de nouvelle RPC. 1 migration `_010` seed permission `orders.read` (MANAGER+/ADMIN+/SUPER_ADMIN). **Wiring** : 13/17 reports avec drill-down actif (sales/stock/production groupes 3.A/3.B/3.C) ; 4 reports accounting (P&L, BS, CF, PB1) + PaymentByMethod + SalesByHour + Pb1 + RecipeCostTimeline = **terminal documentés** car RPCs retournent `code` 3-4 digit au lieu de UUID (GL S26b attend UUID) — RPC bumps deferred S32+. **Schema discoveries vs spec** : `customer_type` enum `retail|b2b` (pas walk_in/account) ; `orders.total` + `served_by` (pas total_amount/created_by) ; `order_items.name_snapshot` + `modifiers` (pas product_name/modifiers_json) ; table `refunds` (pas order_refunds) ; "Recipe" est M2M product↔ingredient (1 row par ingrédient) — route keyed sur `:productId` (output product) pas `:id` ; `recipe_bom_full_v1` retourne TABLE flat (pas tree). **Tests ~27 PASS** : 13 unit `buildDrilldownUrl` + 3 component smoke + 6 detail pages (CustomerDetail 2/2 + OrderDetail 2/2 + RecipeDetail 2/2) + 5 wiring sample smoke + 2 pgTAP `orders_read_perm`. `pnpm typecheck` 6/6 PASS. **12 tests pré-S31 retroactively patched** (script Python idempotent) pour wrap `<MemoryRouter>` — sans ça `<Link>` du DrilldownLink crashe sur `useContext(...)` car les tests existants rendent les pages directement. **Hors scope S32+** : `/backoffice/orders` list page filtrable (débloquerait PaymentByMethod + SalesByHour drill), RPC bumps accounting (P&L/BS/CF/StockMovements pour exposer UUIDs), actions sur detail pages (refund/void/edit), mobile responsive, UnifiedReportFilters extra dims, compare toggle sur reports S30, hub mini-KPI bar + favorites/pinning, 6 Soon cards restantes. Deviations tracked in INDEX §10 (9 entries, all informational).
- **Session 30 reference:** Session 30 — Vague B : 5 bakery reports ✓ merged 2026-05-25 via PR #38 (commit `60a1ff3`) on `swarm/session-30` (15 commits, 10 migrations block `20260615000010..019` + `20260524231049..124`, INDEX: [`docs/workplan/plans/2026-05-24-session-30-INDEX.md`](docs/workplan/plans/archive/2026-05-24-session-30-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-30-spec.md`](docs/workplan/specs/archive/2026-05-24-session-30-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-30-plan.md`](docs/workplan/plans/archive/2026-05-24-session-30-plan.md)). Base `master` @ `d14cf9b` (post-merge S29 PR #37). Livre 5 nouveaux reports métier promus depuis "Soon" cards du hub : **Wastage & Spoilage** (manual_waste + auto spoilage stock_lots.expired aggregated by product + lines top 500), **Payment by Method** (split cash/card/qris/edc/transfer/store_credit + by_day pivoted + share_pct), **VAT/PB1 Report** monthly NON-PKP (reuses `current_pb1_rate()` + `calculate_pb1_payable_v1` S26 helpers + balance on account `2110` PB1 Payable), **Stock Movement history** cursor-paginé (filters product_id + movement_type, infinite query BO pattern S13 AuditLog), **Perishable Turnover** (per-product velocity_score 1-5 bucket basé sur avg_days_in_stock vs shelf_life_p50, waste_pct = expired/(consumed+expired)*100). Réutilise infra S29 (ExportButtons générique, EF generate-pdf 12→17 templates, helpers buildCsv/downloadCsv/previousPeriod). **5 RPCs SECURITY DEFINER + REVOKE pairs canoniques S25** : `get_wastage_report_v1(text, text)`, `get_payments_by_method_v1(text, text)`, `get_pb1_report_v1(int, int)`, `get_stock_movements_v1(text, text, uuid, text, int, timestamptz)`, `get_perishable_turnover_v1(text, text)`. Aucune nouvelle perm seedée — réutilise `reports.inventory.read` (3 reports) + `reports.financial.read` (2 reports). **EF generate-pdf v2** : registry extended 12→17 templates (5 nouveaux dans `_shared/pdf-templates/` : `wastage.ts`, `payment_by_method.ts`, `pb1.ts`, `stock_movements.ts`, `perishable_turnover.ts`). **BO** : 5 hooks (`useWastageReport`, `usePaymentsByMethod`, `usePb1Report` month/year params, `useStockMovementsReport` infinite cursor pattern, `usePerishableTurnover`) + 5 pages avec ExportButtons (Wastage/PaymentByMethod/Pb1/StockMovementHistory CSV-only/PerishableTurnover) + 5 routes PermissionGate + 5 sidebar entries indent 1 sous Reports + ReportsIndexPage hub passes 13→18 active cards (4 Soon promus + 1 nouveau Perishable Turnover card). **Tests** : pgTAP `bakery_reports.test.sql` **15/15 PASS** via cloud MCP (T1-T15 : perm gate + shape + filter + clamp + score range) + regression S29 zreports 14/14 + S28 expense_governance 18/18 PASS + BO smoke 5/5 PASS + typecheck 6/6 PASS. **Hors scope S31+** : Vague C (drill-down navigation `<DrilldownLink>`, UnifiedReportFilters extra dims category/terminal/customer, compare toggle sur ces 5 reports, mobile responsive, hub mini-KPI bar, favorites/pinning, 6 Soon cards restantes : Daily Sales/Purchase×3/Staff Performance/Production Report/Production Efficiency/Price Changes/Permission Change Log). Backlog Vague D+ : Scheduled email TASK-14-008, Unusual Transactions TASK-14-013, Custom report builder TASK-14-007. Deviations tracked in INDEX §12 (8 entries, all informational).
- **Session 29 reference:** Session 29 — Reports Export + Z-Report PDF (Vague A) ✓ merged 2026-05-24 via PR #37 (`d14cf9b`) on `swarm/session-29` (30 commits, 14 migrations block `20260606000010..023`, INDEX: [`docs/workplan/plans/2026-05-24-session-29-INDEX.md`](docs/workplan/plans/archive/2026-05-24-session-29-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-29-spec.md`](docs/workplan/specs/archive/2026-05-24-session-29-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-29-plan.md`](docs/workplan/plans/archive/2026-05-24-session-29-plan.md)). Base `master` @ `66f77d6` (post-merge S28 PR #36). Closes **TASK-14-005** (compare toggle vs période précédente — DONE) + **TASK-12-002** (Z-Report PDF signable — DONE) + gap 14-3 (CSV/PDF uniforme sur toutes les pages reports — DONE). Architecture **flow 2-temps Z-Report** : `close_shift_v2` insère draft `z_reports` row avec snapshot JSONB figé (orders + payments + refunds + expenses du shift) → EF `generate-zreport-pdf` génère PDF async via pdf-lib + upload bucket `zreports/` 7 ans (conformité Indonésie, service_role) → manager signe depuis BO via `<SignZReportModal>` (PIN `x-manager-pin` header S25 pattern) + `sign_zreport_v1` idempotency replay. Flow non-bloquant côté POS : si PDF échoue, draft row reste en DB pour retry BO. **3 RPCs** : `sign_zreport_v1(p_zreport_id)` SECURITY DEFINER gate `zreports.sign` + audit_log `zreport.signed` + idempotent replay ; `void_zreport_v1(p_zreport_id, p_reason)` gate `zreports.sign` + reason ≥ 10 chars ; `get_zreport_snapshot_v1(p_zreport_id)` gate `zreports.read` ; `close_shift_v2` bump (non-blocking pdf EF call via `pg_net`). **2 EFs** : `generate-zreport-pdf` (idempotent `x-idempotency-key` obligatoire, S25 flavor 1, 7-yr bucket) ; `generate-pdf` 12 templates (pnl/bs/cf/tb/recipes/inventory/sales/opname/expenses/zreport/customers/audit) rate-limit 30/min durable, `reports-exports/` TTL 30d. **Domain helpers** : `buildCsv<T>(rows, columns, opts?)` TDD 9/9 (RFC 4180 + UTF-8 BOM + id-ID locale IDR) ; `previousPeriod(range) → DateRange` + `formatDelta(curr, prev) → string` TDD 9/9 (calendar-aware month vs n-day fallback). **BO** : nouvelle page `ZReportsListPage` + 5 components (`SignZReportModal` PIN 6 digits header + `VoidZReportModal` textarea + `ZReportRow` + `ZReportStatusBadge` + `ZReportPreviewDrawer`) + 6 hooks (`useZReports`, `useSignZReport`, `useVoidZReport`, `useGenerateZReportPdf`, `useExportCsv`, `useGeneratePdf`) + `<ExportButtons>` générique câblé 13 pages + `<DateRangePickerWithCompare>` + `<DeltaPct>` câblés 5 reports (P&L, BS, CF, SalesByHour, SalesByCategory). **POS** : `useCloseShift` bumped v1→v2 (EF chain non-blocking). **Permissions seedées (3)** : `zreports.{read, sign, void}`. **Tests 42/42 PASS** : pgTAP `zreports.test.sql` **14/14 PASS** via cloud MCP + BO smoke ~30 PASS + POS smoke ~5 PASS + domain unit 18/18 PASS + Vitest live EF **12 cas** authored env-gated (3 fichiers : `generate-pdf.test.ts` GP1-GP5, `generate-zreport-pdf.test.ts` ZP1-ZP4, `sign-zreport.test.ts` SZ1-SZ3). `pnpm typecheck` 6/6 PASS. **Hors scope S30+** : Vague B (5 nouveaux reports bakery), Vague C (drill-down + UnifiedReportFilters extra dims + mobile responsive), `pg_net` non-blocking EF call in close_shift_v2 (currently synchronous), B2B invoice PDF (TASK-09-001 deferred). Deviations tracked in INDEX §13 (22 entries, 1 medium DEV-S29-1.D-03 fixed via corrective, 21 informational).
- **Session 28 reference:** Session 28 — Expense Governance ✓ merged 2026-05-24 via PR #36 (`66f77d6`) on `swarm/session-28` (31 commits, 14 migrations + 2 correctives = block `20260524111854..123026`, INDEX: [`docs/workplan/plans/2026-05-24-session-28-INDEX.md`](docs/workplan/plans/archive/2026-05-24-session-28-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-28-spec.md`](docs/workplan/specs/archive/2026-05-24-session-28-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-28-plan.md`](docs/workplan/plans/archive/2026-05-24-session-28-plan.md)). Base `swarm/session-27c` @ `0f1ad11` (S27c not yet merged — PR sequence S27c → S28). Ferme **TASK-11-001** (Workflow approbation multi-niveau — DONE complet) + gaps 11-1 (SOD), 11-2 (multi-step chain), 11-3 (cash sync), 11-4 (settings page). Architecture **snapshot-at-submit** retenue : on fige les règles d'approbation au moment du submit dans `expenses.required_approval_steps_snapshot JSONB` — pattern emprunté à `complete_order_v9` (tax_rate snapshot) + `recipe_versions.snapshot` (S15+S17). Un changement de seuil admin n'invalide pas une expense en cours de chaîne. **2 nouvelles tables** : `expense_approval_thresholds` (configurable per-category, 3 seeded defaults : [0-100k auto-approve], [100k-1M 1-step MANAGER], [1M-9.9G 2-step MANAGER + ADMIN/SUPER_ADMIN]) ; `expense_approvals` append-only audit (UNIQUE(expense_id, approver_user_id) enforce SOD — un user ne peut pas approuver 2 steps du même expense). **3 nouvelles cols sur `expenses`** : `required_approval_steps_snapshot JSONB NULL` (legacy NULL = workflow v1 fallback), `current_approval_step SMALLINT NOT NULL DEFAULT 0`, `auto_approved BOOLEAN NOT NULL DEFAULT false`. **5 RPCs** : `submit_expense_v2(p_expense_id, p_idempotency_key)` (bump v1, threshold resolution `ORDER BY category_id NULLS LAST LIMIT 1` + snapshot freeze + auto-approve path) ; `approve_expense_v2(p_expense_id)` (bump v1, SOD block 1 `created_by != caller` + SOD block 2 UNIQUE expense_approvals approver + multi-step chain + legacy NULL fallback 1-step + JE emit on final step) ; `set_expense_threshold_v1(p_threshold_id, p_category_id, p_amount_min, p_amount_max, p_steps)` (UPSERT admin-gated avec step shape + range + overlap validation) ; `delete_expense_threshold_v1(p_threshold_id)` ; trigger function `sync_cash_expense_to_session()` (AFTER UPDATE OF status ON expenses WHEN paid+cash → `pos_sessions.cash_out_total += amount + vat_amount` ; no-block fallback `audit_log expense.cash_paid_no_session` si pas de session open). **1 helper interne** : `_emit_expense_je(uuid)` extrait de `approve_expense_v1` (REVOKEd from authenticated — appelé uniquement par v2 RPCs SECURITY DEFINER). Tous avec gate `expenses.create`/`expenses.approve`/`expenses.thresholds.write` + audit_log canonical cols (`actor_id, action, entity_type, entity_id, metadata`) + REVOKE pair S25 canonique. **PIN-en-header (S25 pattern)** appliqué à `approve_expense_v2` via `useApproveExpense` hook (header `x-manager-pin`). **Idempotency** appliquée à `submit_expense_v2` via `useRef(crypto.randomUUID())` dans `useSubmitExpense` (S25 2-flavors flavor 2). **Permissions seedées (2)** : `expenses.thresholds.{read,write}` pour MANAGER+/ADMIN+ respectivement. **BO** — nouvelle page `/settings/expense-thresholds` avec sidebar entry icon `Scale` ; 4 nouveaux components (`ThresholdFormDialog` create/edit avec steps builder native HTML — `@breakery/ui` n'exporte pas Select/SelectItem ; `ApprovalTimeline` stepper vertical avec project semantic tokens ; `ThresholdResolutionBadge` Badge primitive ; `ApproveDialog` bumped SOD-aware + PIN input swap notes textarea) ; 5 hooks (`useExpenseThresholds`, `useSet/DeleteExpenseThreshold`, `useExpenseApprovals`, bumped `useSubmit/ApproveExpense` v2) ; `ExpenseDetailPage` wire badge + timeline. **Tests 26/26 PASS** : pgTAP `expense_governance.test.sql` **18/18 PASS** via cloud MCP (T1-T18 covering auto-approve / multi-step chain / SOD blocks / overlap validation / category override / cash sync / legacy fallback / boundary inclusive / REVOKE verification) + **BO smoke 8/8 PASS** (3 fichiers : expense-thresholds-page + approval-timeline + approve-dialog-sod). `pnpm typecheck` 6/6 PASS. **Hors scope S29+** : TASK-11-003 recurring expenses, TASK-11-004 OCR receipt (BLOCKED Capacitor), TASK-11-005 cost centers, TASK-11-006 per diem/advances, notifications email/in-app aux approvers (toast UI only), reject workflow étendu (reste 1-shot v1), migration des expenses historiques (fallback NULL snapshot préservé). **VAT path** : `_emit_expense_je` préserve la logique v1 mais `vat_amount > 0` raise P0002 sur `resolve_mapping_account('EXPENSE_VAT_INPUT')` car le compte 1151 est désactivé S26 ADR-003 NON-PKP — non-régression de S28. Deviations tracked in INDEX §12 (9 entries, 3 medium tous fixés via correctives 1.A.1 / 2.A.1 + 1 medium informational sur audit_logs cols).
- **Session 27c reference:** Product Variants ✓ merged 2026-05-24 via PR #36 (livré dans la PR S28, branche `swarm/session-28` basée sur `swarm/session-27c`) (34 commits, 19 migrations block `20260524002129..012658`, INDEX: [`docs/workplan/plans/2026-05-24-session-27c-INDEX.md`](docs/workplan/plans/archive/2026-05-24-session-27c-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-27c-spec.md`](docs/workplan/specs/archive/2026-05-24-session-27c-spec.md)). Base `master` @ `cab4ce3` (post-merge PR #35 turbo bump 2.9.14). Ferme **TASK-05-003** (Variants tab placeholder S27/S27b) + **S27b §7 follow-up #2** (deferred variants schema + UI). Architecture **Linked-Products** retenue : on étend la table `products` existante avec 4 cols nullables (`parent_product_id`, `variant_label`, `variant_axis`, `variant_sort_order`) + ENUM `variant_axis_type` ('flavor' | 'size' | 'format') + CHECK XOR + trigger `enforce_variant_no_nesting` (BEFORE INSERT/UPDATE empêche un variant d'être lui-même parent — 1 niveau max). 3 cases pour un product : standalone (inchangé), parent (ne se vend pas directement — POS affiche modal au tap), variant (enfant, propre SKU/prix/stock). **6 SECURITY DEFINER RPCs** : `convert_product_to_parent_v1` (standalone → parent, crée 1er variant) ; `create_variant_v1` (allowlist 14 cols clone-from-parent + overrides) ; `update_variant_v1` (allowlist 6 cols patch) ; `delete_variant_v1` (soft via `is_active = false` — never hard, FK orders.order_items) ; `reorder_variants_v1(p_parent_id, p_variant_ids[])` (assigns sort_order 10/20/30... + complete-coverage gate) ; `convert_parent_to_standalone_v1` (parent → standalone, garde 1er variant + hard-delete parent + soft-delete autres variants). Tous avec gate `products.variants.write` + audit_log + REVOKE pair S25 canonique. **3 correctives DB** : Wave 2.G `_005339` hard-delete parent (soft laissait orphan SKU — `products.sku` UNIQUE GLOBAL pas partial) ; Wave 2.H `_005402` SKU collision pre-check sur suffix `-PARENT` (évite 23505 unique violation mi-transaction) ; Wave 2.I `_012658` XOR partial-NULL fix sur variants soft-deleted (NULLait juste `parent_product_id`, laissait les 3 autres cols → violation CHECK 23514 ; discovered via Wave 4 pgTAP T15). **Permissions seedées (2)** : `products.variants.{read, write}` pour MANAGER/ADMIN/SUPER_ADMIN. **BO** — Le `<StubPanel module='Variants' />` du ProductDetailPage est remplacé par `<VariantsPanel>` 3-case switch consumant 8 hooks (2 read `useProductVariants` + `useProductParent` ; 6 write 1-per-RPC) ; 5 nouveaux components : `VariantsPanel`, `ConvertToParentDialog` (3-button axis fallback car @breakery/ui n'a pas `<RadioGroup>`), `AddVariantDialog`, `DissolveParentDialog`, `VariantRowSortable` (DnD @dnd-kit) ; ProductsFilters extension filter variant + badge parent/variant dans Grid/Table. **POS** — `useProducts` filtre variants out (parents s'affichent avec badge "Variants") via 2-query fallback (PostgREST relation embed non-élégant pour self-reference) ; nouveau `<VariantSelectModal>` Radix Dialog plein écran tablet (wiré dans `ProductTapHandler.tsx`, pas dans ProductGrid — seam plus propre) ; domain `Product` type widened +2 optional fields (`parent_product_id?`, `has_variants?`). **Tests 34/34 PASS** : pgTAP `product_variants.test.sql` **20/20 PASS** via cloud MCP (T1-T15 RPC happy/perm/edge cases + trigger anti-nesting + XOR fix + 5 audit_log asserts) + **BO smoke 10/10 PASS** (5 fichiers : variants-panel-empty/parent/variant + convert-to-parent-dialog + products-list-filter) + **POS smoke 4/4 PASS** (2 fichiers : variant-select-modal + pos-grid-hides-variants). `pnpm typecheck` 6/6 PASS (8s avec cache) ; full BO sweep 97 files / 322 tests PASS ; full POS sweep 62 files / 326 tests PASS — **aucune régression**. **Hors scope S27d** : bulk operations variants, UnitsPanel write mode (RPC `set_product_units_v1` existe depuis S27 catch-up mais pas consumer BO), Sections editor BO, `delete_category_v1`, pgTAP `update_category_v1`, NewProductDialog Zod, fully-optimistic reorder, edit-variant inline button (hook livré, UI déférée), modifier inheritance parent→variant, KDS variant rendering, receipt template variants. Deviations tracked in INDEX §11 (17 entries, 3 medium tous fixés via correctives 2.G/2.H/2.I — le reste informational).
- **Session 26b reference:** Comptable Cockpit UI core — 6 pages BO (CoA, JE + saisie OD, GL, TB, fiscal periods, hub 4-tiles) + RPC `update_account_active_v1` ✓ merged 2026-05-23 via PR #32/#33 on `swarm/session-26b`. 1 migration `20260523135820`. Détail : INDEX archivé [`2026-05-23-session-26b-INDEX.md`](docs/workplan/plans/archive/2026-05-23-session-26b-INDEX.md), spec [`2026-05-23-session-26b-spec.md`](docs/workplan/specs/archive/2026-05-23-session-26b-spec.md).
- **Session 26 reference:** Comptable Cockpit Wave 1 — DB hardening NON-PKP (ratification ADR-003 : PB1 10 % sortie PEMDA, PPN input foldée en coût inventory) ✓ merged 2026-05-20 via PR #31 (`6b796b7`) on `swarm/session-26`. 17 migrations `20260603000010..026` (PB1 dynamic + split par méthode, `calculate_pb1_payable_v1`, `record_cash_movement_v2`, dedupe P&L/BS, 4 RPCs cockpit, 6 perms `accounting.*`). Détail : INDEX archivé [`2026-05-20-session-26-INDEX.md`](docs/workplan/plans/archive/2026-05-20-session-26-INDEX.md), spec [`2026-05-20-session-26-spec.md`](docs/workplan/specs/archive/2026-05-20-session-26-spec.md), ADR : [`docs/adr/003-pkp-status-non-pkp.md`](docs/adr/003-pkp-status-non-pkp.md).
- **Session 27 + 27b reference:** Product CRUD : update + create + categories ✓ merged 2026-05-20 via PR #30 on `claude/continue-session-27-5iall` (10 commits across 2 sub-sessions, 27 migrations total (18 catch-up + 9 new), INDEX S27: [`docs/workplan/plans/2026-05-20-session-27-INDEX.md`](docs/workplan/plans/archive/2026-05-20-session-27-INDEX.md), INDEX S27b: [`docs/workplan/plans/2026-05-20-session-27b-INDEX.md`](docs/workplan/plans/archive/2026-05-20-session-27b-INDEX.md)). **S27b add-on** (this afternoon) closes the rest of the originally-planned S27 scope: `create_product_v1(jsonb)` 21-col allowlist + SKU uniqueness check + seeds product_unit_contexts ; `create_category_v1` auto-slugify ; `update_category_v1` 6-col patch ; `reorder_categories_v1(uuid[])` assigns sort_order=10,20,... with complete-coverage gate (caught a 42702 ambiguous-id bug via pgTAP T7 → corrective `_102709`) ; 4× REVOKE pair S25 canonique. **BO** : `useCreateProduct` + `NewProductDialog` (name/sku/cat/unit/retail/desc + SKU auto-uppercase + perm gate `products.create`) wired into ProductsHeader "+ New Product" pill ; new `/backoffice/categories` route + sidebar entry (gated `categories.read`) + `CategoriesPage` with `@dnd-kit` DnD reorder (pointer + keyboard sensors + rollback-on-error) + inline toggle active + `CategoryFormDialog` (create/edit). **Tests** : pgTAP `product_category_crud.test.sql` 10/10 PASS cloud ; BO smoke `new-product-dialog.smoke.test.tsx` 2/2 PASS ; S27 smoke regression 2/2 still PASS ; typecheck 6/6 PASS. **Scope déferé S27c** : bulk operations, variants schema + UI, UI consumers pour `set_product_units_v1` / `set_product_sections_v1` / `upsert_product_modifiers_v1` (RPCs existent depuis S27 catch-up mais Variants/Units/sections restent stubs), `delete_category_v1`, pgTAP `update_category_v1`. Closes TASK-05-002 (DONE), TASK-05-006 (DONE), TASK-05-007 (DONE).
- **Session 27 detail:** Product CRUD : update flow MVP ✓ merged 2026-05-20 via PR #30 (`8bbb137`) on `claude/continue-session-27-5iall` (6 commits, 4 phases, 18 catch-up migrations + 5 pgTAP + 2 BO smoke, INDEX: [`docs/workplan/plans/2026-05-20-session-27-INDEX.md`](docs/workplan/plans/archive/2026-05-20-session-27-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-20-session-27-spec.md`](docs/workplan/specs/archive/2026-05-20-session-27-spec.md). **Situation inhabituelle :** une session précédente avait appliqué 18 migrations à la base V3 dev cloud (timestamps `20260520022207..025140`) sans les commit en git — cette session **rattrape l'écart cloud↔git** + **finit le wiring UI** pour `update_product_v1`. **DB catch-up** (18 migrations mirrorées avec les timestamps cloud actuels pour matcher `schema_migrations.version`) — `_022207` ALTER products +5 settings cols (`description`, `visible_on_pos`, `available_for_sale`, `track_inventory`, `deduct_stock`) ; `_022244..341` 3 nouvelles tables (`product_unit_alternatives`, `product_unit_contexts`, `product_sections` M2M) ; `_022404` product_modifiers +ingredients_to_deduct JSONB ; `_022442` seed alts+contexts ; `_023035..543` 4 RPCs SECURITY DEFINER (`update_product_v1(uuid,jsonb)`, `set_product_units_v1`, `set_product_sections_v1`, `upsert_product_modifiers_v1`) ; `_023915..957` REVOKE pair S25 canonique ×4 ; `_024026..113` seed perms `products.{modifiers,sections,units}.update` ; `_025140` corrective set_product_sections_v1 primary reassign two-step. **Types** : regen MCP + `PermissionCode` étendu avec 3 perms. **BO** — nouveau hook `useUpdateProduct` (ProductUpdatePatch matche le 18-col allowlist) + `useProductDetail`/`useProducts` SELECT étendus aux 8 nouvelles colonnes + `ProductRow` enrichi + `ProductDetailPage` wiring (controlled patch state, dirty flag, save success reset, perm gate `products.update` via authStore) + `GeneralPanel` re-sync draft on prop change + 5 toggles controlled (role="switch") + description controlled + `ProductDetailHeader` isSaving prop + data-testid. **Tests** — pgTAP `update_product_v1.test.sql` 5/5 PASS cloud (T1 happy path, T2 CASHIER 42501, T3 not found P0002, T4 cost_price ignored, T5 audit_logs ×2) ; BO smoke `product-detail-save.smoke.test.tsx` 2/2 PASS (disabled-when-clean + RPC call assertion) ; `pnpm typecheck` 6/6 PASS. **Scope déferé S27b** : `create_product_v1` + new product flow, categories CRUD + DnD page, bulk operations, variants tab. Closes TASK-05-002 (PARTIAL — update done, create deferred). Deviations tracked in INDEX §6 (1 medium DEV-S27-SCOPE-01 = scope split into 27a/b).
- **Session 25 reference:** Hardening Idempotency Cross-EF ✓ merged 2026-05-20 via PR #28 (`b8814ef`) on `swarm/session-25`. 6 migrations `20260602000010..015` ; refund-order EF v7 (PIN `x-manager-pin` header + `p_idempotency_key`), `create_tablet_order_v2(p_client_uuid)` + table dédiée. Les 2 patterns canoniques issus de cette session (PIN-en-header, idempotency 2-flavors) vivent dans les Critical patterns ci-dessous. Détail : INDEX archivé [`2026-05-19-session-25-INDEX.md`](docs/workplan/plans/archive/2026-05-19-session-25-INDEX.md), spec [`2026-05-19-session-25-spec.md`](docs/workplan/specs/archive/2026-05-19-session-25-spec.md).
- **Session 24 reference:** B2B Foundation ✓ merged 2026-05-19 via PR #27 (`1749d92`) on `swarm/session-24`. 11 migrations `20260601000005..022` (`b2b_payments` ledger append-only, views `view_b2b_invoices`/`view_ar_aging`, RPCs `record_b2b_payment_v1`/`adjust_b2b_balance_v1`/`create_b2b_order_v1` + gate credit-limit). Détail : INDEX archivé [`2026-05-19-session-24-INDEX.md`](docs/workplan/plans/archive/2026-05-19-session-24-INDEX.md), spec [`2026-05-19-session-24-spec.md`](docs/workplan/specs/archive/2026-05-19-session-24-spec.md).
- **Session 19 reference:** Hardening polish — rate-limit Postgres durable (`record_rate_limit_v1` + 5 EFs migrées), session timeout per-role (`roles.session_timeout_minutes` + `useIdleTimeout`), PIN strength warn-only ✓ merged 2026-05-17 on `swarm/session-19`. 7 migrations `20260523000010..022`. Détail : INDEX archivé [`2026-05-17-session-19-INDEX.md`](docs/workplan/plans/archive/2026-05-17-session-19-INDEX.md), spec [`2026-05-17-session-19-spec.md`](docs/workplan/specs/archive/2026-05-17-session-19-spec.md).
- **Session 18 reference:** Recipe Cost History Report — RPC `recipe_cost_history_v1` dual-mode + 2 pages BO (Overview cross-recipe + Timeline recharts), gate `reports.financial.read` ✓ merged 2026-05-17 on `swarm/session-18`. 1 migration `20260522000010`. Détail : INDEX archivé [`2026-05-17-session-18-INDEX.md`](docs/workplan/plans/archive/2026-05-17-session-18-INDEX.md), spec [`2026-05-17-session-18-spec.md`](docs/workplan/specs/archive/2026-05-17-session-18-spec.md).
- **Session 17 reference:** Full price chain — PO receipt → WAC `products.cost_price` → cascade snapshots recettes (`WITH RECURSIVE`) + `recipe_bom_full_v1` ✓ merged 2026-05-17 via PR #21 (`5e79509`) on `swarm/session-17`. 7 migrations `20260521000010..030`. Détail : INDEX archivé [`2026-05-17-session-17-INDEX.md`](docs/workplan/plans/archive/2026-05-17-session-17-INDEX.md), spec [`2026-05-17-session-17-spec.md`](docs/workplan/specs/archive/2026-05-17-session-17-spec.md).
- **Session 16 reference:** CI revival + S15 follow-ups — pgTAP nightly cron vs V3 dev (`pgtap-nightly.yml`), `products.is_semi_finished` + index pg_trgm, coût per-version dans `recipe_versions.snapshot`, `expandRecipeCascade` ✓ merged 2026-05-16 via PR #20 (`f7c83b2`) on `swarm/session-16`. 8 migrations `20260520000010..022`. Détail : INDEX archivé [`2026-05-16-session-16-INDEX.md`](docs/workplan/plans/archive/2026-05-16-session-16-INDEX.md), spec [`2026-05-16-session-16-spec.md`](docs/workplan/specs/archive/2026-05-16-session-16-spec.md).
- **Session 15 reference:** Bakery Production — F6 sub-recipes (anti-cycle + cost cascade + `recipe_versions` + cascade `record_production`), F5 yield tracking, recipe pro features (IngredientPicker, DnD, Batch, Schedule, Margin alerts, Boulanger %, allergens EU) ✓ merged 2026-05-16 on `swarm/session-15` (53 commits, 32 migrations `20260519000001..162`). Détail : INDEX archivé [`2026-05-15-session-15-INDEX.md`](docs/workplan/plans/archive/2026-05-15-session-15-INDEX.md).
- **Session 14 reference:** UX completion ✓ merged `d7d60d5` on 2026-05-14 (68 commits, 6 waves). INDEX: [`docs/workplan/plans/2026-05-14-session-14-INDEX.md`](docs/workplan/plans/archive/2026-05-14-session-14-INDEX.md).
- **Session 13 follow-ups (deferred Session 20+):** Playwright CI job (D-W6-6C-05), `pg_net`-based birthday cron (D-W6-6B-02), Cash Flow Investing/Financing sections (D-W6-6A-2), `mv_pl_monthly` branched reuse (D-W6-6A-1), staging-deploy.yml secrets (D-W6-CICD-01).
- **Session 15 follow-ups:** DEV-S15-5.C-01 (Receipt template + customer display allergen integration) — **WONTFIX per user decision 2026-05-17** (memory: `project_allergens_wontfix`) — no allergens module needed for receipt/display.
- **Session 16 follow-ups (deferred Session 20+):** Trigram indexes not yet engaged by RPC predicate (DEV-S16-2.A-01, would need `%` operator + `set_limit`) ; legacy `recipe_versions` rows have no cost reconstructible (DEV-S16-2.B-02) ; nightly pgTAP is the only automated check, no PR-time gate (DEV-S16-1.A-01).
- **Session 17 follow-ups (deferred Session 20+):** N-snapshots-in-one-tx for high-fanout recipe graphs (DEV-S17-1.A-01, informational, Breakery depth ≤ 5 well within bounds) ; manual `products.cost_price` UPDATE bypasses WAC and doesn't emit `stock_movements` audit row (DEV-S17-1.B-01) ; WAC applies uniformly to all purchase movements, no opt-out for sample stock / promo (DEV-S17-1.C-01, low) ; WAC garbage-in if `current_stock` stale (DEV-S17-1.C-02, informational) ; `expandRecipeCascade` has no current consumer in `apps/` — preserved as public `@breakery/domain` API (DEV-S17-2.A-01, informational, downgraded).
- **Session 18 follow-ups (deferred Session 20+):** RPC scans all `recipe_versions` per call — no index on `(product_id, created_at DESC)` ; at current cardinality (~13 products × ~3 versions) negligible (DEV-S18-1.A-01, informational) ; overview baseline lookup uses correlated subqueries (~2× planner work), could be rewritten with a window function (DEV-S18-2.A-01, informational ; note: implem uses correlated subquery, not LATERAL as originally written) ; CSV exports raw NUMERIC without locale formatting (DEV-S18-2.A-02, informational) ; timeline chart X-axis = raw ISO date strings, no locale formatting (DEV-S18-2.B-01, informational) ; no zoom interaction on chart (DEV-S18-2.B-02, informational).
- **Session 25 follow-ups (deferred Session 26+):** `_shared/idempotency.ts` UUID_REGEX accepts UUID v1-5 while the error message says "must be UUID v4" (harmless because POS uses `crypto.randomUUID()` which always returns v4 — tighten regex if strict v4 needed) (DEV-S25-1.A-01, informational) ; corrective `_013` `ALTER DEFAULT PRIVILEGES FROM PUBLIC` is currently a no-op since S20 global sweep already set it, but the `_012` + `_013` pair is now the canonical template for future REVOKE-EXECUTE migrations (DEV-S25-1.A-02, informational) ; silent-skip on unknown `product_id` in `create_tablet_order_v2` FOR loop (inherited from v1) — with idempotency the failure mode becomes permanently unrecoverable (first call creates truncated order, all retries replay truncated `order_id`), track for v3 to raise `Product % not found` (DEV-S25-1.A-03, **medium**) ; idempotency replay in `create_tablet_order_v2` bypasses `sales.create` permission check (deliberate, matches S24 `record_b2b_payment_v1` pattern — replay is read-only-like) (DEV-S25-1.A-04, informational) ; `TabletCheckoutButton.tsx` extended with the same `clientUuidRef` lifecycle as `TabletOrderPage` — not in spec but necessary for the 2nd call site (DEV-S25-1.A-05, informational) ; corrective `_014` relax `orders.session_id` for tablet caught dormant S24 bug — S24 `_006` relaxed only for `order_type='b2b'`, broke tablet flow silently until pgTAP T1 surfaced it (DEV-S25-1.A-06, **medium, fixed this session**) ; corrective `_015` `fix_refund_order_rpc_v2_replay_record` fixed a dormant S13→S25 bug where `refund_order_rpc_v2` line 90 wrote to `v_order.order_number` on an unassigned RECORD — fixed via CREATE OR REPLACE with local TEXT var, signature unchanged (DEV-S25-1.A-07, **medium, fixed this session**) ; Vitest live `idempotency-hardening.test.ts` structurally validated but actual live run requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars exported user-side — same project-wide gap as S19 DEV-S19-2.A-01 ; refund-order EF v7 deployed to V3 dev via MCP so live tests will exercise the right code (DEV-S25-2.A-01, informational) ; 13 backoffice test files fail with `Invalid environment variables: VITE_SUPABASE_URL Required` — pre-existing env-gating pattern, S25 touched zero backoffice files (DEV-S25-2.A-02, informational) ; pgTAP test file uses GUCs (`current_setting('breakery.t1_pass'::text)`) to chain pass/fail flags between DO blocks because pgTAP's `ok()` must be called from a SELECT, not a DO — portable pattern (DEV-S25-2.A-03, informational).
- **Session 19 follow-ups (deferred Session 20+):** RPC `record_rate_limit_v1` holds a row-lock during upsert — under sustained attack ≥100 req/s on the same bucket, attacker + defender serialize on the same bucket ; inconsequential at Breakery's traffic (DEV-S19-1.A-01, informational) ; fail-open on DB error is a deliberate trade-off — investigate pool sizing before reconsidering fail-closed (DEV-S19-1.A-02, informational) ; `audit_logs.entity_id` is NULL for `role.session_timeout_changed` rows — `role_code` lives in `payload` JSON instead (DEV-S19-1.B-01, informational) ; **medium** — `REVOKE FROM anon` is not implied by `REVOKE ALL FROM PUBLIC` on public-schema functions ; the corrective migration `20260523000022` adds explicit `REVOKE EXECUTE ... FROM anon` on `update_role_session_timeout_v1` and is now a project-wide critical pattern (DEV-S19-1.B-02, medium, see Critical patterns below) ; Vitest env vars unset in `supabase/tests/functions/*` mean the cross-instance RL simulation requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` exported manually (DEV-S19-2.A-01, informational) ; 429 responses lack `Retry-After` header — project-wide gap (DEV-S19-2.A-02, informational) ; `pin-strength.ts` duplicated in `packages/utils` and `supabase/functions/_shared` — sync test catches drift but doesn't prevent it ; consider build-time copy (DEV-S19-2.B-01, informational) ; top-100 leaked PIN list inlined as a literal array — manual refresh cadence (DEV-S19-2.B-02, informational) ; `COMMON_PINS` has 101 entries including dead string `'232425'` (DEV-S19-2.B-03, informational) ; `useIdleTimeout` fires `signOut()` immediately — no "you are about to be signed out" warning toast (DEV-S19-3.A-01, informational) ; pre-existing : `UserDetailPage` validates 4-8 digits, EF requires exactly 6 — one-line regex fix for S20+ (DEV-S19-3.B-01, informational) ; BO admin-reset uses `reset_user_pin_v1` RPC (not `auth-change-pin` EF) ; shared `evaluatePinStrength` util preserves single source of truth (DEV-S19-3.B-02, informational) ; POS used `NumpadPin` collection primitive instead of verification-only `PinPad` — minor spec wording deviation (DEV-S19-3.C-01, informational) ; hint surfaces at step 3 not step 2 (DEV-S19-3.C-02, informational, UX polish) ; mismatch resets to step 1 instead of conventional step 2 (DEV-S19-3.C-03, informational, UX polish).
- **Module reference (canonical):** [`docs/reference/04-modules/`](docs/reference/04-modules/) per module (e.g., `15-production-recipes.md` for production).
- **Backlogs:** `docs/workplan/backlog-by-module/01-…25-….md` — 25 modules, ~280 tasks total. Session 33+ source : TBD (open with backlog triage post-S32 merge).
- **Execution skill:** invoke `superpowers:subagent-driven-development` (or `superpowers:executing-plans`) before running a phase. Each phase is isolated → one subagent per phase, parallelizable per Wave.
- **Workplan layout:** `docs/workplan/{plans,specs,refs,backlog-by-module}/`. Plans/specs are **dated, append-only history** — never rewrite past plans; create a new dated file. Backlog files are living docs (update in place).
- **Migration sequence active:** Session 47 (2026-06-19, `swarm/session-47-combos`) used NAME-block `20260704000010..022` (cloud `version`s clock-assigned per S36+ convention ; base verified via `list_migrations`) : `_010` `combo_schema` (products combo cols + `combo_groups` + `combo_group_options` + parent/anti-nesting guards + RLS) ; `_011` `upsert_combo_v1` + `_012` REVOKE pair ; `_013` `delete_combo_v1` (soft) + `_014` REVOKE pair ; `_015` migrate legacy combos → groups + **drop `combo_items`** ; `_016` `complete_order_with_payment_v12→v13` (+`order_items.combo_components`, deduct components not the virtual combo, DROP v12) + `_017` REVOKE pair ; `_018` `combo_aware_reversals` (void/refund restore component stock) ; **fire-path extension (DEV-S47-Aprime-01)** `_019` `fire_counter_order_v2→v3` (persist `combo_components` snapshot, DROP v2) + `_020` REVOKE pair ; `_021` `pay_existing_order_v8→v9` (deduct each persisted component, display-aware, DROP v8) + `_022` REVOKE pair. **Perm DEV-S47-A2-01** : reuse S11 `combos.{create,update,delete}` — no new `products.combos.write`, `PermissionCode` union unchanged. Types regen committé (v13/v9/v3 present, v12/v8/v2 absent ; +`order_items.combo_components` ; +combo tables/RPCs). EF `process-payment` redeploy **version 11 → v13** (`verify_jwt=false`, mandatory: A5 dropped v12). For the A′ pair the controller folded each REVOKE into the function's `apply_migration` (2 cloud entries `bump_fire_counter_order_v3`/`bump_pay_existing_order_v9`); local `_020`/`_022` files are the canonical REVOKE record, idempotent on replay (DEV-S47-Aprime-02). pgTAP cloud `combo_fire_pay` 8/8 + existing combo suites. Session 46 (2026-06-18, PR #95) used NAME-block `20260701000010..019` (10 migrations, base verified via `list_migrations`, prior max NAME `20260630000024`) : `_010` ALTER `purchase_order_items` +`unit_factor_to_base NUMERIC(20,10) NOT NULL DEFAULT 1` + CHECK > 0 ; `_011` `receive_purchase_order_v2` (conversion `qty × factor` → base unit, +DROP v1) ; `_012` table `purchase_payments` (append-only, RLS `auth_read` gated `purchasing.po.read`, REVOKE INSERT/UPDATE/DELETE) ; `_013` `record_po_payment_v1` (gated `purchasing.po.pay`, idempotency flavor 2, JE ledger-first puis paiement) + helper interne **no-gate** `_record_po_payment_internal` (REVOKEd PUBLIC/anon/authenticated, appelé par le trigger pour l'auto-pay cash — DEV-S46-C1) + REVOKE pairs ; `_014` seed mapping `PURCHASE_PAYMENT_BANK`→1112 ; `_015` redesign trigger `create_purchase_journal_entry` (**DR INVENTORY / CR PAYABLE 2141 toujours** à la réception, VAT-fold NON-PKP ADR-003 préservée ; branche cash → `PERFORM _record_po_payment_internal` ; pattern S38 `pg_get_functiondef`+replace) ; `_016` `update_purchase_order_v1` (gated `purchasing.po.edit`, lock D6 GRN/paiement P0001, recalcul totaux, `purchase_order_items.subtotal` GENERATED omise de l'INSERT — DEV-S46-C3) + REVOKE pair ; `_017` seed perms `purchasing.po.{pay,edit}` MANAGER/ADMIN/SUPER_ADMIN ; `_018` `create_purchase_order_v2` (Wave B0 — guard `category_type='raw_material'` R1 + `unit_factor_to_base` persist R2, +DROP v1, +REVOKE pair canonique — sinon R1/R2 ne tenaient que sur le chemin edit, DEV-S46-B2-01 medium) ; corrective `_019` `revoke_extra_privileges_purchase_payments` (REVOKE TRUNCATE/TRIGGER/REFERENCES — TRUNCATE contournait le REVOKE DELETE de l'append-only, pattern-guardian DEV-S46-B-PG1, mirroir Stock-Audit m1 `_016` ; anon avait déjà 0 privilège via S20). **Pas de migration backfill** (`_015` du plan abandonné — 0 PO cash reçu, open-question #2 option b ; numérotation glissée). Types regen committé (v1→v2 ×2, +`purchase_payments`, +`record_po_payment_v1`, +`update_purchase_order_v1`, +`unit_factor_to_base`). **Process DEV-S46-PROC-01** : subagents sans MCP → `db-engineer` a rédigé le SQL Wave A, le **contrôleur** a appliqué via MCP `apply_migration`, rejoué le pgTAP en cloud (54/54), regen types, et appliqué `_018`/`_019` lui-même ; cloud versions clock-assignées (convention S36+), miroirs locaux = NAME-block exact. Fix bug latent : parenthèse manquante `po_je_double_entry.test.sql` T8 (`) < 1,` → `) < 1),`, DEV-S46-B-JE-T8). Post-S45 fix PR #83 (2026-06-16, DEV-S45-IMP-01) used NAME-block `20260629000013..016` (4 migrations, base verified prior max NAME `20260629000012`) : `_013` `fix_import_catalog_v1_numeric_range_validation` (règle V19 bornes NUMERIC products, `CREATE OR REPLACE` body déployé, signature inchangée → pas de types regen) ; `_014` `extend_import_catalog_v1_v19_recipe_unit_ranges` (V19 étendue recipes.quantity + product_unit_alternatives.factor_to_base) ; `_015` `widen_recipe_cost_walk_numeric_overflow` (`_calculate_recipe_cost_walk` + `_snapshot_recipe_version` working vars → `NUMERIC(38,s)`, même scale 4/2, anti-`22003` sur tout chemin recette) ; `_016` `add_recipe_unit_to_recipe_bom_full_v1` (DROP+CREATE `RETURNS TABLE` +`recipe_unit`, grants ré-appliqués, **types regen committé** additif). Session 45 (2026-06-13) used NAME-block `20260629000010..012` (3 migrations — `_010` `create_delete_product_v1_rpc` + `_011` `revoke_anon_delete_product_v1` (REVOKE pair 5 lignes, DEV-S45-A-02) + corrective `_012` `fix_delete_product_v1_set_deleted_at` (CREATE OR REPLACE, signature inchangée — ajoute `deleted_at=now()` + replay guard sur `deleted_at`, DEV-S45-A-01). Base verified via `list_migrations`, prior max NAME `20260628000018`. **Process DEV-S45-PROC-01** : MCP indisponible dans les subagents → `db-engineer` a appliqué via `supabase db query --linked` + insertion ledger manuelle, **vérifié en cloud par le contrôleur via MCP** (`pg_get_functiondef` confirme `sets_deleted_at`/`replay_guard_deleted_at` + pgTAP 8/8 ré-exécuté). Types regen additif (`delete_product_v1(uuid,uuid)→jsonb`), committé. Session 44 (2026-06-13) used NAME-block `20260628000010..018` (9 migrations — appliquées via MCP `apply_migration`, versions cloud clock-assignées convention S36+ ; miroirs locaux = NAME-block exact ; base verified via `list_migrations`, prior max NAME `20260627000016`) : `_010` `get_loyalty_multiplier(INT)` + REVOKE pair (3ᵉ ligne ALTER DEFAULT PRIVILEGES omise → fermée par `_018`) ; `_011` corrective `create_sale_journal_entry` (CASE enum réel + seed `SALE_PAYMENT_TRANSFER` + fallback cash observable, fonction trigger sans REVOKE pair — non callable PostgREST) ; `_012` `complete_order_with_payment_v12` (DO-block DROP v11 + CREATE) ; `_013` REVOKE pair v12 (3 lignes) ; `_014` `pay_existing_order_v8` (DROP v7) ; `_015` REVOKE pair v8 ; `_016` `fire_counter_order_v2` (DROP v1) + REVOKE pair inline (3ᵉ ligne omise → `_018`) ; corrective `_017` `fix_reversal_rpcs_display_stock` (pattern S38 `pg_get_functiondef`+replace sur `void_order_rpc_v2` + `refund_order_rpc_v3`, signatures inchangées, guard `IF v_def NOT LIKE '%display_movements%'`) ; corrective `_018` `assert_alter_default_privileges_s44` (pattern-guardian P11, ré-assertion unique PUBLIC+anon, pattern S40 `_022`). Types regen committé après B4 (v12/v8/v2 présents, v11/v7/v1 absents). EF `process-payment` redeploy v10 via MCP `deploy_edge_function` (5 fichiers index+_shared, `verify_jwt=false`). Note non-régression : les 3 pgTAP S37 (`order_discount_gate`/`pay_existing_discount_gate`/`loyalty_transactions_append_only`) bumpés v12/v8 (args nommés, multiplier jamais passé), DEV-S44-E-01. Session 43 (2026-06-13) used NAME-block `20260627000010..016` (7 migrations — 2 planifiées + 5 correctives, base verified via `list_migrations`, prior max NAME `20260626000020` ; appliquées via MCP `apply_migration`, versions = NAME-block exact) : `_010` table `counter_fire_idempotency_keys` (flavor 2 S25, RLS sans policy + REVOKE all — RPC-only) ; `_011` `fire_counter_order_v1(UUID, UUID, JSONB, UUID, TEXT, order_type)` + REVOKE pair inline (forme combinée `FROM PUBLIC, anon` — DEV-S43-P11-01, les 3 garanties satisfaites) ; corrective `_012` `fix_orders_order_number_daily_unique` (DROP du UNIQUE global `orders_order_number_key` → index unique `(order_number, jour)` — DEV-S43-C1-01, le pgTAP a réveillé un 23505 partagé v11/v2/fire sur la 1ʳᵉ commande de chaque jour) ; corrective `_013` clamp discount ligne `[0, brut]` dans `fire_counter_order_v1` (CREATE OR REPLACE, signature inchangée — v7 encaisse `SUM(line_total)` verbatim) ; `_014` data-only images placeholder → NULL + Patio sort_order ≥ 100 (prévu `_012` au plan, décalé par les correctives) ; corrective `_015` `fix_order_number_daily_bucket_timezone` (bucket UTC → **Asia/Makassar**, le fuseau réel de la DB — DEV-S43-F1-02, fenêtre 00:00–08:00 locale attrapée en réel par l'E2E T3) ; corrective `_016` `fix_pay_existing_v7_accept_fired_counter_orders` (DO-block S38 `pg_get_functiondef` + replace, signature inchangée — gate `draft`-only → `draft OU (pending_payment ET created_via='pos')`, DEV-S43-F1-03). Types regen committé (`63142ce`, +44 lignes). Hors migrations : fix `supabase/functions/_shared/acting-user.ts` (résolution `JWT_SECRET ?? SUPABASE_JWT_SECRET` — DEV-S43-F1-01) + redeploy de 6 EFs (cf. bullet S43). Stock Audit Fixes (2026-06-12) used NAME-block `20260626000010..020` (11 migrations — 6 planifiées + 2 correctives `_014`/`_017` découvertes en route + m4 cleanup `_018`/`_019` validé owner + corrective `_020` restore BEV-AMER, base verified via `list_migrations`, prior max NAME `20260625000016`) : `_010` fix cron context primitive + seed profil SYSTEM ; `_011` widen `margin_alerts` pct (7,2) ; `_012` `section_required` production ×2 RPCs ; `_013` solde par section transfers + data fix négatifs ; `_014` corrective transfers `track_inventory` ; `_015` `production_in` coût réel + WAC trigger `production_in` ; `_016` REVOKE extra privileges tables stock ; `_017` corrective `to_jsonb` ; `_018` m4 cleanup data (merge catégorie + soft-delete résidus) ; `_019` corrective m4 (BEV-CAPP rename → collision avec BEV-004 utilisé → soft-delete du batch seed mnémonique) ; `_020` corrective restore BEV-AMER (le soft-delete `_018` cassait `update_cost_price_v1` en cascade — fixture du seed canonique restaurée, cf. bullet Stock Audit Fixes). Pattern dominant : corrective S38 (DO-block `pg_get_functiondef` + replace, signatures inchangées, ACL conservées). Types regen vérifié no-diff (`_011` ne change que des précisions NUMERIC). Session 41 used NAME-block `20260625000010..016` (7 migrations, base verified via `list_migrations`, prior max NAME `20260624000022`) : `_010` table `catalog_import_idempotency_keys` (RLS sans policy, REVOKE all — RPC-only) ; `_011` `import_catalog_v1(JSONB, BOOLEAN, UUID)` + REVOKE pair inline ; `_012` `export_catalog_v1()` + REVOKE pair inline ; `_013` seed perms `catalog.{import,export}` MANAGER/ADMIN/SUPER_ADMIN ; corrective `_014` `fix_import_catalog_v1_min_stock_threshold` (COALESCE 0 — NULL explicite écrasait le DEFAULT NOT NULL, DEV-S41-T5-01) ; corrective `_015` `fix_import_catalog_v1_update_where_clause` (`WHERE 1=1` — la protection Supabase dangerous-writes rejette un UPDATE sans WHERE in-RPC via PostgREST, DEV-S41-T6-01) ; corrective `_016` `fix_import_catalog_v1_soft_deleted_sku_conflict` (restore des SKUs soft-deleted — UNIQUE global, DEV-S41-T7-01). Types regen committé (`9f114f0`). Note process DEV-S41-A1-01 : MCP indisponible dans les subagents → migrations appliquées via `supabase db query --linked` + ledger inséré manuellement (versions = NAME-block exact, pas clock-assignées). Session 40 used NAME-block `20260624000010..022` (13 migrations, cloud versions clock-assignées convention S36 ; base verified via `list_migrations`, prior max NAME `20260623000012`) : `_010` trigger `audit_role_permissions_changes` sur `role_permissions` (fonction SECURITY DEFINER REVOKEd PUBLIC/anon, pas de GRANT — trigger-only) ; `_011..014` RPCs `get_daily_sales_v1` / `get_purchase_items_v1` / `get_purchase_by_date_v1` / `get_purchase_by_supplier_v1` (REVOKE pair inline) ; corrective `_015` `fix_get_daily_sales_v1_refund_only_days` (FULL OUTER JOIN — DEV-S40-A1-01, a décalé la numérotation A2 du plan) ; `_016..020` RPCs `get_staff_performance_v1` / `get_production_report_v1` / `get_production_efficiency_v1` / `get_price_changes_v1` / `get_permission_changes_v1` ; corrective `_021` `fix_get_permission_changes_v1_gate` (`audit_log.read` → `reports.audit.read`, DEV-S40-C-01) ; corrective `_022` `assert_alter_default_privileges_s40` (pattern-guardian P11 — la 3ᵉ ligne canonique manquait dans les 12 migrations, une seule ré-assertion les ferme toutes, DEV-S40-D-01). Types regen committé (`f3aa767`). Session 39 used NAME-block `20260623000010..012` (3 migrations BO-15 : `_010` table singleton `b2b_settings` + seed + RLS + REVOKE table ; `_011` `get_b2b_settings_v1` + `update_b2b_settings_v1` ; `_012` REVOKE pair canonique S25. Base verified via `list_migrations`, prior max NAME `20260622000016`. Types regen committé `f566575`. Note : rows ledger `_011`/`_012` ont `name` NULL côté cloud, DEV-S39-A-01 — contenu vérifié sans drift). Session 38 used NAME-block `20260622000010..016` (6 migrations appliquées, cloud versions clock-assignées convention S36 ; base verified via `list_migrations`, prior max NAME `20260620000017`) : `_010` create helper `_verify_pin_with_lockout` + REVOKE pair (réécrit par `_016`) ; `_011..015` wire `_verify_pin_with_lockout` dans `sign_zreport_v2`/`close_fiscal_period_v1`/`create_manual_je_v1`/`approve_expense_v3`/`complete_order_with_payment_v11` via DO-block `pg_get_functiondef` + replace (signatures **inchangées** — pattern corrective S25, pas de bump) ; `_016` `record_pin_failure_v1` (service_role only, comptage en tx séparée car le `RAISE P0003` rollback le comptage in-RPC — DEV-S38-A-02) + réécriture du helper pour déléguer sa branche échec. Types regen committé (2 fonctions additives, aucune signature existante touchée). Session 37 used NAME-block `20260621000010..020` (11 migrations appliquées, cloud versions clock-assignées convention S36 ; base verified via `list_migrations`, prior max NAME `20260620000017`) : `_010` bump `complete_order_with_payment_v11` (discount gate + PIN + unit_price reconcil + audits + DROP v10) ; `_011` REVOKE pair v11 ; `_012` bump `pay_existing_order_v7` (discount gate permission-only + jsonb envelope + DROP v6) ; `_013` REVOKE pair v7 ; `_014` REVOKE writes `loyalty_transactions` (append-only) ; `_015` bump `sign_zreport_v2` PIN in-arg + REVOKE pair inline + DROP v1 ; `_016` **skipped** (D4 : `sales.discount` pré-existante S5 réutilisée) ; `_017` customers POS RPCs v2 (`search/get/create_customer_v2` embed `category JSONB` + DROP v1 ×3 + REVOKE pairs) ; corrective `_019` `create_customer_v2` default category server-side ; `_020` bump `get_pos_b2b_debts_v2` (sémantique panel + DROP v1 + REVOKE pair) ; `_018` `gate_customers_read` (**hard cutover PII, appliquée EN DERNIER** — chronologiquement après `_020`, order-independent au replay : policy `auth_read` exige `customers.read` + grant MANAGER/ADMIN/SUPER_ADMIN). Types regen ×2. Session 36 used `20260620000017` (1 migration NAME-block `_017` `revoke_send_items_to_kitchen_anon` — canonical S25 REVOKE pair on `send_items_to_kitchen(UUID[])` ; REVOKE/GRANT only, no schema change, no types regen ; cloud `version` clock-assigned ; anon EXECUTE already `false` from the S20 sweep so this is defense-in-depth + a regression guard, DEV-S36-A-02). Base verified via `list_migrations` (prior max NAME `20260620000016`). Session 35 used `20260620000010..016` (7 migrations F-003 held orders : `_010` ALTER orders +`is_held`+`notes` + partial index ; `_011` `held_order_idempotency_keys` table + `hold_order_v1` RPC ; `_012` REVOKE pair hold ; `_013` `restore_held_order_v1` + REVOKE pair inline ; `_014` `discard_held_order_v1` + REVOKE pair inline ; corrective `_015` relax `orders_session_id_required_for_pos` to exempt `is_held=true` — caught by pgTAP T1 ; corrective `_016` `fix_held_rpcs_default_privileges_from_public` — pattern-guardian P11, ALTER DEFAULT PRIVILEGES `FROM anon` → canonical `FROM PUBLIC`). Base verified via `list_migrations` (prior max `20260606000013`). Types regen post-`_015`. Session 34 used `20260601043059` (1 migration — data-only UPDATE categories dispatch_station remap; no schema change). Session 33 used `20260618000010..023` + cloud-clock corrective `20260529200749` (15 migrations total — see S33 reference). Session 32 used `20260617000010..014` (4 migrations : `_010` bump `get_profit_loss_v1` expose `account_id` additive ; `_011` bump `get_balance_sheet_v1` expose `account_id` additive + add `lines[]` array ; `_013` create `get_orders_list_v1` RPC cursor-paginated JSONB filters ; `_014` REVOKE pair on `get_orders_list_v1` S25 canonical ; `_012` skipped — CF bump deferred S33+ DEV-S32-1.D-01 indirect method has no per-account lines). Session 31 used `20260616000010` (1 migration : seed perm `orders.read` + role_perms grants MANAGER/ADMIN/SUPER_ADMIN). Session 30 used `20260615000010..019` + `20260524231049..124` (10 migrations : inventory RPCs `_000010` `get_wastage_report_v1` + `_000011` REVOKE pair ; `_000016` `get_stock_movements_v1` + `_000017` REVOKE pair ; `_000018` `get_perishable_turnover_v1` + `_000019` REVOKE pair ; finance RPCs `_231049` `get_payments_by_method_v1` + `_231054` REVOKE pair ; `_231118` `get_pb1_report_v1` + `_231124` REVOKE pair ; 0 correctives). Session 29 used `20260606000010..023` (14 migrations : `_000010` `z_reports` table + RLS + storage bucket `zreports/` ; `_000011` REVOKE pair z_reports ; `_000012` `sign_zreport_v1` RPC + REVOKE pair ; `_000013` `void_zreport_v1` RPC + REVOKE pair ; `_000014` `get_zreport_snapshot_v1` RPC + REVOKE pair ; `_000015` bump `close_shift_v2` snapshot insert ; `_000016` REVOKE pair close_shift_v2 ; `_000017` seed perms `zreports.{read,sign,void}` ; `_000018` EF `generate-zreport-pdf` deploy ; `_000019` EF `generate-pdf` deploy ; `_000020` `reports-exports/` bucket + storage policies ; `_000021` types regen ; `_000022` corrective `close_shift_v2` non-blocking pg_net ; `_000023` corrective REVOKE anon generate-pdf EF public function. Timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 28 used `20260524111854..123026` (16 migrations block tables + RPCs + trigger + perm seed + 2 correctives : `_111854` create `expense_approval_thresholds` table ; corrective `_112433` `fix_thresholds_index_nulls_order` NULLS LAST ; `_112621` create `expense_approvals` table ; `_113023` ALTER expenses +3 cols ; `_113353` seed 3 brackets defaults ; `_114442` bump `submit_expense_v2` + extract `_emit_expense_je` helper ; corrective `_115443` `fix_submit_expense_v2_security_hardening` (auth-first + pg_temp + vat coalesce + helper REVOKE) ; `_115713` REVOKE pair submit_v2 ; `_120104` bump `approve_expense_v2` SOD + multi-step ; `_121140` REVOKE pair approve_v2 ; `_121337` `set_expense_threshold_v1` UPSERT + overlap ; `_122002` REVOKE pair set_v1 ; `_122136` `delete_expense_threshold_v1` ; `_122427` REVOKE pair delete_v1 ; `_122632` `sync_cash_expense_to_session` trigger ; `_123026` seed perms `expenses.thresholds.{read,write}` + types regen. Timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 27c used `20260524002129..012658` (19 migrations block ENUM + ALTER + trigger + 6 RPCs avec REVOKE pair + 3 correctives + perms seed : `_002129` ENUM `variant_axis_type` ; `_002210` ALTER products +4 cols variant + CHECK XOR + 2 indexes partiels ; `_002257` trigger `enforce_variant_no_nesting` + types regen ; `_003312/3325` `convert_product_to_parent_v1` + REVOKE pair ; `_003433/3441` `create_variant_v1` + REVOKE pair ; `_003538/3543` `update_variant_v1` + REVOKE pair ; `_003629/3636` `delete_variant_v1` + REVOKE pair ; `_003729/3736` `reorder_variants_v1` + REVOKE pair ; `_003827/3833` `convert_parent_to_standalone_v1` + REVOKE pair ; corrective `_005339` hard-delete parent (orphan SKU bug, products.sku UNIQUE GLOBAL) ; corrective `_005402` SKU collision pre-check sur `-PARENT` suffix ; `_005926` seed perms `products.variants.{read,write}` + types regen ; corrective `_012658` XOR partial-NULL fix sur variants soft-deleted (discovered via Wave 4 pgTAP T15) ; timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 26b used `20260523135820` (1 migration — `create_update_account_active_v1_rpc` ; timestamp clock-based MCP-assigned, conservé pour matcher `schema_migrations.version`, convention héritée S27). Session 26 used `20260603000010..026` (17 migrations Wave 1 DB hardening : `_010` `current_pb1_rate()` helper ; `_011` bump `create_sale_journal_entry` PB1 dynamic ; `_012` bump `create_purchase_journal_entry` fold PPN dans INVENTORY ; `_013` create `calculate_pb1_payable_v1` + drop `calculate_vat_payable` ; `_014` bump `create_sale_journal_entry` split par order_payments.method ; `_015` seed cash_movement mapping keys ; `_016` bump `record_cash_movement_v2` + drop v1 ; `_017` bump `get_profit_loss_v1` dedupe ; `_018` bump `get_balance_sheet_v1` dedupe ; `_019` seed 3200 Retained Earnings ; `_020` reclass 5910 class 6 ; `_021` deactivate 1151 NON-PKP ; `_022` `close_fiscal_period_v1` ; `_023` `get_general_ledger_v1` ; `_024` `get_trial_balance_v1` ; `_025` `create_manual_je_v1` ; `_026` seed accounting cockpit permissions). Session 27b used `20260520101735..102709` (9 migrations : `_101735` create_product_v1 RPC + `_101749` REVOKE pair ; `_101810` create_category_v1 + `_101830` update_category_v1 + `_101850` reorder_categories_v1 + `_101902/13/24` 3× REVOKE pairs ; corrective `_102709` fix reorder_categories_v1 ambiguous-id). Session 27 used `20260520022207..025140` (18 migrations rapatriées du cloud — timestamps clock-based assignés par le MCP de la session précédente, conservés pour matcher `schema_migrations.version` : `_022207` ALTER products +5 cols ; `_022244` `_022314` `_022341` 3 tables ; `_022404` JSONB col ; `_022442` seed ; `_023035` `_023314` `_023419` `_023543` 4 RPCs ; `_023915` `_023932` `_023945` `_023957` REVOKE pair ×4 ; `_024026` `_024052` `_024113` 3 perms seed ; `_025140` corrective). Session 25 used `20260602000010..099` (6 migrations applied : `10_create_tablet_order_idempotency_keys_table` + `11_bump_create_tablet_order_v2` + `12_revoke_anon_create_tablet_order_v2` + corrective `13_alter_default_privileges_revoke_from_public` + corrective `14_relax_orders_session_id_for_tablet` + corrective `15_fix_refund_order_rpc_v2_replay_record`). Session 24 used `20260601000005..099` (11 migrations applied : `005/006/007` enum + relax `orders.session_id` ; `010` `b2b_payments` ledger ; `011/012` views `view_b2b_invoices` + `view_ar_aging` ; `013` REVOKE UPDATE `customers.b2b_current_balance` ; `014` seed `B2B_PAYMENT_BANK` ; `020/021/022` 3 RPCs). Session 19 used `20260523000001..099` (7 migrations applied : Thread A `10_create_record_rate_limit_v1_rpc` + `11_schedule_rl_purge_cron` + corrective `12_fix_record_rate_limit_v1_race` ; Thread B `19_audit_logs_add_payload` + `20_add_session_timeout_to_roles` + `21_create_update_role_session_timeout_v1_rpc` + corrective `22_fix_update_role_session_timeout_v1_revoke_anon`). Session 18 used `20260522000001..099` (1 migration applied : `10_create_recipe_cost_history_v1_rpc`). Session 17 used `20260521000001..099` (7 migrations applied : 6 planned + 1 corrective `21_fix_recipe_bom_full_v1_numeric_cast`). Session 16 used `20260520000001..099` (8 planned). Session 15 used `20260519000001..162` (32 migrations). Session 14 used `20260518000001..003`. Session 13 used `20260517xxxxxx`. Keep numbering monotonic — check `supabase/migrations/` before picking the next number.

## Project Conventions (The Breakery ERP)

### Critical patterns — don't break these
- **DB target is Supabase cloud, NOT local Docker** — As of 2026-05-14, Docker / local supabase stack is **retired** on this machine. All migrations, RPCs, pgTAP tests, and types regen run against the V3 dev project on the cloud: **`ikcyvlovptebroadgtvd`** (`the-breakery-v3-dev`, region `ap-southeast-1`, Pro plan $10/mo) — dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>. Apply migrations via `mcp__plugin_supabase_supabase__apply_migration`, run SQL via `execute_sql`, regen types via `generate_typescript_types`. **DO NOT run** `pnpm db:reset`, `supabase start`, `supabase db reset`, or `bash supabase/tests/run_pgtap.sh` — they require Docker and will fail. Prod (ref `abjabuniwkqpfsenxljp`) is V2 monolith and incompatible with V3 migration lineage.
- **PIN auth fetch wrapper** — the `auth-verify-pin` EF issues HS256 JWTs that GoTrue (ES256) can't validate via the default header. The Supabase client uses a custom fetch wrapper that injects the PIN JWT on every request via `setSupabaseAccessToken` (in `packages/supabase`). Never bypass with raw `Authorization` headers or `auth.setSession`.
- **Realtime channel names must be unique per mount** — StrictMode double-mounts components and shared channel names collide silently. See `apps/pos/src/features/kds/hooks/useKdsRealtime.ts`.
- **`packages/domain` is IO-free** — no `fetch`, no Supabase, no React. Pure TS, unit-testable.
- **Order writes go through RPCs** — never raw inserts. RPCs: `complete_order_with_payment_v10` (called server-side by the `process-payment` EF — the POS does NOT call it directly; `apps/pos/src/features/payment/hooks/useCheckout.ts:124` POSTs the EF, which calls the RPC at `supabase/functions/process-payment/index.ts:149`), `pay_existing_order_v6`, `create_tablet_order_v2`, `pickup_tablet_order` (unversioned), `evaluate_promotions_v1`, `mark_item_served` (unversioned). They handle JE triggers, loyalty, promotions, table state atomically. RPC versions are monotonic — verify `supabase/migrations/` + the call-site before relying on a version.
- **`stock_movements` is an append-only ledger** — RLS revokes UPDATE/DELETE for `authenticated`. All writes go through SECURITY DEFINER RPCs (`record_stock_movement_v1` primitive ; `adjust_stock_v1`, `receive_stock_v1`, `record_incoming_stock_v1`, `waste_stock_v1`, future `*_transfer_v1` / `record_production_v1` / `finalize_opname_v1`). Never `INSERT INTO stock_movements` directly from app code or tests.
- **`stock_movements.unit` is NOT NULL** — any direct insert (tests, fixtures, RPCs) must populate `unit`. The `record_stock_movement_v1` primitive auto-resolves from `products.unit` if NULL is passed — don't bypass it. See migration `20260516000019_fix_record_stock_movement_v1_unit.sql`.
- **`stock_movements` section constraint is movement-type-aware** — `transfer_in/out` require both `from_section_id` AND `to_section_id` ; `adjustment*`, `waste`, `incoming`, `purchase`, `sale*`, `production*`, `opname*` require at least one (relaxed in migration `20260516000020`). Don't tighten without re-checking all RPCs.
- **Inventory RPCs accept `p_idempotency_key UUID`** — replay returns the existing movement row instead of doubling it. Always pass one from the client on retry-able mutations.
- **RPC versioning is monotonic** — never edit a published `_vN` signature. Create `_vN+1` and `DROP FUNCTION ... vN(<old args>)` in the same migration if replacing. See `20260516000019` (drop original `record_stock_movement_v1` then recreate with `unit`).
- **Supabase auto-grants EXECUTE on public functions to `anon`** — `REVOKE ALL FROM PUBLIC` does NOT cancel it. Always add an explicit `REVOKE EXECUTE ... FROM anon` on admin-only RPCs to enforce gate intent at the role level (defense in depth). See S19 migration `20260523000022` for an example.
- **Anon GRANT defense-in-depth (S20)** — `REVOKE ALL FROM anon ON public.*` is the project-wide default for tables, views, AND functions, future-proofed via `ALTER DEFAULT PRIVILEGES FOR ROLE postgres`. Critical caveat: `REVOKE EXECUTE ... FROM anon` on functions is INSUFFICIENT on its own — `anon` inherits EXECUTE through PUBLIC membership via the `=X/postgres` ACL entry. Future REVOKE-on-functions migrations MUST also `REVOKE EXECUTE ... FROM PUBLIC` and `ALTER DEFAULT PRIVILEGES FOR ROLE postgres ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC`. See S20 migrations `20260524000020..031`. `supabase_admin`-owned extension objects (pgtap: `pg_all_foreign_keys`, `tap_funky`, pgtap helper functions) are platform-managed and not user-revocable — pgTAP suite excludes them. If a future feature legitimately needs anon (public landing-page RPC, embeddable widget), grant explicitly per-object with `COMMENT ON FUNCTION ... IS 'anon-callable: <reason>'`.
- **PIN / auth secrets en header HTTP, jamais en body JSON (S25)** — Any EF that consumes a manager PIN or other validation secret MUST read it from a dedicated HTTP header (e.g., `x-manager-pin`), NEVER from the JSON body. Rationale : request bodies get logged by default in PostgREST access logs, pgaudit, reverse proxies, and Supabase function logs ; headers are far less commonly captured. Hard cutover pattern : drop the body field in the SAME commit as the header read — no dual-mode fallback unless the EF has uncontrolled external callers (rare on this project, only POS calls these EFs). Reference : S25 `refund-order` migration body `manager_pin` → header `x-manager-pin` (`supabase/functions/refund-order/index.ts`). Sweep **DONE** : `void-order` + `cancel-item` hardened S34/PR #53 ; `kiosk-issue-jwt` verified compliant S36 (DEV-S36-A-01) ; all three read `x-manager-pin` from the header.
- **Idempotency 2-flavors selon la sémantique (S25)** — Two distinct patterns coexist on this project ; pick the right one for the flow :
  1. **HTTP `x-idempotency-key` header (EF retry safety)** — For HTTP requests where the client may legitimately retry (flaky network, double-click, React-Query auto-retry). The client generates a UUID v4 (`crypto.randomUUID()` stored in a `useRef` so it survives re-renders), sends it via header. The EF reads via `getIdempotencyKey(req)` from the shared helper `supabase/functions/_shared/idempotency.ts` (1 export, validates UUID regex, returns `string | null`) and propagates it as `p_idempotency_key` arg to the RPC. Reference : S25 `refund-order` EF + `refund_order_rpc_v2` (the RPC already had the arg from S13, only the EF + POS wiring was missing).
  2. **RPC arg `p_client_uuid` / `p_idempotency_key` (idempotence sémantique métier)** — For RPCs where idempotency is intrinsic to the business flow (e.g., "this cart, this tap" for tablet, "this payment record" for B2B, "this stock movement record" for inventory). The arg is REQUIRED at the RPC level (NOT NULL CHECK) and used as the primary key of a **dedicated** idempotency-keys table (never as a nullable column on the business table — isolation makes REVOKE simpler and avoids polluting the metric tables). Concurrency race handled via PK `unique_violation` catch + re-read. References : `create_tablet_order_v2(p_client_uuid)` with `tablet_order_idempotency_keys` (S25), `record_b2b_payment_v1(p_idempotency_key)` with `b2b_payments.idempotency_key UNIQUE` (S24), `record_stock_movement_v1` & family (S12). Replay returns the result of the first successful execution — by convention, RPCs return either the exact same value or an envelope `{ ..., idempotent_replay: true }` so callers can audit.

### Git
- Branches: `swarm/session-N` for ongoing session work, `feat/<scope>` or `fix/<scope>` for focused PRs. For phased plans, prefer `swarm/session-N` and squash-merge per phase.
- Commits: conventional commits (`feat(scope): …`, `fix(scope): …`, `test(scope): …`, `docs(scope): …`, `refactor(scope): …`). For session 13 phases: `feat(db|domain|ui|backoffice|pos|edge|inventory): session 13 — phase X.Y — <topic>`. Co-author Claude when AI-assisted.

## Agent Comms (SendMessage-First Coordination)

Named agents coordinate via `SendMessage`, not polling or shared state.

```
Lead (you) ←→ architect ←→ developer ←→ tester ←→ reviewer
              (named agents message each other directly)
```

### Spawning a Coordinated Team

```javascript
// ALL agents in ONE message, each knows WHO to message next
Agent({ prompt: "Research the codebase. SendMessage findings to 'architect'.",
  subagent_type: "researcher", name: "researcher", run_in_background: true })
Agent({ prompt: "Wait for 'researcher'. Design solution. SendMessage to 'coder'.",
  subagent_type: "system-architect", name: "architect", run_in_background: true })
Agent({ prompt: "Wait for 'architect'. Implement it. SendMessage to 'tester'.",
  subagent_type: "coder", name: "coder", run_in_background: true })
Agent({ prompt: "Wait for 'coder'. Write tests. SendMessage results to 'reviewer'.",
  subagent_type: "tester", name: "tester", run_in_background: true })
Agent({ prompt: "Wait for 'tester'. Review code quality and security.",
  subagent_type: "reviewer", name: "reviewer", run_in_background: true })

// Kick off the pipeline
SendMessage({ to: "researcher", summary: "Start", message: "[task context]" })
```

### Patterns

| Pattern | Flow | Use When |
|---------|------|----------|
| **Pipeline** | A → B → C → D | Sequential dependencies (feature dev) |
| **Fan-out** | Lead → A, B, C → Lead | Independent parallel work (research) |
| **Supervisor** | Lead ↔ workers | Ongoing coordination (complex refactor) |

### Rules

- ALWAYS name agents — `name: "role"` makes them addressable
- ALWAYS include comms instructions in prompts — who to message, what to send
- Spawn ALL agents in ONE message with `run_in_background: true`
- After spawning: STOP, tell user what's running, wait for results
- NEVER poll status — agents message back or complete automatically

## Swarm & Routing

### Config
- **Topology**: hierarchical-mesh (anti-drift)
- **Max Agents**: 15
- **Memory**: hybrid
- **HNSW**: Enabled
- **Neural**: Enabled

```bash
npx @claude-flow/cli@latest swarm init --topology hierarchical --max-agents 8 --strategy specialized
```

### Agent Routing

| Task | Agents | Topology |
|------|--------|----------|
| Bug Fix | researcher, coder, tester | hierarchical |
| Feature | architect, coder, tester, reviewer | hierarchical |
| Refactor | architect, coder, reviewer | hierarchical |
| Performance | perf-engineer, coder | hierarchical |
| Security | security-architect, auditor | hierarchical |

### When to Swarm
- **YES**: 3+ files, new features, cross-module refactoring, API changes, security, performance
- **NO**: single file edits, 1-2 line fixes, docs updates, config changes, questions

### 3-Tier Model Routing

| Tier | Handler | Use Cases |
|------|---------|-----------|
| 1 | Agent Booster (WASM) | Simple transforms — skip LLM, use Edit directly |
| 2 | Haiku | Simple tasks, low complexity |
| 3 | Sonnet/Opus | Architecture, security, complex reasoning |

## Memory & Learning

### Before Any Task
```bash
npx @claude-flow/cli@latest memory search --query "[task keywords]" --namespace patterns
npx @claude-flow/cli@latest hooks route --task "[task description]"
```

### After Success
```bash
npx @claude-flow/cli@latest memory store --namespace patterns --key "[name]" --value "[what worked]"
npx @claude-flow/cli@latest hooks post-task --task-id "[id]" --success true --store-results true
```

### MCP Tools (use `ToolSearch("keyword")` to discover)

| Category | Key Tools |
|----------|-----------|
| **Memory** | `memory_store`, `memory_search`, `memory_search_unified` |
| **Bridge** | `memory_import_claude`, `memory_bridge_status` |
| **Swarm** | `swarm_init`, `swarm_status`, `swarm_health` |
| **Agents** | `agent_spawn`, `agent_list`, `agent_status` |
| **Hooks** | `hooks_route`, `hooks_post-task`, `hooks_worker-dispatch` |
| **Security** | `aidefence_scan`, `aidefence_is_safe`, `aidefence_has_pii` |
| **Hive-Mind** | `hive-mind_init`, `hive-mind_consensus`, `hive-mind_spawn` |

### Background Workers

| Worker | When |
|--------|------|
| `audit` | After security changes |
| `optimize` | After performance work |
| `testgaps` | After adding features |
| `map` | Every 5+ file changes |
| `document` | After API changes |

```bash
npx @claude-flow/cli@latest hooks worker dispatch --trigger audit
```

## Agents

**Core**: `coder`, `reviewer`, `tester`, `planner`, `researcher`
**Architecture**: `system-architect`, `backend-dev`, `mobile-dev`
**Security**: `security-architect`, `security-auditor`
**Performance**: `performance-engineer`, `perf-analyzer`
**Coordination**: `hierarchical-coordinator`, `mesh-coordinator`, `adaptive-coordinator`
**GitHub**: `pr-manager`, `code-review-swarm`, `issue-tracker`, `release-manager`

Any string works as a custom agent type.

### Project agents & skills (The Breakery — adaptés au projet)

Au-delà des agents génériques ci-dessus, le repo versionne une **équipe spécialisée** (PR #55). Les **agents** (`.claude/agents/*.md`) sont spawnables via la tâche Agent ; les **skills** (`.claude/skills/<name>/SKILL.md`) s'auto-déclenchent par `pathPatterns`/`promptSignals` quand on touche leur domaine. Chacun pointe vers ce CLAUDE.md comme source de vérité et vérifie le schéma réel (migrations/MCP) avant d'affirmer un fait.

**Agents** (`.claude/agents/`) :
- `pos-specialist` (sonnet) — `apps/pos/`
- `backoffice-specialist` (sonnet) — `apps/backoffice/`
- `db-engineer` (sonnet) — `supabase/migrations/` + RPCs (versioning, REVOKE pairs, MCP V3)
- `edge-functions-engineer` (sonnet) — `supabase/functions/` (PIN header, idempotency, rate-limit)
- `pattern-guardian` (sonnet, **read-only**) — revue de diff vs les Critical patterns ci-dessous
- `test-engineer` (sonnet) — pgTAP/Vitest/smoke + baseline env-gated
- `session-coordinator` (**opus**) — orchestration `swarm/session-N` (spec→plan→waves→closeout)

**Skills** (`.claude/skills/`, auto-déclenchés) :
- `stock-management` — inventory/recipes/production/WAC/lots
- `accounting` — COA/JE/PB1 NON-PKP/fiscal/GL/TB · `b2b-credit` — AR/credit-limit/b2b_payments
- `reports-exports` — report RPCs/PDF/CSV/Z-report/drill-down · `expense-governance` — thresholds/SOD/multi-step
- `products-catalog` — products CRUD/variants/categories · `orders` — lifecycle/list v2/edit-items/void/refund
- `security-auth` — RLS/REVOKE/perms/PIN-JWT/rate-limit · `breakery-ui-kit` — conventions `packages/ui`

Conception : `docs/superpowers/specs/2026-05-31-agents-skills-team-design.md` (spec) + `docs/superpowers/plans/2026-05-31-agents-skills-team.md` (plan). `.gitignore` versionne les `.md` racine de `.claude/agents/` ; les sous-dossiers ruflo restent ignorés.

## Build & Test

- ALWAYS run tests after code changes
- ALWAYS verify build succeeds before committing
- This project uses **pnpm 9.15** + **turbo** — never `npm`

### Local commands (no Docker required)
```bash
pnpm build && pnpm test     # turbo run build / turbo run test --concurrency=1
pnpm typecheck               # turbo run typecheck
```

### DB workflow — Supabase cloud staging (Docker retired 2026-05-14)
All DB operations target the cloud V3 dev project `ikcyvlovptebroadgtvd`. **Do NOT run** `pnpm db:reset`, `supabase start`, or `bash supabase/tests/run_pgtap.sh` — Docker is gone.

| Operation | MCP tool | Notes |
|---|---|---|
| Apply migration | `mcp__plugin_supabase_supabase__apply_migration` | `project_id='ikcyvlovptebroadgtvd'`, `name` in snake_case, body = SQL. Wrapped in transaction. |
| Run SQL (incl. pgTAP) | `mcp__plugin_supabase_supabase__execute_sql` | Use `BEGIN ... ROLLBACK` envelope for pgTAP. Extension `pgtap` already enabled. |
| Regen types | `mcp__plugin_supabase_supabase__generate_typescript_types` | Returns `{ types: "..." }` — write to `packages/supabase/src/types.generated.ts` and commit. |
| Check drift | `mcp__plugin_supabase_supabase__list_migrations` | Compares `supabase_migrations.schema_migrations` to local. |
| Direct psql (rare) | `postgresql://postgres.ikcyvlovptebroadgtvd:<URL_ENCODED_PWD>@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres` | Always go through the pooler — `db.<ref>.supabase.co` has no DNS A record. |

Dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>

### Targeted iteration (much faster than full suite during phase work)
```bash
pnpm --filter @breakery/supabase test inventory     # Vitest live RPC tests
pnpm --filter @breakery/backoffice test inventory   # BO smoke + unit
pnpm --filter @breakery/domain test inventory       # pure-TS unit
```

After Supabase schema changes (new migration via MCP `apply_migration`), **always** regen types via `mcp__plugin_supabase_supabase__generate_typescript_types`, write to `packages/supabase/src/types.generated.ts`, and commit. A missing regen is the #1 cause of broken CI on this repo.

### Inventory phase test layout
- pgTAP (DB): `supabase/tests/inventory.test.sql` (steady-state suite) + `supabase/tests/inventory_phase1_complete.test.sql` (phase 1 acceptance — T1-T15+).
- Vitest live RPC: `supabase/tests/functions/inventory-*.test.ts` (per-phase, one file per RPC family).
- Domain unit: co-located `__tests__/` in `packages/domain/src/inventory/`.
- BO smoke/unit: co-located `__tests__/` in `apps/backoffice/src/features/inventory*/`.

## CLI Quick Reference

```bash
npx @claude-flow/cli@latest init --start-all         # Setup (already done)
npx @claude-flow/cli@latest swarm init --v3-mode     # Start swarm
npx @claude-flow/cli@latest memory search --query "" # Vector search
npx @claude-flow/cli@latest hooks route --task ""    # Route to agent
npx @claude-flow/cli@latest doctor --fix             # Diagnostics
npx @claude-flow/cli@latest security scan            # Security scan
npx @claude-flow/cli@latest performance benchmark    # Benchmarks
```

26 commands, 140+ subcommands. Use `--help` on any command for details.

## Setup

```bash
claude mcp add claude-flow -- npx -y @claude-flow/cli@latest
npx @claude-flow/cli@latest daemon start
npx @claude-flow/cli@latest doctor --fix
```

**Agent tool** handles execution (agents, files, code, git). **MCP tools** handle coordination (swarm, memory, hooks). **CLI** is the same via Bash.
