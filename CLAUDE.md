# Ruflo — Claude Code Configuration

## Rules

- Do what has been asked; nothing more, nothing less
- NEVER create files unless absolutely necessary — prefer editing existing files
- NEVER create documentation files unless explicitly requested
- NEVER save working files or tests to root — this is a pnpm/turbo monorepo: code goes in `apps/{pos,backoffice}/src`, `packages/{domain,supabase,ui,utils}/src`, or `supabase/{functions,migrations,tests}`. Co-locate tests in `__tests__/` next to the code.
- ALWAYS read a file before editing it
- NEVER commit secrets, credentials, or .env files
- Keep files under 500 lines
- Validate input at system boundaries

## Active Workplan

> Read this **before** opening code on Session 20+ work.

- **Current session:** Session 34 — Station Ticket Printing ✓ ready to merge `swarm/session-34`. Spec : [`docs/workplan/specs/2026-06-01-session-34-station-printing-spec.md`](docs/workplan/specs/2026-06-01-session-34-station-printing-spec.md), Plan : [`docs/workplan/plans/2026-06-01-session-34-station-printing-plan.md`](docs/workplan/plans/2026-06-01-session-34-station-printing-plan.md), INDEX : [`docs/workplan/plans/2026-06-01-session-34-station-printing-INDEX.md`](docs/workplan/plans/2026-06-01-session-34-station-printing-INDEX.md).
- **Session 34 reference:** Station ticket printing for the POS — prep tickets routed per product category to barista/kitchen/bakery thermal printers; bill printing (pre-payment addition) to cashier/waiter; auto-fire of unprinted prep items on checkout; receipt routing to cashier printer. Architecture mock-first (`VITE_PRINT_MOCK=1` in CI; external print-bridge `localhost:3001` at runtime — **DEV-S34-W0-02, bridge deployment deferred S35**). 5 printer roles (`barista`, `kitchen`, `bakery` prep + `cashier`, `waiter` document) resolved via `lan_devices.capabilities->>'station'` from new `useStationPrinters`. Domain: `groupItemsByStation(items, stationByProductId)` (pure, IO-free). POS: `printService.ts` (`printStationTicket` + `printReceipt` + mock buffer), `useFireToStations` mutation (concurrent per-station fire, markLocked+markPrinted on success), `usePrintBill` (whole-order bill, re-printable), `SendToKitchenButton` rewired, `PrintBillButton` new, `PaymentTerminal` auto-fire + SuccessModal receipt→cashier. cartStore: `printedItemIds` + `markPrinted` + `unprintedItems()`. **1 migration** `20260601043059` (data-only: UPDATE categories dispatch_station — Beverage→barista, Sandwiches→kitchen, Pastry/Bread→bakery). **Tests** : pgTAP `category_station_remap` 7/7 PASS via cloud MCP + 5 POS smoke files 7/7 PASS + `pnpm --filter @breakery/app-pos typecheck` PASS. **Business pivot** (DEV-S34-W0-01): KDS on-screen station displays deferred S35 — replaced with hardware printers. **Hors scope S35+** : bridge deployment, KDS screens, receipt enhancements (method/tax/drawer), openCashDrawer error surfacing, cashierPrinter cold-query pre-warm, S33 backlog (BO+POS smokes, refund from BO, edit other fields, mobile responsive, CF drill, UnifiedReportFilters, compare toggle, hub KPI/favorites, 6 Soon cards). Deviations tracked in INDEX §9 (4 entries, all informational).
- **Session 33 reference:** Closes 4 trous post-S32 sur `/backoffice/orders` (observe → filter → control) : RPC `get_orders_list_v1→v2` server-side filters (refund_status / hour / terminal_id via JOIN `pos_sessions` post col add), realtime channel `useOrdersRealtime` (postgres_changes INSERT+UPDATE, StrictMode-safe via useId), `useVoidOrder` BO + `VoidOrderModal` (body PIN per DEV-S33-PRE-02 — void-order EF non-hardened S25), edit items sur open/draft/pending_payment orders via 3 RPCs atomiques (`add_order_item_v1`, `update_order_item_qty_v1`, `remove_order_item_v1`) + helper interne `_recalc_order_totals` + idempotency table dédiée `order_edit_idempotency_keys`. Orchestrateur BO `useEditOrderItems` séquence removes→updates→adds avec idempotency keys propres par RPC. `EditOrderItemsModal` 2-col (product picker stub V1 + cart preview live). Bonus : `pos_sessions.terminal_id UUID NULL REFERENCES lan_devices(id)` + POS `useOpenShift` bumpé (INSERT direct, pas d'RPC) + OpenShiftModal terminal selector + localStorage `pos:last_terminal_id` pre-select. **15 migrations** block `20260618000010..023` + cloud-clock corrective `20260529200749` (13 obligatoires + 2 correctives). 2 nouvelles perms (`orders.edit_open` + `orders.void`). **Tests ~29 PASS** : pgTAP `orders_list_v2` 10/10 + `order_edit_items` 12/12 + `pos_session_terminal` 3/3 via cloud MCP + BO unit `useOrdersList` 4/4 + typecheck 3/3 packages. **Correctives in-flight** : (1) CTE scope bug — original v2 référençait CTE `filtered` dans 2 SELECTs séparés (42P01) → fusionné en SELECT INTO multi-target ; (2) status enum check `'open'` n'existe pas dans `order_status` enum (actual: draft, paid, voided, pending_payment, completed, b2b_pending) → fixé en `('draft', 'pending_payment')` + `products.retail_price` (pas `.price`). **Hors scope S34+** : BO + POS smoke test suites (DEV-S33-4-SKIP-01), refund actions from BO, void-order EF header-PIN refactor, ProductPicker pour EditOrderItemsModal, edit autres champs open orders (customer/notes/table), mobile responsive, NOT NULL promotion `pos_sessions.terminal_id` post-backfill, concurrent edit conflict detection, realtime merge in-place, CF account drill (DEV-S32-1.D-01), UnifiedReportFilters extra dims, compare toggle S30 reports, hub mini-KPI + favorites, 6 Soon cards restantes. Deviations tracked in INDEX §9 (9 entries, 1 medium DEV-S33-4.2-01 fixed via corrective `_023`).
- **Previous session:** none — `master` clean @ `4aa61df` (post-merge S32 PR #40). Next session (S34+) opens with S33 follow-ups : BO+POS smoke tests, refund from BO, edit-other-fields, mobile responsive, plus the unchanged S33 backlog list (CF account drill, UnifiedReportFilters, compare toggle, hub KPI/favorites, 6 Soon cards).
- **Backoffice menu reorg (2026-05-27):** Refactor standalone livré sur `feat/bo-menu-reorg` (post-S32) — 3 groupes (Operations/Management/Admin) → 7 groupes (Operations/Sales/Purchase/Stock/Finance/Reports/Settings) avec 9 sous-sections visuelles internes. 3 drops (POS/KDS/New user) + 8 renames (Product Categories, Customer Categories, B2B Credit Settings, Cash Closing, Live Movements, Stock Movement History, RBAC Editor, Permissions Matrix). Aucune URL ne change. **Wave 8 addendum** : les 9 sous-groupes nommés (Finance > Expenses/Accounting ; Reports > 5 ; Settings > Devices/Users & Access) sont cliquables/dépliables, défaut all-collapsed, persistance navigateur via localStorage `bo:sidebar:subgroups`. Spec : [`docs/workplan/specs/2026-05-27-backoffice-menu-reorg-spec.md`](docs/workplan/specs/2026-05-27-backoffice-menu-reorg-spec.md). Plan : [`docs/workplan/plans/2026-05-27-backoffice-menu-reorg-plan.md`](docs/workplan/plans/2026-05-27-backoffice-menu-reorg-plan.md). Addendum collapsible : [`docs/workplan/plans/2026-05-27-backoffice-menu-reorg-addendum-collapsible.md`](docs/workplan/plans/2026-05-27-backoffice-menu-reorg-addendum-collapsible.md).
- **Session 32 reference:** Session 32 — Reports Vague C close-out drill-down + /backoffice/orders list ✓ merged 2026-05-27 via PR #40 (commit `780e12e`) on `swarm/session-32` (~24 commits, 4 waves, 4 migrations `20260617000010..014`, INDEX: [`docs/workplan/plans/2026-05-26-session-32-INDEX.md`](docs/workplan/plans/2026-05-26-session-32-INDEX.md), spec: [`docs/workplan/specs/2026-05-26-session-32-spec.md`](docs/workplan/specs/2026-05-26-session-32-spec.md), plan: [`docs/workplan/plans/2026-05-26-session-32-plan.md`](docs/workplan/plans/2026-05-26-session-32-plan.md)). Base `master` @ `c74e295` (post-merge S31 PR #39). Ferme les 7 reports "terminal documentés" de S31 — 4 accounting (P&L, BS, CF*, PB1) + StockMovementHistory product drill + PaymentByMethod + SalesByHour. **2 bumps DB additifs** (`get_profit_loss_v1` + `get_balance_sheet_v1` exposent `account_id` UUID en plus du `code` 3-4 digit — CF déféré S33+ DEV-S32-1.D-01 car la méthode indirecte n'a pas de lignes per-account natives). **1 nouvelle RPC** `get_orders_list_v1(p_start DATE, p_end DATE, p_filters JSONB, p_limit INT, p_cursor TEXT)` SECURITY DEFINER cursor-paginée avec filtres JSONB unique (10 axes : status / order_type / customer_id / served_by / payment_method / customer_type / total_min/max / refund_status* / hour* — \* post-fetch client-side V1 ; `terminal_id` dropped V1 car col absente DEV-S32-1.A-01). REVOKE pair S25 canonique. **1 nouveau hook réutilisable** `useAccountIdByCode(code)` dans `features/accounting/hooks/` (24h cache, résout COA code → UUID client-side, futur consumer pour cards single-account KPI). **`buildDrilldownUrl` étendu** avec entity `'order_list'` filter-only (id ignoré, URL = base + filter ; 18/18 unit PASS, +5 nouveaux). **1 nouvelle page** `/backoffice/orders` audit-grade (URL state = source of truth, 9 colonnes, infinite scroll cursor-based, barre filtres 9 contrôles + chips actifs, sidebar entry Management group icon ShoppingBag gate `orders.read`). **GeneralLedgerPage** accepte `?account_id=&start=&end=` URL params (Wave 3.A). **7 drill-down wirings** : P&L code → GL ; BS — nouvelle "Per-account detail" table sous le 3-col A=L+E ; PB1 payable KPI → GL acc 2110 ; StockMovementHistory product → ProductDetail ; PaymentByMethod → /orders?payment_method ; SalesByHour — nouvelle "Per-hour detail" table sous le BarChart → /orders?hour. **Schema discoveries vs spec** : `orders.terminal_id` n'existe pas (DEV-S32-1.A-01) ; `customers.name` (pas `full_name`, DEV-S32-1.A-02) ; `refunds.total` (pas `amount`, DEV-S32-1.A-03). **Tests 40 PASS** : 11 pgTAP (2 `accounting_account_id_exposed` + 9 `orders_list_v1`) via cloud MCP + 18 unit `buildDrilldownUrl` + 2 unit `useOrdersList` + 3 smoke `GeneralLedgerPage` + 3 smoke `OrdersListPage` + 6 smoke drill wirings + 5 S31 non-régression. `pnpm typecheck` 6/6 PASS. **Hors scope S33+** : CF account drill (refactor RPC indirect→direct method), `get_orders_list_v1` v2 server-side filters (refund_status, hour, terminal_id post-schema-bump), OrdersListPage refund/void actions + "+ New Order" button + mobile responsive, UnifiedReportFilters extra dims, compare toggle reports S30, hub mini-KPI + favorites, 6 Soon cards restantes. Deviations tracked in INDEX §10 (10 entries, all informational).
- **Session 31 reference:** Session 31 — Reports Drill-Down + 3 detail pages ✓ merged 2026-05-26 via PR #39 (commit `c74e295`) on `swarm/session-31` (~17 commits, 4 waves, 1 migration `20260616000010`, INDEX: [`docs/workplan/plans/2026-05-22-session-31-INDEX.md`](docs/workplan/plans/2026-05-22-session-31-INDEX.md), spec: [`docs/workplan/specs/2026-05-22-session-31-spec.md`](docs/workplan/specs/2026-05-22-session-31-spec.md), plan: [`docs/workplan/plans/2026-05-22-session-31-plan.md`](docs/workplan/plans/2026-05-22-session-31-plan.md)). Base `master` @ `60a1ff3` (post-merge S30 PR #38). **Premier chantier Vague C** : drill-down navigation transverse sur les 17 reports BO + 3 nouvelles detail pages minimales read-only (`customers/:id`, `orders/:id`, `inventory/recipes/:productId`). Composant entity-aware `<DrilldownLink entity={...} id={...} filter={...} />` consume helper pur `buildDrilldownUrl(entity, id, filter)` (13/13 unit PASS). 3 detail pages utilisent direct SELECT PostgREST + reuse RPC `recipe_bom_full_v1` (S17) — pas de nouvelle RPC. 1 migration `_010` seed permission `orders.read` (MANAGER+/ADMIN+/SUPER_ADMIN). **Wiring** : 13/17 reports avec drill-down actif (sales/stock/production groupes 3.A/3.B/3.C) ; 4 reports accounting (P&L, BS, CF, PB1) + PaymentByMethod + SalesByHour + Pb1 + RecipeCostTimeline = **terminal documentés** car RPCs retournent `code` 3-4 digit au lieu de UUID (GL S26b attend UUID) — RPC bumps deferred S32+. **Schema discoveries vs spec** : `customer_type` enum `retail|b2b` (pas walk_in/account) ; `orders.total` + `served_by` (pas total_amount/created_by) ; `order_items.name_snapshot` + `modifiers` (pas product_name/modifiers_json) ; table `refunds` (pas order_refunds) ; "Recipe" est M2M product↔ingredient (1 row par ingrédient) — route keyed sur `:productId` (output product) pas `:id` ; `recipe_bom_full_v1` retourne TABLE flat (pas tree). **Tests ~27 PASS** : 13 unit `buildDrilldownUrl` + 3 component smoke + 6 detail pages (CustomerDetail 2/2 + OrderDetail 2/2 + RecipeDetail 2/2) + 5 wiring sample smoke + 2 pgTAP `orders_read_perm`. `pnpm typecheck` 6/6 PASS. **12 tests pré-S31 retroactively patched** (script Python idempotent) pour wrap `<MemoryRouter>` — sans ça `<Link>` du DrilldownLink crashe sur `useContext(...)` car les tests existants rendent les pages directement. **Hors scope S32+** : `/backoffice/orders` list page filtrable (débloquerait PaymentByMethod + SalesByHour drill), RPC bumps accounting (P&L/BS/CF/StockMovements pour exposer UUIDs), actions sur detail pages (refund/void/edit), mobile responsive, UnifiedReportFilters extra dims, compare toggle sur reports S30, hub mini-KPI bar + favorites/pinning, 6 Soon cards restantes. Deviations tracked in INDEX §10 (9 entries, all informational).
- **Session 30 reference:** Session 30 — Vague B : 5 bakery reports ✓ merged 2026-05-25 via PR #38 (commit `60a1ff3`) on `swarm/session-30` (15 commits, 10 migrations block `20260615000010..019` + `20260524231049..124`, INDEX: [`docs/workplan/plans/2026-05-24-session-30-INDEX.md`](docs/workplan/plans/2026-05-24-session-30-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-30-spec.md`](docs/workplan/specs/2026-05-24-session-30-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-30-plan.md`](docs/workplan/plans/2026-05-24-session-30-plan.md)). Base `master` @ `d14cf9b` (post-merge S29 PR #37). Livre 5 nouveaux reports métier promus depuis "Soon" cards du hub : **Wastage & Spoilage** (manual_waste + auto spoilage stock_lots.expired aggregated by product + lines top 500), **Payment by Method** (split cash/card/qris/edc/transfer/store_credit + by_day pivoted + share_pct), **VAT/PB1 Report** monthly NON-PKP (reuses `current_pb1_rate()` + `calculate_pb1_payable_v1` S26 helpers + balance on account `2110` PB1 Payable), **Stock Movement history** cursor-paginé (filters product_id + movement_type, infinite query BO pattern S13 AuditLog), **Perishable Turnover** (per-product velocity_score 1-5 bucket basé sur avg_days_in_stock vs shelf_life_p50, waste_pct = expired/(consumed+expired)*100). Réutilise infra S29 (ExportButtons générique, EF generate-pdf 12→17 templates, helpers buildCsv/downloadCsv/previousPeriod). **5 RPCs SECURITY DEFINER + REVOKE pairs canoniques S25** : `get_wastage_report_v1(text, text)`, `get_payments_by_method_v1(text, text)`, `get_pb1_report_v1(int, int)`, `get_stock_movements_v1(text, text, uuid, text, int, timestamptz)`, `get_perishable_turnover_v1(text, text)`. Aucune nouvelle perm seedée — réutilise `reports.inventory.read` (3 reports) + `reports.financial.read` (2 reports). **EF generate-pdf v2** : registry extended 12→17 templates (5 nouveaux dans `_shared/pdf-templates/` : `wastage.ts`, `payment_by_method.ts`, `pb1.ts`, `stock_movements.ts`, `perishable_turnover.ts`). **BO** : 5 hooks (`useWastageReport`, `usePaymentsByMethod`, `usePb1Report` month/year params, `useStockMovementsReport` infinite cursor pattern, `usePerishableTurnover`) + 5 pages avec ExportButtons (Wastage/PaymentByMethod/Pb1/StockMovementHistory CSV-only/PerishableTurnover) + 5 routes PermissionGate + 5 sidebar entries indent 1 sous Reports + ReportsIndexPage hub passes 13→18 active cards (4 Soon promus + 1 nouveau Perishable Turnover card). **Tests** : pgTAP `bakery_reports.test.sql` **15/15 PASS** via cloud MCP (T1-T15 : perm gate + shape + filter + clamp + score range) + regression S29 zreports 14/14 + S28 expense_governance 18/18 PASS + BO smoke 5/5 PASS + typecheck 6/6 PASS. **Hors scope S31+** : Vague C (drill-down navigation `<DrilldownLink>`, UnifiedReportFilters extra dims category/terminal/customer, compare toggle sur ces 5 reports, mobile responsive, hub mini-KPI bar, favorites/pinning, 6 Soon cards restantes : Daily Sales/Purchase×3/Staff Performance/Production Report/Production Efficiency/Price Changes/Permission Change Log). Backlog Vague D+ : Scheduled email TASK-14-008, Unusual Transactions TASK-14-013, Custom report builder TASK-14-007. Deviations tracked in INDEX §12 (8 entries, all informational).
- **Session 29 reference:** Session 29 — Reports Export + Z-Report PDF (Vague A) ✓ ready to merge `swarm/session-29` (30 commits, 14 migrations block `20260606000010..023`, INDEX: [`docs/workplan/plans/2026-05-24-session-29-INDEX.md`](docs/workplan/plans/2026-05-24-session-29-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-29-spec.md`](docs/workplan/specs/2026-05-24-session-29-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-29-plan.md`](docs/workplan/plans/2026-05-24-session-29-plan.md)). Base `master` @ `66f77d6` (post-merge S28 PR #36). Closes **TASK-14-005** (compare toggle vs période précédente — DONE) + **TASK-12-002** (Z-Report PDF signable — DONE) + gap 14-3 (CSV/PDF uniforme sur toutes les pages reports — DONE). Architecture **flow 2-temps Z-Report** : `close_shift_v2` insère draft `z_reports` row avec snapshot JSONB figé (orders + payments + refunds + expenses du shift) → EF `generate-zreport-pdf` génère PDF async via pdf-lib + upload bucket `zreports/` 7 ans (conformité Indonésie, service_role) → manager signe depuis BO via `<SignZReportModal>` (PIN `x-manager-pin` header S25 pattern) + `sign_zreport_v1` idempotency replay. Flow non-bloquant côté POS : si PDF échoue, draft row reste en DB pour retry BO. **3 RPCs** : `sign_zreport_v1(p_zreport_id)` SECURITY DEFINER gate `zreports.sign` + audit_log `zreport.signed` + idempotent replay ; `void_zreport_v1(p_zreport_id, p_reason)` gate `zreports.sign` + reason ≥ 10 chars ; `get_zreport_snapshot_v1(p_zreport_id)` gate `zreports.read` ; `close_shift_v2` bump (non-blocking pdf EF call via `pg_net`). **2 EFs** : `generate-zreport-pdf` (idempotent `x-idempotency-key` obligatoire, S25 flavor 1, 7-yr bucket) ; `generate-pdf` 12 templates (pnl/bs/cf/tb/recipes/inventory/sales/opname/expenses/zreport/customers/audit) rate-limit 30/min durable, `reports-exports/` TTL 30d. **Domain helpers** : `buildCsv<T>(rows, columns, opts?)` TDD 9/9 (RFC 4180 + UTF-8 BOM + id-ID locale IDR) ; `previousPeriod(range) → DateRange` + `formatDelta(curr, prev) → string` TDD 9/9 (calendar-aware month vs n-day fallback). **BO** : nouvelle page `ZReportsListPage` + 5 components (`SignZReportModal` PIN 6 digits header + `VoidZReportModal` textarea + `ZReportRow` + `ZReportStatusBadge` + `ZReportPreviewDrawer`) + 6 hooks (`useZReports`, `useSignZReport`, `useVoidZReport`, `useGenerateZReportPdf`, `useExportCsv`, `useGeneratePdf`) + `<ExportButtons>` générique câblé 13 pages + `<DateRangePickerWithCompare>` + `<DeltaPct>` câblés 5 reports (P&L, BS, CF, SalesByHour, SalesByCategory). **POS** : `useCloseShift` bumped v1→v2 (EF chain non-blocking). **Permissions seedées (3)** : `zreports.{read, sign, void}`. **Tests 42/42 PASS** : pgTAP `zreports.test.sql` **14/14 PASS** via cloud MCP + BO smoke ~30 PASS + POS smoke ~5 PASS + domain unit 18/18 PASS + Vitest live EF **12 cas** authored env-gated (3 fichiers : `generate-pdf.test.ts` GP1-GP5, `generate-zreport-pdf.test.ts` ZP1-ZP4, `sign-zreport.test.ts` SZ1-SZ3). `pnpm typecheck` 6/6 PASS. **Hors scope S30+** : Vague B (5 nouveaux reports bakery), Vague C (drill-down + UnifiedReportFilters extra dims + mobile responsive), `pg_net` non-blocking EF call in close_shift_v2 (currently synchronous), B2B invoice PDF (TASK-09-001 deferred). Deviations tracked in INDEX §13 (22 entries, 1 medium DEV-S29-1.D-03 fixed via corrective, 21 informational).
- **Session 28 reference:** Session 28 — Expense Governance ✓ ready to merge `swarm/session-28` (31 commits, 14 migrations + 2 correctives = block `20260524111854..123026`, INDEX: [`docs/workplan/plans/2026-05-24-session-28-INDEX.md`](docs/workplan/plans/2026-05-24-session-28-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-28-spec.md`](docs/workplan/specs/2026-05-24-session-28-spec.md), plan: [`docs/workplan/plans/2026-05-24-session-28-plan.md`](docs/workplan/plans/2026-05-24-session-28-plan.md)). Base `swarm/session-27c` @ `0f1ad11` (S27c not yet merged — PR sequence S27c → S28). Ferme **TASK-11-001** (Workflow approbation multi-niveau — DONE complet) + gaps 11-1 (SOD), 11-2 (multi-step chain), 11-3 (cash sync), 11-4 (settings page). Architecture **snapshot-at-submit** retenue : on fige les règles d'approbation au moment du submit dans `expenses.required_approval_steps_snapshot JSONB` — pattern emprunté à `complete_order_v9` (tax_rate snapshot) + `recipe_versions.snapshot` (S15+S17). Un changement de seuil admin n'invalide pas une expense en cours de chaîne. **2 nouvelles tables** : `expense_approval_thresholds` (configurable per-category, 3 seeded defaults : [0-100k auto-approve], [100k-1M 1-step MANAGER], [1M-9.9G 2-step MANAGER + ADMIN/SUPER_ADMIN]) ; `expense_approvals` append-only audit (UNIQUE(expense_id, approver_user_id) enforce SOD — un user ne peut pas approuver 2 steps du même expense). **3 nouvelles cols sur `expenses`** : `required_approval_steps_snapshot JSONB NULL` (legacy NULL = workflow v1 fallback), `current_approval_step SMALLINT NOT NULL DEFAULT 0`, `auto_approved BOOLEAN NOT NULL DEFAULT false`. **5 RPCs** : `submit_expense_v2(p_expense_id, p_idempotency_key)` (bump v1, threshold resolution `ORDER BY category_id NULLS LAST LIMIT 1` + snapshot freeze + auto-approve path) ; `approve_expense_v2(p_expense_id)` (bump v1, SOD block 1 `created_by != caller` + SOD block 2 UNIQUE expense_approvals approver + multi-step chain + legacy NULL fallback 1-step + JE emit on final step) ; `set_expense_threshold_v1(p_threshold_id, p_category_id, p_amount_min, p_amount_max, p_steps)` (UPSERT admin-gated avec step shape + range + overlap validation) ; `delete_expense_threshold_v1(p_threshold_id)` ; trigger function `sync_cash_expense_to_session()` (AFTER UPDATE OF status ON expenses WHEN paid+cash → `pos_sessions.cash_out_total += amount + vat_amount` ; no-block fallback `audit_log expense.cash_paid_no_session` si pas de session open). **1 helper interne** : `_emit_expense_je(uuid)` extrait de `approve_expense_v1` (REVOKEd from authenticated — appelé uniquement par v2 RPCs SECURITY DEFINER). Tous avec gate `expenses.create`/`expenses.approve`/`expenses.thresholds.write` + audit_log canonical cols (`actor_id, action, entity_type, entity_id, metadata`) + REVOKE pair S25 canonique. **PIN-en-header (S25 pattern)** appliqué à `approve_expense_v2` via `useApproveExpense` hook (header `x-manager-pin`). **Idempotency** appliquée à `submit_expense_v2` via `useRef(crypto.randomUUID())` dans `useSubmitExpense` (S25 2-flavors flavor 2). **Permissions seedées (2)** : `expenses.thresholds.{read,write}` pour MANAGER+/ADMIN+ respectivement. **BO** — nouvelle page `/settings/expense-thresholds` avec sidebar entry icon `Scale` ; 4 nouveaux components (`ThresholdFormDialog` create/edit avec steps builder native HTML — `@breakery/ui` n'exporte pas Select/SelectItem ; `ApprovalTimeline` stepper vertical avec project semantic tokens ; `ThresholdResolutionBadge` Badge primitive ; `ApproveDialog` bumped SOD-aware + PIN input swap notes textarea) ; 5 hooks (`useExpenseThresholds`, `useSet/DeleteExpenseThreshold`, `useExpenseApprovals`, bumped `useSubmit/ApproveExpense` v2) ; `ExpenseDetailPage` wire badge + timeline. **Tests 26/26 PASS** : pgTAP `expense_governance.test.sql` **18/18 PASS** via cloud MCP (T1-T18 covering auto-approve / multi-step chain / SOD blocks / overlap validation / category override / cash sync / legacy fallback / boundary inclusive / REVOKE verification) + **BO smoke 8/8 PASS** (3 fichiers : expense-thresholds-page + approval-timeline + approve-dialog-sod). `pnpm typecheck` 6/6 PASS. **Hors scope S29+** : TASK-11-003 recurring expenses, TASK-11-004 OCR receipt (BLOCKED Capacitor), TASK-11-005 cost centers, TASK-11-006 per diem/advances, notifications email/in-app aux approvers (toast UI only), reject workflow étendu (reste 1-shot v1), migration des expenses historiques (fallback NULL snapshot préservé). **VAT path** : `_emit_expense_je` préserve la logique v1 mais `vat_amount > 0` raise P0002 sur `resolve_mapping_account('EXPENSE_VAT_INPUT')` car le compte 1151 est désactivé S26 ADR-003 NON-PKP — non-régression de S28. Deviations tracked in INDEX §12 (9 entries, 3 medium tous fixés via correctives 1.A.1 / 2.A.1 + 1 medium informational sur audit_logs cols).
- **Session 27c reference:** Product Variants ✓ ready to merge `swarm/session-27c` (34 commits, 19 migrations block `20260524002129..012658`, INDEX: [`docs/workplan/plans/2026-05-24-session-27c-INDEX.md`](docs/workplan/plans/2026-05-24-session-27c-INDEX.md), spec: [`docs/workplan/specs/2026-05-24-session-27c-spec.md`](docs/workplan/specs/2026-05-24-session-27c-spec.md)). Base `master` @ `cab4ce3` (post-merge PR #35 turbo bump 2.9.14). Ferme **TASK-05-003** (Variants tab placeholder S27/S27b) + **S27b §7 follow-up #2** (deferred variants schema + UI). Architecture **Linked-Products** retenue : on étend la table `products` existante avec 4 cols nullables (`parent_product_id`, `variant_label`, `variant_axis`, `variant_sort_order`) + ENUM `variant_axis_type` ('flavor' | 'size' | 'format') + CHECK XOR + trigger `enforce_variant_no_nesting` (BEFORE INSERT/UPDATE empêche un variant d'être lui-même parent — 1 niveau max). 3 cases pour un product : standalone (inchangé), parent (ne se vend pas directement — POS affiche modal au tap), variant (enfant, propre SKU/prix/stock). **6 SECURITY DEFINER RPCs** : `convert_product_to_parent_v1` (standalone → parent, crée 1er variant) ; `create_variant_v1` (allowlist 14 cols clone-from-parent + overrides) ; `update_variant_v1` (allowlist 6 cols patch) ; `delete_variant_v1` (soft via `is_active = false` — never hard, FK orders.order_items) ; `reorder_variants_v1(p_parent_id, p_variant_ids[])` (assigns sort_order 10/20/30... + complete-coverage gate) ; `convert_parent_to_standalone_v1` (parent → standalone, garde 1er variant + hard-delete parent + soft-delete autres variants). Tous avec gate `products.variants.write` + audit_log + REVOKE pair S25 canonique. **3 correctives DB** : Wave 2.G `_005339` hard-delete parent (soft laissait orphan SKU — `products.sku` UNIQUE GLOBAL pas partial) ; Wave 2.H `_005402` SKU collision pre-check sur suffix `-PARENT` (évite 23505 unique violation mi-transaction) ; Wave 2.I `_012658` XOR partial-NULL fix sur variants soft-deleted (NULLait juste `parent_product_id`, laissait les 3 autres cols → violation CHECK 23514 ; discovered via Wave 4 pgTAP T15). **Permissions seedées (2)** : `products.variants.{read, write}` pour MANAGER/ADMIN/SUPER_ADMIN. **BO** — Le `<StubPanel module='Variants' />` du ProductDetailPage est remplacé par `<VariantsPanel>` 3-case switch consumant 8 hooks (2 read `useProductVariants` + `useProductParent` ; 6 write 1-per-RPC) ; 5 nouveaux components : `VariantsPanel`, `ConvertToParentDialog` (3-button axis fallback car @breakery/ui n'a pas `<RadioGroup>`), `AddVariantDialog`, `DissolveParentDialog`, `VariantRowSortable` (DnD @dnd-kit) ; ProductsFilters extension filter variant + badge parent/variant dans Grid/Table. **POS** — `useProducts` filtre variants out (parents s'affichent avec badge "Variants") via 2-query fallback (PostgREST relation embed non-élégant pour self-reference) ; nouveau `<VariantSelectModal>` Radix Dialog plein écran tablet (wiré dans `ProductTapHandler.tsx`, pas dans ProductGrid — seam plus propre) ; domain `Product` type widened +2 optional fields (`parent_product_id?`, `has_variants?`). **Tests 34/34 PASS** : pgTAP `product_variants.test.sql` **20/20 PASS** via cloud MCP (T1-T15 RPC happy/perm/edge cases + trigger anti-nesting + XOR fix + 5 audit_log asserts) + **BO smoke 10/10 PASS** (5 fichiers : variants-panel-empty/parent/variant + convert-to-parent-dialog + products-list-filter) + **POS smoke 4/4 PASS** (2 fichiers : variant-select-modal + pos-grid-hides-variants). `pnpm typecheck` 6/6 PASS (8s avec cache) ; full BO sweep 97 files / 322 tests PASS ; full POS sweep 62 files / 326 tests PASS — **aucune régression**. **Hors scope S27d** : bulk operations variants, UnitsPanel write mode (RPC `set_product_units_v1` existe depuis S27 catch-up mais pas consumer BO), Sections editor BO, `delete_category_v1`, pgTAP `update_category_v1`, NewProductDialog Zod, fully-optimistic reorder, edit-variant inline button (hook livré, UI déférée), modifier inheritance parent→variant, KDS variant rendering, receipt template variants. Deviations tracked in INDEX §11 (17 entries, 3 medium tous fixés via correctives 2.G/2.H/2.I — le reste informational).
- **Session 26b reference:** Comptable Cockpit UI core ✓ merged ready on `swarm/session-26b` (5 commits feat + 1 spec, 1 migration `20260523135820`, INDEX: [`docs/workplan/plans/2026-05-23-session-26b-INDEX.md`](docs/workplan/plans/2026-05-23-session-26b-INDEX.md), spec: [`docs/workplan/specs/2026-05-23-session-26b-spec.md`](docs/workplan/specs/2026-05-23-session-26b-spec.md)). Base `master` @ `6b796b7` (post-merge PR #31 S26 Wave 1 DB hardening). Livré **6 pages BO cockpit comptable** + **1 nouvelle RPC** (`update_account_active_v1` SECURITY DEFINER, gate `accounting.coa.write` SUPER_ADMIN only, audit_log + idempotent no-op — ferme R1/DEV-S26b-1.A-01 RLS UPDATE indisponible sur `accounts`) consommant les 4 RPCs + 6 permissions seedées par S26 Wave 1.I. **Wave 1 ChartOfAccounts** : page + filter class + search + toggle inline. **Wave 2 JournalEntries** : page avec table 6 cols + drilldown Sheet drawer + modal saisie OD (Dialog stepper 2 steps — header + lines table balanced check + PIN 6 digits). **Wave 3 GeneralLedger** : account selector + date range + lines avec `running_balance` calculé client-side (delta selon `balance_type`) + Load more via `next_cursor`. **Wave 4 TrialBalance** : table + badge balanced + CSV export (`buildTrialBalanceCsv` UTF-8 BOM + Intl id-ID `1.500.000`). **Wave 5 FiscalPeriod** : `useFiscalPeriods` + `useCloseFiscalPeriod` + `FiscalPeriodModal` stepper 2 steps (period + lock checkbox + PIN) + `SettingsAccountingPage` (status badge color-coded open/closed/locked). **Wave 6 wiring** : 5 nouvelles routes `/accounting/*` + 1 route `/settings/accounting` + 4 entries sidebar (BookOpen ClipboardList LineChart Scale) + entry sidebar Settings 'Fiscal Periods' + `AccountingIndexPage` 4-tiles hub (replace `<ComingSoonPage module='Accounting' />` placeholder). **Tests** : pgTAP `update_account_active_v1.test.sql` 4/4 PASS via cloud MCP (T1 happy SUPER_ADMIN ; T2 MANAGER P0003 ; T3 not found P0002 ; T4 audit_log row) + 6 BO smoke files 12/12 PASS (`pnpm --filter @breakery/app-backoffice test accounting` sweep 7 fichiers 15/15 incluant 3 S13 mappings). **Hors scope S26c** : PB1ManagementPage, ARAgingPage, BankReconciliationPage, CALK SAK EMKM, vrai keyset paginate JE (LIMIT 200 ordered DESC MVP), docs reference rebase Partie II. **Pre-existing** : typecheck `@breakery/ui` fail (env install incomplet `@dnd-kit/*`/`recharts`/`sonner` — reproduit sur master, non lié S26b). Closes TASK-10-011 (DONE — UI), TASK-10-016 (DONE — modal). Deviations tracked in INDEX §10 (14 informational, aucune medium).
- **Session 26 reference:** Comptable Cockpit Wave 1 (DB hardening NON-PKP) ✓ merged 2026-05-20 via PR #31 (commit `6b796b7`) on `swarm/session-26` (10 commits, 17 migrations block `20260603000010..026`, INDEX: [`docs/workplan/plans/2026-05-20-session-26-INDEX.md`](docs/workplan/plans/2026-05-20-session-26-INDEX.md), spec: [`docs/workplan/specs/2026-05-20-session-26-spec.md`](docs/workplan/specs/2026-05-20-session-26-spec.md), ADR: [`docs/adr/003-pkp-status-non-pkp.md`](docs/adr/003-pkp-status-non-pkp.md)). **Ratification business 2026-05-20** : The Breakery est **NON-PKP** (PB1 10% sortie PEMDA Bali, PPN 11% input supplier non récupérable → fold dans inventory cost). Option B « start clean » ratifiée : pas de rejouage historique des JE purchase (V3 jamais déployée prod). **DB hardening Wave 1** ferme 11 findings audit V3 + ADR-003 : `current_pb1_rate()` helper (lit `business_config.tax_rate`) + bump `create_sale_journal_entry` PB1 dynamic + split par `order_payments.method` (1110/1115/1116) ; bump `create_purchase_journal_entry` fold PPN supplier dans INVENTORY_GENERAL (1130) ; `calculate_pb1_payable_v1` (drop `calculate_vat_payable`) NON-PKP simplifié `pb1_payable = pb1_output` ; `record_cash_movement_v2` (drop v1) émet JE selon `p_reason_code` (apport_owner → DR 1110/CR 3100 ; bank_transfer → DR/CR 1110↔1112 ; replenishment/misc/NULL → no JE) + 2 nouveaux mapping keys (`CASH_MOVEMENT_OWNER_CAPITAL` → 3100, `CASH_MOVEMENT_BANK` → 1112) ; dedupe `sale_void`+`sale_refund` dans `get_profit_loss_v1` + `get_balance_sheet_v1` (option a — exclude sale_void quand refund existe pour même order_id) ; COA cleanup (seed 3200 Retained Earnings class 3 equity + reclasser 5910 Cash Variance Loss class 5→6 expense + désactiver 1151 VAT Input NON-PKP avec name "VAT Input — RESERVED"). **4 RPCs cockpit** : `close_fiscal_period_v1(p_period_id, p_manager_pin, p_lock)` PIN+perm gate ; `get_general_ledger_v1(p_account_id, p_start, p_end, p_limit=50, p_cursor)` cursor-based paginate avec opening_balance + lines + totals + next_cursor ; `get_trial_balance_v1(p_start, p_end)` tous comptes actifs avec sum DR/CR + balanced flag ; `create_manual_je_v1(p_description, p_entry_date, p_lines, p_manager_pin)` saisie OD avec validation stricte (account_id résolus + is_active + is_postable, lines ≥ 2, balanced, debit XOR credit). **Permissions seedées (6)** : `accounting.{period.close, je.create_manual, gl.read, tb.read, coa.read, coa.write}` + role_permissions pour MANAGER/ADMIN/SUPER_ADMIN. **POS hook** : `useCashMovement.ts` migré v1→v2 avec optional `reason_code: CashMovementReasonCode` ('apport_owner' | 'bank_transfer' | 'replenishment' | 'misc'). **Tests** : pgTAP `s26_db_hardening.test.sql` **15/15 PASS** via cloud MCP (couvre 1.B→1.I). Types regen × 2 (post 1.F + post 1.I). **Scope déféré S26b/S26c** : Waves 2-5 (9 pages UI cockpit + smoke tests + docs ref rebase) — voir INDEX §8 pour justification du split DB-first / UI-after. **Pre-existing**: `pnpm typecheck` `@breakery/ui` fail (env install incomplet, `@testing-library/user-event` pas dans node_modules — reproduit sur master sans modifs S26, non lié). Closes TASK-10-011 (PARTIAL — backend done), TASK-10-016 (PARTIAL — RPC livré, modal à venir). Deviations tracked in INDEX §9.
- **Session 27 + 27b reference:** Product CRUD : update + create + categories ✓ merged 2026-05-20 via PR #30 on `claude/continue-session-27-5iall` (10 commits across 2 sub-sessions, 27 migrations total (18 catch-up + 9 new), INDEX S27: [`docs/workplan/plans/2026-05-20-session-27-INDEX.md`](docs/workplan/plans/2026-05-20-session-27-INDEX.md), INDEX S27b: [`docs/workplan/plans/2026-05-20-session-27b-INDEX.md`](docs/workplan/plans/2026-05-20-session-27b-INDEX.md)). **S27b add-on** (this afternoon) closes the rest of the originally-planned S27 scope: `create_product_v1(jsonb)` 21-col allowlist + SKU uniqueness check + seeds product_unit_contexts ; `create_category_v1` auto-slugify ; `update_category_v1` 6-col patch ; `reorder_categories_v1(uuid[])` assigns sort_order=10,20,... with complete-coverage gate (caught a 42702 ambiguous-id bug via pgTAP T7 → corrective `_102709`) ; 4× REVOKE pair S25 canonique. **BO** : `useCreateProduct` + `NewProductDialog` (name/sku/cat/unit/retail/desc + SKU auto-uppercase + perm gate `products.create`) wired into ProductsHeader "+ New Product" pill ; new `/backoffice/categories` route + sidebar entry (gated `categories.read`) + `CategoriesPage` with `@dnd-kit` DnD reorder (pointer + keyboard sensors + rollback-on-error) + inline toggle active + `CategoryFormDialog` (create/edit). **Tests** : pgTAP `product_category_crud.test.sql` 10/10 PASS cloud ; BO smoke `new-product-dialog.smoke.test.tsx` 2/2 PASS ; S27 smoke regression 2/2 still PASS ; typecheck 6/6 PASS. **Scope déferé S27c** : bulk operations, variants schema + UI, UI consumers pour `set_product_units_v1` / `set_product_sections_v1` / `upsert_product_modifiers_v1` (RPCs existent depuis S27 catch-up mais Variants/Units/sections restent stubs), `delete_category_v1`, pgTAP `update_category_v1`. Closes TASK-05-002 (DONE), TASK-05-006 (DONE), TASK-05-007 (DONE).
- **Session 27 reference:** Product CRUD : update flow MVP ✓ ready to merge `claude/continue-session-27-5iall` (6 commits, 4 phases, 18 catch-up migrations + 5 pgTAP + 2 BO smoke, INDEX: [`docs/workplan/plans/2026-05-20-session-27-INDEX.md`](docs/workplan/plans/2026-05-20-session-27-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-20-session-27-spec.md`](docs/workplan/specs/2026-05-20-session-27-spec.md). **Situation inhabituelle :** une session précédente avait appliqué 18 migrations à la base V3 dev cloud (timestamps `20260520022207..025140`) sans les commit en git — cette session **rattrape l'écart cloud↔git** + **finit le wiring UI** pour `update_product_v1`. **DB catch-up** (18 migrations mirrorées avec les timestamps cloud actuels pour matcher `schema_migrations.version`) — `_022207` ALTER products +5 settings cols (`description`, `visible_on_pos`, `available_for_sale`, `track_inventory`, `deduct_stock`) ; `_022244..341` 3 nouvelles tables (`product_unit_alternatives`, `product_unit_contexts`, `product_sections` M2M) ; `_022404` product_modifiers +ingredients_to_deduct JSONB ; `_022442` seed alts+contexts ; `_023035..543` 4 RPCs SECURITY DEFINER (`update_product_v1(uuid,jsonb)`, `set_product_units_v1`, `set_product_sections_v1`, `upsert_product_modifiers_v1`) ; `_023915..957` REVOKE pair S25 canonique ×4 ; `_024026..113` seed perms `products.{modifiers,sections,units}.update` ; `_025140` corrective set_product_sections_v1 primary reassign two-step. **Types** : regen MCP + `PermissionCode` étendu avec 3 perms. **BO** — nouveau hook `useUpdateProduct` (ProductUpdatePatch matche le 18-col allowlist) + `useProductDetail`/`useProducts` SELECT étendus aux 8 nouvelles colonnes + `ProductRow` enrichi + `ProductDetailPage` wiring (controlled patch state, dirty flag, save success reset, perm gate `products.update` via authStore) + `GeneralPanel` re-sync draft on prop change + 5 toggles controlled (role="switch") + description controlled + `ProductDetailHeader` isSaving prop + data-testid. **Tests** — pgTAP `update_product_v1.test.sql` 5/5 PASS cloud (T1 happy path, T2 CASHIER 42501, T3 not found P0002, T4 cost_price ignored, T5 audit_logs ×2) ; BO smoke `product-detail-save.smoke.test.tsx` 2/2 PASS (disabled-when-clean + RPC call assertion) ; `pnpm typecheck` 6/6 PASS. **Scope déferé S27b** : `create_product_v1` + new product flow, categories CRUD + DnD page, bulk operations, variants tab. Closes TASK-05-002 (PARTIAL — update done, create deferred). Deviations tracked in INDEX §6 (1 medium DEV-S27-SCOPE-01 = scope split into 27a/b).
- **Session 25 reference:** Hardening Idempotency Cross-EF ✓ ready to merge `swarm/session-25` (18 commits, 3 waves, 6 migrations, INDEX: [`docs/workplan/plans/2026-05-19-session-25-INDEX.md`](docs/workplan/plans/2026-05-19-session-25-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-19-session-25-spec.md`](docs/workplan/specs/2026-05-19-session-25-spec.md). Closes the 2 mutating-flow gaps flagged by S23 audit §2 (refund PIN in body JSON + `p_idempotency_key` not propagated ; `create_tablet_order` v1 has no `client_uuid`). **DB** — block `20260602000010..015` : `010` `tablet_order_idempotency_keys` table dédiée (PK = `client_uuid`, REVOKE all from anon/authenticated, GRANT SELECT only) ; `011` `create_tablet_order_v2(p_client_uuid UUID, ...)` SECURITY DEFINER avec replay sur PK lookup + `EXCEPTION WHEN unique_violation` re-read + drop v1 dans la même migration (CLAUDE.md RPC versioning rule) ; `012` REVOKE EXECUTE `create_tablet_order_v2` FROM anon ; corrective `013` `ALTER DEFAULT PRIVILEGES FOR ROLE postgres REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` (now the canonical defense-in-depth template for new RPCs — pairs `_012` + `_013` together) ; corrective `014` relax `orders.session_id` NOT NULL pour `created_via='tablet'` (latent S24 bug : S24 `_006` relaxed only for `order_type='b2b'`, broke tablet flow silently — caught by pgTAP T1) ; corrective `015` fix dormant S13 RECORD-not-assigned bug in `refund_order_rpc_v2` replay branch (line 90 wrote to `v_order.order_number` on unassigned RECORD — only surfaced once S25 wired `p_idempotency_key` end-to-end, S13→S25 dormant). **EF** — new `supabase/functions/_shared/idempotency.ts` helper (`getIdempotencyKey(req, { required? })` + `MissingIdempotencyKeyError` + `InvalidIdempotencyKeyError`) ; `refund-order` EF v7 deployed to V3 dev cloud — PIN body → header `x-manager-pin` (**hard cutover**, no dual-mode), `p_idempotency_key` propagé via header `x-idempotency-key` au RPC `refund_order_rpc_v2` (existant S13), log `audit_logs.action='refund.replay'` sur replay hit. **POS** — 6 files : `useRefundOrder.ts` + `RefundOrderModal.tsx` (headers + `useRef(crypto.randomUUID())` lifecycle reset on dismiss) ; `useCreateTabletOrder.ts` signature `clientUuid: string` requis + RPC v2 ; `TabletOrderPage.tsx` + `TabletCheckoutButton.tsx` chacun owns son propre `clientUuidRef = useRef(crypto.randomUUID())` reset on success. **Tests** : pgTAP `idempotency_hardening.test.sql` 8/8 PASS via cloud MCP ; Vitest live `idempotency-hardening.test.ts` 5 scénarios TS1-TS5 authored (live run requires `SUPABASE_SERVICE_ROLE_KEY` env var, refund EF v7 deployed to V3 dev) ; POS smoke `tablet-send-idempotent.smoke.test.tsx` 2/2 + `refund-modal-pin-header.smoke.test.tsx` 2/2 PASS ; `pnpm typecheck` 6/6 PASS. **CLAUDE.md Critical patterns block enrichi** : nouveau bullet "PIN/auth secrets en header HTTP (pas en body JSON)" + nouveau bullet "Idempotency 2-flavors selon la sémantique" (header HTTP retry-safety vs RPC arg pour idempotence sémantique métier). Closes TASK-17-002 (DONE — online side), TASK-03-001/002 refund EF wing, gaps audit S23 03-1 / 03-2 / 17-1. Sweep of other manager-PIN EFs (`void-order`, `cancel-item`, `kiosk-issue-jwt`) deferred to backlog post-S30. Deviations tracked in INDEX §10.
- **Session 24 reference:** B2B Foundation ✓ merged 2026-05-19 via PR #27 (commit `1749d92`) on `swarm/session-24` (12 commits, 3 waves, 11 migrations, INDEX: [`docs/workplan/plans/2026-05-19-session-24-INDEX.md`](docs/workplan/plans/2026-05-19-session-24-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-19-session-24-spec.md`](docs/workplan/specs/2026-05-19-session-24-spec.md). Closes the audit gap on module 09 (surface UI shipped S14 without backend). **DB** — block `20260601000005..022` : `005/006/007` extend `order_type`/`order_status` enums + relax `orders.session_id` NOT NULL (CHECK gates on `order_type='b2b'`) ; `010` `b2b_payments` ledger (append-only, RLS, REVOKE INSERT/UPDATE/DELETE, sequence `b2b_payment_seq` for `BP-YYYY-NNNN` numbering) ; `011/012` `view_b2b_invoices` + `view_ar_aging` (SECURITY INVOKER, buckets current/31-60/61-90/90+ over `CURRENT_DATE - orders.created_at`) ; `013` REVOKE UPDATE `customers.b2b_current_balance` for authenticated/anon/PUBLIC (pattern S22 `update_cost_price_v1`) ; `014` seed `B2B_PAYMENT_BANK` mapping (1112) ; `020` `record_b2b_payment_v1` (DR Cash/Bank / CR B2B_AR + idempotency via UNIQUE key + FIFO allocation metadata) ; `021` `adjust_b2b_balance_v1` (admin adjust with reason + idempotency via audit_logs.metadata) ; `022` `create_b2b_order_v1` (b2b_pending order + JE DR B2B_AR / CR SALE_B2B_REVENUE + **gate `validate_b2b_credit_limit_v1` — closes the S14 RPC-without-caller gap**). **UI BO** — `useB2bDashboard` aging KPI now reads `view_ar_aging` (no more `last_visit_at` proxy) ; new `CreateB2bOrderModal` activates dashboard "+ New B2B Order" button (closes D-W6-B2B-01) ; new `RecordB2bPaymentModal` + B2BPaymentsPage "Received" tab consuming `b2b_payments` ledger (closes D-W6-B2BPAY-01) ; surfaces credit_limit_exceeded payload (would_exceed_by) to UI alert. **Tests** : pgTAP `b2b_foundation.test.sql` 15 cas via cloud MCP, Vitest live `record-b2b-payment.test.ts` 5 scénarios, BO smoke `b2b-foundation.smoke.test.tsx` 3 cas (T1 aging KPI / T2 button enabled / T3 record_payment mutation). Closes TASK-09-001 (PARTIAL — KPI aging done, PDF/email deferred S29), TASK-09-002 (DONE — gate wired), TASK-09-006 (DONE — dashboard KPI fixed). Deviations tracked in INDEX §10.
- **Session 19 reference:** Hardening polish ✓ merged `swarm/session-19` (12 commits, 4 waves, 7 migrations, INDEX: [`docs/workplan/plans/2026-05-17-session-19-INDEX.md`](docs/workplan/plans/2026-05-17-session-19-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-17-session-19-spec.md`](docs/workplan/specs/2026-05-17-session-19-spec.md). Delivered three hardening threads : **Thread A** — durable Postgres-backed rate-limit (`record_rate_limit_v1` RPC + `pg_advisory_xact_lock` race fix + `pg_cron rl-purge` daily + `checkRateLimitDurable` wired + 5 EFs migrated — `auth-verify-pin`, `kiosk-issue-jwt` ×2 buckets, `refund-order`, `void-order`, `cancel-item` — closing TASK-01-002 follow-up from S13) ; **Thread B** — per-role session timeout (`roles.session_timeout_minutes INT NOT NULL DEFAULT 30 CHECK (5..480)` + per-role seed CASHIER 30 / waiter 30 / MANAGER 60 / ADMIN 120 / SUPER_ADMIN 240 + `update_role_session_timeout_v1` admin-gated audit-logged RPC + `useIdleTimeout` hook in `packages/ui` mounted in POS+BO + new `/settings/security` BO page + `auth-get-session` EF returns `session_timeout_minutes` — closing TASK-01-006) ; **Thread C** — PIN strength warn-only (`evaluatePinStrength` in `packages/utils` + Deno mirror with cross-package sync test + `auth-change-pin` extended `{ ok, weak, weak_reason? }` + BO `UserDetailPage` weak banner + new POS greenfield `ChangePinModal` 3-step + `SideMenuDrawer` "Change PIN" item + `useChangePin` hook — closing TASK-01-008). 7 migrations applied (block `20260523000010..022`, including 1 race-fix `12` and 1 corrective REVOKE `22`). Deviations tracked in INDEX §10.
- **Previous session:** Session 18 — Recipe Cost History Report ✓ merged 2026-05-17 on `swarm/session-18` (5 commits : `7924822`, `8851c70`, `423bad8`, `4f4c1a6`, `510ffdb`, 4 waves, 1 migration, INDEX: [`docs/workplan/plans/2026-05-17-session-18-INDEX.md`](docs/workplan/plans/2026-05-17-session-18-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-17-session-18-spec.md`](docs/workplan/specs/2026-05-17-session-18-spec.md). Delivered: Recipe Cost History Report — new RPC `recipe_cost_history_v1` (dual-mode) + 2 BO pages (Overview cross-recipe + Timeline single-recipe with recharts LineChart) + wiring (routes + sidebar + tile). Closes the read-side gap on S17's append-only `recipe_versions` history. Gated by `reports.financial.read`. 1 migration applied (block `20260522000010`). Deviations tracked in INDEX §10 (DEV-S18-1.A-01, 2.A-01/02, 2.B-01/02 — all informational).
- **Session 17 reference:** Full price chain ✓ merged 2026-05-17 via PR #21 (commit `5e79509`) on `swarm/session-17` (6 commits, 4 waves, 7 migrations, INDEX: [`docs/workplan/plans/2026-05-17-session-17-INDEX.md`](docs/workplan/plans/2026-05-17-session-17-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-17-session-17-spec.md`](docs/workplan/specs/2026-05-17-session-17-spec.md). Delivered: full price chain — PO receipt → WAC auto-update of `products.cost_price` → ancestor recipe snapshots cascade via `WITH RECURSIVE` walk → full-cascade `product_cost_at_version` (depth-5) + new `recipe_bom_full_v1` RPC replacing client-side BFS + `IngredientAggregatePreview` UI rewire — resolves `DEV-S16-2.B-01/03/04/05`, `DEV-S16-2.C-01/02` + new PO→cost requirement via WAC. 7 migrations applied (block `20260521000010..030`, including 1 corrective `21_fix_recipe_bom_full_v1_numeric_cast`). Deviations tracked in INDEX §10 (DEV-S17-1.A-01, 1.B-01, 1.C-01/02, 2.A-01).
- **Session 16 reference:** CI revival + S15 follow-ups ✓ merged 2026-05-16 via PR #20 (commit `f7c83b2`) on `swarm/session-16` (11 commits, 4 waves, 8 migrations, INDEX: [`docs/workplan/plans/2026-05-16-session-16-INDEX.md`](docs/workplan/plans/2026-05-16-session-16-INDEX.md)). Spec: [`docs/workplan/specs/2026-05-16-session-16-spec.md`](docs/workplan/specs/2026-05-16-session-16-spec.md). Delivered: dropped broken Docker `supabase-tests` job and added `.github/workflows/pgtap-nightly.yml` cron `0 19 * * *` UTC against V3 dev — resolves `DEV-S15-CI-01` (medium) ; `products.is_semi_finished` boolean flag + maintenance trigger + pg_trgm GIN indexes on `name`/`sku` + trigram ranking in `search_ingredients_v1` — resolves `DEV-S15-3.A-01/02` ; per-version recipe cost embedded in `recipe_versions.snapshot` (breaking shape change) + history UI cost column with legacy-row tolerance + CHECK constraint — resolves `DEV-S15-2.B-01` ; new `expandRecipeCascade` domain helper + BFS graph builder in `IngredientAggregatePreview` — resolves `DEV-S15-4.A-02`. 8 migrations applied (block `20260520000010..022`). Deviations tracked in INDEX §10 (DEV-S16-1.A-01, 2.A-01, 2.B-02, 2.C-01..02 — others resolved by S17).
- **Session 15 reference:** Bakery Production ✓ merged 2026-05-16 on `swarm/session-15` (53 commits, 6 waves, 32 migrations, INDEX: [`docs/workplan/plans/2026-05-15-session-15-INDEX.md`](docs/workplan/plans/2026-05-15-session-15-INDEX.md)). Delivered F6 sub-recipes (anti-cycle + cost cascade + recipe_versions + record_production cascade), F5 yield tracking, recipe pro features (IngredientPicker + DnD + Duplicate + Batch + Schedule + Margin alerts + Boulanger % + EU allergens). Deviations tracked in INDEX §13.
- **Session 14 reference:** UX completion ✓ merged `d7d60d5` on 2026-05-14 (68 commits, 6 waves). INDEX: [`docs/workplan/plans/2026-05-14-session-14-INDEX.md`](docs/workplan/plans/2026-05-14-session-14-INDEX.md).
- **Session 13 follow-ups (deferred Session 20+):** Playwright CI job (D-W6-6C-05), `pg_net`-based birthday cron (D-W6-6B-02), Cash Flow Investing/Financing sections (D-W6-6A-2), `mv_pl_monthly` branched reuse (D-W6-6A-1), staging-deploy.yml secrets (D-W6-CICD-01).
- **Session 15 follow-ups:** DEV-S15-5.C-01 (Receipt template + customer display allergen integration) — **WONTFIX per user decision 2026-05-17** (memory: `project_allergens_wontfix`) — no allergens module needed for receipt/display.
- **Session 16 follow-ups (deferred Session 20+):** Trigram indexes not yet engaged by RPC predicate (DEV-S16-2.A-01, would need `%` operator + `set_limit`) ; legacy `recipe_versions` rows have no cost reconstructible (DEV-S16-2.B-02) ; nightly pgTAP is the only automated check, no PR-time gate (DEV-S16-1.A-01).
- **Session 17 follow-ups (deferred Session 20+):** N-snapshots-in-one-tx for high-fanout recipe graphs (DEV-S17-1.A-01, informational, Breakery depth ≤ 5 well within bounds) ; manual `products.cost_price` UPDATE bypasses WAC and doesn't emit `stock_movements` audit row (DEV-S17-1.B-01) ; WAC applies uniformly to all purchase movements, no opt-out for sample stock / promo (DEV-S17-1.C-01, low) ; WAC garbage-in if `current_stock` stale (DEV-S17-1.C-02, informational) ; `expandRecipeCascade` has no current consumer in `apps/` — preserved as public `@breakery/domain` API (DEV-S17-2.A-01, informational, downgraded).
- **Session 18 follow-ups (deferred Session 20+):** RPC scans all `recipe_versions` per call — no index on `(product_id, created_at DESC)` ; at current cardinality (~13 products × ~3 versions) negligible (DEV-S18-1.A-01, informational) ; overview baseline lookup uses correlated subqueries (~2× planner work), could be rewritten with a window function (DEV-S18-2.A-01, informational ; note: implem uses correlated subquery, not LATERAL as originally written) ; CSV exports raw NUMERIC without locale formatting (DEV-S18-2.A-02, informational) ; timeline chart X-axis = raw ISO date strings, no locale formatting (DEV-S18-2.B-01, informational) ; no zoom interaction on chart (DEV-S18-2.B-02, informational).
- **Session 25 follow-ups (deferred Session 26+):** `_shared/idempotency.ts` UUID_REGEX accepts UUID v1-5 while the error message says "must be UUID v4" (harmless because POS uses `crypto.randomUUID()` which always returns v4 — tighten regex if strict v4 needed) (DEV-S25-1.A-01, informational) ; corrective `_013` `ALTER DEFAULT PRIVILEGES FROM PUBLIC` is currently a no-op since S20 global sweep already set it, but the `_012` + `_013` pair is now the canonical template for future REVOKE-EXECUTE migrations (DEV-S25-1.A-02, informational) ; silent-skip on unknown `product_id` in `create_tablet_order_v2` FOR loop (inherited from v1) — with idempotency the failure mode becomes permanently unrecoverable (first call creates truncated order, all retries replay truncated `order_id`), track for v3 to raise `Product % not found` (DEV-S25-1.A-03, **medium**) ; idempotency replay in `create_tablet_order_v2` bypasses `sales.create` permission check (deliberate, matches S24 `record_b2b_payment_v1` pattern — replay is read-only-like) (DEV-S25-1.A-04, informational) ; `TabletCheckoutButton.tsx` extended with the same `clientUuidRef` lifecycle as `TabletOrderPage` — not in spec but necessary for the 2nd call site (DEV-S25-1.A-05, informational) ; corrective `_014` relax `orders.session_id` for tablet caught dormant S24 bug — S24 `_006` relaxed only for `order_type='b2b'`, broke tablet flow silently until pgTAP T1 surfaced it (DEV-S25-1.A-06, **medium, fixed this session**) ; corrective `_015` `fix_refund_order_rpc_v2_replay_record` fixed a dormant S13→S25 bug where `refund_order_rpc_v2` line 90 wrote to `v_order.order_number` on an unassigned RECORD — fixed via CREATE OR REPLACE with local TEXT var, signature unchanged (DEV-S25-1.A-07, **medium, fixed this session**) ; Vitest live `idempotency-hardening.test.ts` structurally validated but actual live run requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars exported user-side — same project-wide gap as S19 DEV-S19-2.A-01 ; refund-order EF v7 deployed to V3 dev via MCP so live tests will exercise the right code (DEV-S25-2.A-01, informational) ; 13 backoffice test files fail with `Invalid environment variables: VITE_SUPABASE_URL Required` — pre-existing env-gating pattern, S25 touched zero backoffice files (DEV-S25-2.A-02, informational) ; pgTAP test file uses GUCs (`current_setting('breakery.t1_pass'::text)`) to chain pass/fail flags between DO blocks because pgTAP's `ok()` must be called from a SELECT, not a DO — portable pattern (DEV-S25-2.A-03, informational).
- **Session 19 follow-ups (deferred Session 20+):** RPC `record_rate_limit_v1` holds a row-lock during upsert — under sustained attack ≥100 req/s on the same bucket, attacker + defender serialize on the same bucket ; inconsequential at Breakery's traffic (DEV-S19-1.A-01, informational) ; fail-open on DB error is a deliberate trade-off — investigate pool sizing before reconsidering fail-closed (DEV-S19-1.A-02, informational) ; `audit_logs.entity_id` is NULL for `role.session_timeout_changed` rows — `role_code` lives in `payload` JSON instead (DEV-S19-1.B-01, informational) ; **medium** — `REVOKE FROM anon` is not implied by `REVOKE ALL FROM PUBLIC` on public-schema functions ; the corrective migration `20260523000022` adds explicit `REVOKE EXECUTE ... FROM anon` on `update_role_session_timeout_v1` and is now a project-wide critical pattern (DEV-S19-1.B-02, medium, see Critical patterns below) ; Vitest env vars unset in `supabase/tests/functions/*` mean the cross-instance RL simulation requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` exported manually (DEV-S19-2.A-01, informational) ; 429 responses lack `Retry-After` header — project-wide gap (DEV-S19-2.A-02, informational) ; `pin-strength.ts` duplicated in `packages/utils` and `supabase/functions/_shared` — sync test catches drift but doesn't prevent it ; consider build-time copy (DEV-S19-2.B-01, informational) ; top-100 leaked PIN list inlined as a literal array — manual refresh cadence (DEV-S19-2.B-02, informational) ; `COMMON_PINS` has 101 entries including dead string `'232425'` (DEV-S19-2.B-03, informational) ; `useIdleTimeout` fires `signOut()` immediately — no "you are about to be signed out" warning toast (DEV-S19-3.A-01, informational) ; pre-existing : `UserDetailPage` validates 4-8 digits, EF requires exactly 6 — one-line regex fix for S20+ (DEV-S19-3.B-01, informational) ; BO admin-reset uses `reset_user_pin_v1` RPC (not `auth-change-pin` EF) ; shared `evaluatePinStrength` util preserves single source of truth (DEV-S19-3.B-02, informational) ; POS used `NumpadPin` collection primitive instead of verification-only `PinPad` — minor spec wording deviation (DEV-S19-3.C-01, informational) ; hint surfaces at step 3 not step 2 (DEV-S19-3.C-02, informational, UX polish) ; mismatch resets to step 1 instead of conventional step 2 (DEV-S19-3.C-03, informational, UX polish).
- **Module reference (canonical):** [`docs/reference/04-modules/`](docs/reference/04-modules/) per module (e.g., `15-production-recipes.md` for production).
- **Backlogs:** `docs/workplan/backlog-by-module/01-…25-….md` — 25 modules, ~280 tasks total. Session 33+ source : TBD (open with backlog triage post-S32 merge).
- **Execution skill:** invoke `superpowers:subagent-driven-development` (or `superpowers:executing-plans`) before running a phase. Each phase is isolated → one subagent per phase, parallelizable per Wave.
- **Workplan layout:** `docs/workplan/{plans,specs,refs,backlog-by-module}/`. Plans/specs are **dated, append-only history** — never rewrite past plans; create a new dated file. Backlog files are living docs (update in place).
- **Migration sequence active:** Session 34 used `20260601043059` (1 migration — data-only UPDATE categories dispatch_station remap; no schema change). Session 33 used `20260618000010..023` + cloud-clock corrective `20260529200749` (15 migrations total — see S33 reference). Session 32 used `20260617000010..014` (4 migrations : `_010` bump `get_profit_loss_v1` expose `account_id` additive ; `_011` bump `get_balance_sheet_v1` expose `account_id` additive + add `lines[]` array ; `_013` create `get_orders_list_v1` RPC cursor-paginated JSONB filters ; `_014` REVOKE pair on `get_orders_list_v1` S25 canonical ; `_012` skipped — CF bump deferred S33+ DEV-S32-1.D-01 indirect method has no per-account lines). Session 31 used `20260616000010` (1 migration : seed perm `orders.read` + role_perms grants MANAGER/ADMIN/SUPER_ADMIN). Session 30 used `20260615000010..019` + `20260524231049..124` (10 migrations : inventory RPCs `_000010` `get_wastage_report_v1` + `_000011` REVOKE pair ; `_000016` `get_stock_movements_v1` + `_000017` REVOKE pair ; `_000018` `get_perishable_turnover_v1` + `_000019` REVOKE pair ; finance RPCs `_231049` `get_payments_by_method_v1` + `_231054` REVOKE pair ; `_231118` `get_pb1_report_v1` + `_231124` REVOKE pair ; 0 correctives). Session 29 used `20260606000010..023` (14 migrations : `_000010` `z_reports` table + RLS + storage bucket `zreports/` ; `_000011` REVOKE pair z_reports ; `_000012` `sign_zreport_v1` RPC + REVOKE pair ; `_000013` `void_zreport_v1` RPC + REVOKE pair ; `_000014` `get_zreport_snapshot_v1` RPC + REVOKE pair ; `_000015` bump `close_shift_v2` snapshot insert ; `_000016` REVOKE pair close_shift_v2 ; `_000017` seed perms `zreports.{read,sign,void}` ; `_000018` EF `generate-zreport-pdf` deploy ; `_000019` EF `generate-pdf` deploy ; `_000020` `reports-exports/` bucket + storage policies ; `_000021` types regen ; `_000022` corrective `close_shift_v2` non-blocking pg_net ; `_000023` corrective REVOKE anon generate-pdf EF public function. Timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 28 used `20260524111854..123026` (16 migrations block tables + RPCs + trigger + perm seed + 2 correctives : `_111854` create `expense_approval_thresholds` table ; corrective `_112433` `fix_thresholds_index_nulls_order` NULLS LAST ; `_112621` create `expense_approvals` table ; `_113023` ALTER expenses +3 cols ; `_113353` seed 3 brackets defaults ; `_114442` bump `submit_expense_v2` + extract `_emit_expense_je` helper ; corrective `_115443` `fix_submit_expense_v2_security_hardening` (auth-first + pg_temp + vat coalesce + helper REVOKE) ; `_115713` REVOKE pair submit_v2 ; `_120104` bump `approve_expense_v2` SOD + multi-step ; `_121140` REVOKE pair approve_v2 ; `_121337` `set_expense_threshold_v1` UPSERT + overlap ; `_122002` REVOKE pair set_v1 ; `_122136` `delete_expense_threshold_v1` ; `_122427` REVOKE pair delete_v1 ; `_122632` `sync_cash_expense_to_session` trigger ; `_123026` seed perms `expenses.thresholds.{read,write}` + types regen. Timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 27c used `20260524002129..012658` (19 migrations block ENUM + ALTER + trigger + 6 RPCs avec REVOKE pair + 3 correctives + perms seed : `_002129` ENUM `variant_axis_type` ; `_002210` ALTER products +4 cols variant + CHECK XOR + 2 indexes partiels ; `_002257` trigger `enforce_variant_no_nesting` + types regen ; `_003312/3325` `convert_product_to_parent_v1` + REVOKE pair ; `_003433/3441` `create_variant_v1` + REVOKE pair ; `_003538/3543` `update_variant_v1` + REVOKE pair ; `_003629/3636` `delete_variant_v1` + REVOKE pair ; `_003729/3736` `reorder_variants_v1` + REVOKE pair ; `_003827/3833` `convert_parent_to_standalone_v1` + REVOKE pair ; corrective `_005339` hard-delete parent (orphan SKU bug, products.sku UNIQUE GLOBAL) ; corrective `_005402` SKU collision pre-check sur `-PARENT` suffix ; `_005926` seed perms `products.variants.{read,write}` + types regen ; corrective `_012658` XOR partial-NULL fix sur variants soft-deleted (discovered via Wave 4 pgTAP T15) ; timestamps cloud-assigned conservés pour matcher `schema_migrations.version`). Session 26b used `20260523135820` (1 migration — `create_update_account_active_v1_rpc` ; timestamp clock-based MCP-assigned, conservé pour matcher `schema_migrations.version`, convention héritée S27). Session 26 used `20260603000010..026` (17 migrations Wave 1 DB hardening : `_010` `current_pb1_rate()` helper ; `_011` bump `create_sale_journal_entry` PB1 dynamic ; `_012` bump `create_purchase_journal_entry` fold PPN dans INVENTORY ; `_013` create `calculate_pb1_payable_v1` + drop `calculate_vat_payable` ; `_014` bump `create_sale_journal_entry` split par order_payments.method ; `_015` seed cash_movement mapping keys ; `_016` bump `record_cash_movement_v2` + drop v1 ; `_017` bump `get_profit_loss_v1` dedupe ; `_018` bump `get_balance_sheet_v1` dedupe ; `_019` seed 3200 Retained Earnings ; `_020` reclass 5910 class 6 ; `_021` deactivate 1151 NON-PKP ; `_022` `close_fiscal_period_v1` ; `_023` `get_general_ledger_v1` ; `_024` `get_trial_balance_v1` ; `_025` `create_manual_je_v1` ; `_026` seed accounting cockpit permissions). Session 27b used `20260520101735..102709` (9 migrations : `_101735` create_product_v1 RPC + `_101749` REVOKE pair ; `_101810` create_category_v1 + `_101830` update_category_v1 + `_101850` reorder_categories_v1 + `_101902/13/24` 3× REVOKE pairs ; corrective `_102709` fix reorder_categories_v1 ambiguous-id). Session 27 used `20260520022207..025140` (18 migrations rapatriées du cloud — timestamps clock-based assignés par le MCP de la session précédente, conservés pour matcher `schema_migrations.version` : `_022207` ALTER products +5 cols ; `_022244` `_022314` `_022341` 3 tables ; `_022404` JSONB col ; `_022442` seed ; `_023035` `_023314` `_023419` `_023543` 4 RPCs ; `_023915` `_023932` `_023945` `_023957` REVOKE pair ×4 ; `_024026` `_024052` `_024113` 3 perms seed ; `_025140` corrective). Session 25 used `20260602000010..099` (6 migrations applied : `10_create_tablet_order_idempotency_keys_table` + `11_bump_create_tablet_order_v2` + `12_revoke_anon_create_tablet_order_v2` + corrective `13_alter_default_privileges_revoke_from_public` + corrective `14_relax_orders_session_id_for_tablet` + corrective `15_fix_refund_order_rpc_v2_replay_record`). Session 24 used `20260601000005..099` (11 migrations applied : `005/006/007` enum + relax `orders.session_id` ; `010` `b2b_payments` ledger ; `011/012` views `view_b2b_invoices` + `view_ar_aging` ; `013` REVOKE UPDATE `customers.b2b_current_balance` ; `014` seed `B2B_PAYMENT_BANK` ; `020/021/022` 3 RPCs). Session 19 used `20260523000001..099` (7 migrations applied : Thread A `10_create_record_rate_limit_v1_rpc` + `11_schedule_rl_purge_cron` + corrective `12_fix_record_rate_limit_v1_race` ; Thread B `19_audit_logs_add_payload` + `20_add_session_timeout_to_roles` + `21_create_update_role_session_timeout_v1_rpc` + corrective `22_fix_update_role_session_timeout_v1_revoke_anon`). Session 18 used `20260522000001..099` (1 migration applied : `10_create_recipe_cost_history_v1_rpc`). Session 17 used `20260521000001..099` (7 migrations applied : 6 planned + 1 corrective `21_fix_recipe_bom_full_v1_numeric_cast`). Session 16 used `20260520000001..099` (8 planned). Session 15 used `20260519000001..162` (32 migrations). Session 14 used `20260518000001..003`. Session 13 used `20260517xxxxxx`. Keep numbering monotonic — check `supabase/migrations/` before picking the next number.

## Project Conventions (The Breakery ERP)

### Critical patterns — don't break these
- **DB target is Supabase cloud, NOT local Docker** — As of 2026-05-14, Docker / local supabase stack is **retired** on this machine. All migrations, RPCs, pgTAP tests, and types regen run against the V3 dev project on the cloud: **`ikcyvlovptebroadgtvd`** (`the-breakery-v3-dev`, region `ap-southeast-1`, Pro plan $10/mo) — dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>. Apply migrations via `mcp__plugin_supabase_supabase__apply_migration`, run SQL via `execute_sql`, regen types via `generate_typescript_types`. **DO NOT run** `pnpm db:reset`, `supabase start`, `supabase db reset`, or `bash supabase/tests/run_pgtap.sh` — they require Docker and will fail. Prod (ref `abjabuniwkqpfsenxljp`) is V2 monolith and incompatible with V3 migration lineage.
- **PIN auth fetch wrapper** — the `auth-verify-pin` EF issues HS256 JWTs that GoTrue (ES256) can't validate via the default header. The Supabase client uses a custom fetch wrapper that injects the PIN JWT on every request via `setSupabaseAccessToken` (in `packages/supabase`). Never bypass with raw `Authorization` headers or `auth.setSession`.
- **Realtime channel names must be unique per mount** — StrictMode double-mounts components and shared channel names collide silently. See `apps/pos/src/features/kds/hooks/useKdsRealtime.ts`.
- **`packages/domain` is IO-free** — no `fetch`, no Supabase, no React. Pure TS, unit-testable.
- **Order writes go through RPCs** — never raw inserts. RPCs: `complete_order_with_payment_v10` (called server-side by the `process-payment` EF — the POS does NOT call it directly; `apps/pos/src/features/payment/hooks/useCheckout.ts:124` POSTs the EF, which calls the RPC at `supabase/functions/process-payment/index.ts:149`), `pay_existing_order_v6`, `create_tablet_order_v2`, `pickup_tablet_order` (unversioned), `evaluate_promotions_v1`, `mark_item_served` (unversioned). They handle JE triggers, loyalty, promotions, table state atomically. RPC versions are monotonic — verify `supabase/migrations/` + the call-site before relying on a version.
- **`stock_movements` is an append-only ledger** — RLS revokes UPDATE/DELETE for `authenticated`. All writes go through SECURITY DEFINER RPCs (`record_stock_movement_v1` primitive ; `adjust_stock_v1`, `receive_stock_v1`, `record_incoming_stock_v1`, `waste_stock_v1`, future `*_transfer_v1` / `record_production_v1` / `finalize_opname_v1`). Never `INSERT INTO stock_movements` directly from app code or tests.
- **`stock_movements.unit` is NOT NULL** — any direct insert (tests, fixtures, RPCs) must populate `unit`. The `record_stock_movement_v1` primitive auto-resolves from `products.unit` if NULL is passed — don't bypass it. See migration `20260516000019_fix_record_stock_movement_v1_unit.sql`.
- **`stock_movements` section constraint is movement-type-aware** — `transfer_in/out` require both `from_section_id` AND `to_section_id` ; `adjustment*`, `waste`, `incoming`, `purchase`, `sale*`, `production*`, `opname*` require at least one (relaxed in migration `20260516000020`). Don't tighten without re-checking all RPCs.
- **Inventory RPCs accept `p_idempotency_key UUID`** — replay returns the existing movement row instead of doubling it. Always pass one from the client on retry-able mutations.
- **RPC versioning is monotonic** — never edit a published `_vN` signature. Create `_vN+1` and `DROP FUNCTION ... vN(<old args>)` in the same migration if replacing. See `20260516000019` (drop original `record_stock_movement_v1` then recreate with `unit`).
- **Supabase auto-grants EXECUTE on public functions to `anon`** — `REVOKE ALL FROM PUBLIC` does NOT cancel it. Always add an explicit `REVOKE EXECUTE ... FROM anon` on admin-only RPCs to enforce gate intent at the role level (defense in depth). See S19 migration `20260523000022` for an example.
- **Anon GRANT defense-in-depth (S20)** — `REVOKE ALL FROM anon ON public.*` is the project-wide default for tables, views, AND functions, future-proofed via `ALTER DEFAULT PRIVILEGES FOR ROLE postgres`. Critical caveat: `REVOKE EXECUTE ... FROM anon` on functions is INSUFFICIENT on its own — `anon` inherits EXECUTE through PUBLIC membership via the `=X/postgres` ACL entry. Future REVOKE-on-functions migrations MUST also `REVOKE EXECUTE ... FROM PUBLIC` and `ALTER DEFAULT PRIVILEGES FOR ROLE postgres ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC`. See S20 migrations `20260524000020..031`. `supabase_admin`-owned extension objects (pgtap: `pg_all_foreign_keys`, `tap_funky`, pgtap helper functions) are platform-managed and not user-revocable — pgTAP suite excludes them. If a future feature legitimately needs anon (public landing-page RPC, embeddable widget), grant explicitly per-object with `COMMENT ON FUNCTION ... IS 'anon-callable: <reason>'`.
- **PIN / auth secrets en header HTTP, jamais en body JSON (S25)** — Any EF that consumes a manager PIN or other validation secret MUST read it from a dedicated HTTP header (e.g., `x-manager-pin`), NEVER from the JSON body. Rationale : request bodies get logged by default in PostgREST access logs, pgaudit, reverse proxies, and Supabase function logs ; headers are far less commonly captured. Hard cutover pattern : drop the body field in the SAME commit as the header read — no dual-mode fallback unless the EF has uncontrolled external callers (rare on this project, only POS calls these EFs). Reference : S25 `refund-order` migration body `manager_pin` → header `x-manager-pin` (`supabase/functions/refund-order/index.ts`). Future EFs to migrate post-S25 : `void-order`, `cancel-item`, `kiosk-issue-jwt` mutation paths — sweep deferred to backlog post-S30.
- **Idempotency 2-flavors selon la sémantique (S25)** — Two distinct patterns coexist on this project ; pick the right one for the flow :
  1. **HTTP `x-idempotency-key` header (EF retry safety)** — For HTTP requests where the client may legitimately retry (flaky network, double-click, React-Query auto-retry). The client generates a UUID v4 (`crypto.randomUUID()` stored in a `useRef` so it survives re-renders), sends it via header. The EF reads via `getIdempotencyKey(req)` from the shared helper `supabase/functions/_shared/idempotency.ts` (1 export, validates UUID regex, returns `string | null`) and propagates it as `p_idempotency_key` arg to the RPC. Reference : S25 `refund-order` EF + `refund_order_rpc_v2` (the RPC already had the arg from S13, only the EF + POS wiring was missing).
  2. **RPC arg `p_client_uuid` / `p_idempotency_key` (idempotence sémantique métier)** — For RPCs where idempotency is intrinsic to the business flow (e.g., "this cart, this tap" for tablet, "this payment record" for B2B, "this stock movement record" for inventory). The arg is REQUIRED at the RPC level (NOT NULL CHECK) and used as the primary key of a **dedicated** idempotency-keys table (never as a nullable column on the business table — isolation makes REVOKE simpler and avoids polluting the metric tables). Concurrency race handled via PK `unique_violation` catch + re-read. References : `create_tablet_order_v2(p_client_uuid)` with `tablet_order_idempotency_keys` (S25), `record_b2b_payment_v1(p_idempotency_key)` with `b2b_payments.idempotency_key UNIQUE` (S24), `record_stock_movement_v1` & family (S12). Replay returns the result of the first successful execution — by convention, RPCs return either the exact same value or an envelope `{ ..., idempotent_replay: true }` so callers can audit.

### Git
- Branches: `swarm/session-N` for ongoing session work, `feat/<scope>` or `fix/<scope>` for focused PRs. For phased plans, prefer `swarm/session-N` and squash-merge per phase.
- Commits: conventional commits (`feat(scope): …`, `fix(scope): …`, `test(scope): …`, `docs(scope): …`, `refactor(scope): …`). For session 13 phases: `feat(db|domain|ui|backoffice|pos|edge|inventory): session 13 — phase X.Y — <topic>`. Co-author Claude when AI-assisted.

## Agent Comms (SendMessage-First Coordination)

Named agents coordinate via `SendMessage`, not polling or shared state.

```
Lead (you) ←→ architect ←→ developer ←→ tester ←→ reviewer
              (named agents message each other directly)
```

### Spawning a Coordinated Team

```javascript
// ALL agents in ONE message, each knows WHO to message next
Agent({ prompt: "Research the codebase. SendMessage findings to 'architect'.",
  subagent_type: "researcher", name: "researcher", run_in_background: true })
Agent({ prompt: "Wait for 'researcher'. Design solution. SendMessage to 'coder'.",
  subagent_type: "system-architect", name: "architect", run_in_background: true })
Agent({ prompt: "Wait for 'architect'. Implement it. SendMessage to 'tester'.",
  subagent_type: "coder", name: "coder", run_in_background: true })
Agent({ prompt: "Wait for 'coder'. Write tests. SendMessage results to 'reviewer'.",
  subagent_type: "tester", name: "tester", run_in_background: true })
Agent({ prompt: "Wait for 'tester'. Review code quality and security.",
  subagent_type: "reviewer", name: "reviewer", run_in_background: true })

// Kick off the pipeline
SendMessage({ to: "researcher", summary: "Start", message: "[task context]" })
```

### Patterns

| Pattern | Flow | Use When |
|---------|------|----------|
| **Pipeline** | A → B → C → D | Sequential dependencies (feature dev) |
| **Fan-out** | Lead → A, B, C → Lead | Independent parallel work (research) |
| **Supervisor** | Lead ↔ workers | Ongoing coordination (complex refactor) |

### Rules

- ALWAYS name agents — `name: "role"` makes them addressable
- ALWAYS include comms instructions in prompts — who to message, what to send
- Spawn ALL agents in ONE message with `run_in_background: true`
- After spawning: STOP, tell user what's running, wait for results
- NEVER poll status — agents message back or complete automatically

## Swarm & Routing

### Config
- **Topology**: hierarchical-mesh (anti-drift)
- **Max Agents**: 15
- **Memory**: hybrid
- **HNSW**: Enabled
- **Neural**: Enabled

```bash
npx @claude-flow/cli@latest swarm init --topology hierarchical --max-agents 8 --strategy specialized
```

### Agent Routing

| Task | Agents | Topology |
|------|--------|----------|
| Bug Fix | researcher, coder, tester | hierarchical |
| Feature | architect, coder, tester, reviewer | hierarchical |
| Refactor | architect, coder, reviewer | hierarchical |
| Performance | perf-engineer, coder | hierarchical |
| Security | security-architect, auditor | hierarchical |

### When to Swarm
- **YES**: 3+ files, new features, cross-module refactoring, API changes, security, performance
- **NO**: single file edits, 1-2 line fixes, docs updates, config changes, questions

### 3-Tier Model Routing

| Tier | Handler | Use Cases |
|------|---------|-----------|
| 1 | Agent Booster (WASM) | Simple transforms — skip LLM, use Edit directly |
| 2 | Haiku | Simple tasks, low complexity |
| 3 | Sonnet/Opus | Architecture, security, complex reasoning |

## Memory & Learning

### Before Any Task
```bash
npx @claude-flow/cli@latest memory search --query "[task keywords]" --namespace patterns
npx @claude-flow/cli@latest hooks route --task "[task description]"
```

### After Success
```bash
npx @claude-flow/cli@latest memory store --namespace patterns --key "[name]" --value "[what worked]"
npx @claude-flow/cli@latest hooks post-task --task-id "[id]" --success true --store-results true
```

### MCP Tools (use `ToolSearch("keyword")` to discover)

| Category | Key Tools |
|----------|-----------|
| **Memory** | `memory_store`, `memory_search`, `memory_search_unified` |
| **Bridge** | `memory_import_claude`, `memory_bridge_status` |
| **Swarm** | `swarm_init`, `swarm_status`, `swarm_health` |
| **Agents** | `agent_spawn`, `agent_list`, `agent_status` |
| **Hooks** | `hooks_route`, `hooks_post-task`, `hooks_worker-dispatch` |
| **Security** | `aidefence_scan`, `aidefence_is_safe`, `aidefence_has_pii` |
| **Hive-Mind** | `hive-mind_init`, `hive-mind_consensus`, `hive-mind_spawn` |

### Background Workers

| Worker | When |
|--------|------|
| `audit` | After security changes |
| `optimize` | After performance work |
| `testgaps` | After adding features |
| `map` | Every 5+ file changes |
| `document` | After API changes |

```bash
npx @claude-flow/cli@latest hooks worker dispatch --trigger audit
```

## Agents

**Core**: `coder`, `reviewer`, `tester`, `planner`, `researcher`
**Architecture**: `system-architect`, `backend-dev`, `mobile-dev`
**Security**: `security-architect`, `security-auditor`
**Performance**: `performance-engineer`, `perf-analyzer`
**Coordination**: `hierarchical-coordinator`, `mesh-coordinator`, `adaptive-coordinator`
**GitHub**: `pr-manager`, `code-review-swarm`, `issue-tracker`, `release-manager`

Any string works as a custom agent type.

### Project agents & skills (The Breakery — adaptés au projet)

Au-delà des agents génériques ci-dessus, le repo versionne une **équipe spécialisée** (PR #55). Les **agents** (`.claude/agents/*.md`) sont spawnables via la tâche Agent ; les **skills** (`.claude/skills/<name>/SKILL.md`) s'auto-déclenchent par `pathPatterns`/`promptSignals` quand on touche leur domaine. Chacun pointe vers ce CLAUDE.md comme source de vérité et vérifie le schéma réel (migrations/MCP) avant d'affirmer un fait.

**Agents** (`.claude/agents/`) :
- `pos-specialist` (sonnet) — `apps/pos/`
- `backoffice-specialist` (sonnet) — `apps/backoffice/`
- `db-engineer` (sonnet) — `supabase/migrations/` + RPCs (versioning, REVOKE pairs, MCP V3)
- `edge-functions-engineer` (sonnet) — `supabase/functions/` (PIN header, idempotency, rate-limit)
- `pattern-guardian` (sonnet, **read-only**) — revue de diff vs les Critical patterns ci-dessous
- `test-engineer` (sonnet) — pgTAP/Vitest/smoke + baseline env-gated
- `session-coordinator` (**opus**) — orchestration `swarm/session-N` (spec→plan→waves→closeout)

**Skills** (`.claude/skills/`, auto-déclenchés) :
- `stock-management` — inventory/recipes/production/WAC/lots
- `accounting` — COA/JE/PB1 NON-PKP/fiscal/GL/TB · `b2b-credit` — AR/credit-limit/b2b_payments
- `reports-exports` — report RPCs/PDF/CSV/Z-report/drill-down · `expense-governance` — thresholds/SOD/multi-step
- `products-catalog` — products CRUD/variants/categories · `orders` — lifecycle/list v2/edit-items/void/refund
- `security-auth` — RLS/REVOKE/perms/PIN-JWT/rate-limit · `breakery-ui-kit` — conventions `packages/ui`

Conception : `docs/superpowers/specs/2026-05-31-agents-skills-team-design.md` (spec) + `docs/superpowers/plans/2026-05-31-agents-skills-team.md` (plan). `.gitignore` versionne les `.md` racine de `.claude/agents/` ; les sous-dossiers ruflo restent ignorés.

## Build & Test

- ALWAYS run tests after code changes
- ALWAYS verify build succeeds before committing
- This project uses **pnpm 9.15** + **turbo** — never `npm`

### Local commands (no Docker required)
```bash
pnpm build && pnpm test     # turbo run build / turbo run test --concurrency=1
pnpm typecheck               # turbo run typecheck
```

### DB workflow — Supabase cloud staging (Docker retired 2026-05-14)
All DB operations target the cloud V3 dev project `ikcyvlovptebroadgtvd`. **Do NOT run** `pnpm db:reset`, `supabase start`, or `bash supabase/tests/run_pgtap.sh` — Docker is gone.

| Operation | MCP tool | Notes |
|---|---|---|
| Apply migration | `mcp__plugin_supabase_supabase__apply_migration` | `project_id='ikcyvlovptebroadgtvd'`, `name` in snake_case, body = SQL. Wrapped in transaction. |
| Run SQL (incl. pgTAP) | `mcp__plugin_supabase_supabase__execute_sql` | Use `BEGIN ... ROLLBACK` envelope for pgTAP. Extension `pgtap` already enabled. |
| Regen types | `mcp__plugin_supabase_supabase__generate_typescript_types` | Returns `{ types: "..." }` — write to `packages/supabase/src/types.generated.ts` and commit. |
| Check drift | `mcp__plugin_supabase_supabase__list_migrations` | Compares `supabase_migrations.schema_migrations` to local. |
| Direct psql (rare) | `postgresql://postgres.ikcyvlovptebroadgtvd:<URL_ENCODED_PWD>@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres` | Always go through the pooler — `db.<ref>.supabase.co` has no DNS A record. |

Dashboard: <https://supabase.com/dashboard/project/ikcyvlovptebroadgtvd>

### Targeted iteration (much faster than full suite during phase work)
```bash
pnpm --filter @breakery/supabase test inventory     # Vitest live RPC tests
pnpm --filter @breakery/backoffice test inventory   # BO smoke + unit
pnpm --filter @breakery/domain test inventory       # pure-TS unit
```

After Supabase schema changes (new migration via MCP `apply_migration`), **always** regen types via `mcp__plugin_supabase_supabase__generate_typescript_types`, write to `packages/supabase/src/types.generated.ts`, and commit. A missing regen is the #1 cause of broken CI on this repo.

### Inventory phase test layout
- pgTAP (DB): `supabase/tests/inventory.test.sql` (steady-state suite) + `supabase/tests/inventory_phase1_complete.test.sql` (phase 1 acceptance — T1-T15+).
- Vitest live RPC: `supabase/tests/functions/inventory-*.test.ts` (per-phase, one file per RPC family).
- Domain unit: co-located `__tests__/` in `packages/domain/src/inventory/`.
- BO smoke/unit: co-located `__tests__/` in `apps/backoffice/src/features/inventory*/`.

## CLI Quick Reference

```bash
npx @claude-flow/cli@latest init --start-all         # Setup (already done)
npx @claude-flow/cli@latest swarm init --v3-mode     # Start swarm
npx @claude-flow/cli@latest memory search --query "" # Vector search
npx @claude-flow/cli@latest hooks route --task ""    # Route to agent
npx @claude-flow/cli@latest doctor --fix             # Diagnostics
npx @claude-flow/cli@latest security scan            # Security scan
npx @claude-flow/cli@latest performance benchmark    # Benchmarks
```

26 commands, 140+ subcommands. Use `--help` on any command for details.

## Setup

```bash
claude mcp add claude-flow -- npx -y @claude-flow/cli@latest
npx @claude-flow/cli@latest daemon start
npx @claude-flow/cli@latest doctor --fix
```

**Agent tool** handles execution (agents, files, code, git). **MCP tools** handle coordination (swarm, memory, hooks). **CLI** is the same via Bash.
