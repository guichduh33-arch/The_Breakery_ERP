# Plafond compté des classes de couleur mortes, hérité au 2026-08-09.
#
# Format : <compte>\t<chemin>\t<classe>
#
# PLAFOND, JAMAIS PLANCHER. Cette liste ne peut que DÉCROÎTRE : elle recense ce
# qui existait le jour où la garde 3 est née, pour qu'elle puisse être allumée
# sans réécrire 25 features d'un coup. Toute occurrence NOUVELLE échoue.
#
# La résorption se fait au fil des éditions : quand tu touches un de ces
# fichiers, corrige ses classes mortes et resserre le plafond avec
#   node scripts/ci/tailwind-dead-classes.mjs --update-baseline
# Le drapeau ne sait que descendre un compte ; il ne peut pas faire passer une
# violation.
#
# LA SEULE ENTRÉE NEUVE LÉGITIME : un ÉLARGISSEMENT DU DÉTECTEUR. « Ne peut que
# décroître » porte sur le DÉFAUT, pas sur le nombre de lignes de ce fichier. Le
# jour où la garde apprend à voir une famille qu'elle ignorait, elle découvre des
# occurrences qui existaient déjà et qu'elle n'avait jamais comptées : la liste
# s'allonge sans qu'aucune classe morte ait été écrite. C'est arrivé le
# 2026-08-17, quand la liste noire du vocabulaire shadcn/ui a été ajoutée —
# `accent-primary` est entré ici pour cette raison, et non parce qu'un auteur
# l'aurait introduit.
# Le test qui les distingue : une entrée ajoutée par élargissement pointe une
# ligne de code INCHANGÉE par le diff qui l'ajoute. Une entrée ajoutée par
# régression pointe une ligne que ce même diff écrit — celle-là est refusée, sans
# exception. Un ajout par élargissement se documente en une ligne sous
# « Dernier resserrement », en nommant ce que le détecteur voit désormais.
#
# Dernier resserrement : 2026-08-24
# Dernier élargissement du détecteur : 2026-08-17 (liste noire shadcn/ui —
#   +1 entrée préexistante rendue visible : `accent-primary`)
1	apps/pos/src/features/payment/split/SplitPaymentFlow.tsx	to-payment
1	packages/ui/src/components/TableSelectorModal.tsx	bg-green-soft
